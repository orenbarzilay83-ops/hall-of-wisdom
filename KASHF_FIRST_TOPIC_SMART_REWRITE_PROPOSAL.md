# KASHF_FIRST_TOPIC_SMART_REWRITE_PROPOSAL — הצעת-שינוי ראשונה: נושא `commerce`

> **מסמך הצעה בלבד. לא נכתב קוד, לא שונה מנוע, לא שונה HTML, לא חוּוַט קובץ, לא חובר AI חי, לא נוסף secret, לא בוצע deploy, לא נוצר storage/database/embeddings/UI, לא נגע ב-`inner-compass`. לא בוצע commit/push — רק יצירת הקובץ.**
> תאריך: 2026-07-08. הדוגמה-הראשונה-האמיתית ל-`KASHF_ARCHITECTURE_ADVISOR_BRAIN_SPEC.md`, בפורמט-7-הסעיפים שהמסמך ההוא הגדיר.

---

## 1. איזה נושא נבחר

**`commerce`** — "מסחר ועסקים — מכירה וקנייה".

**איפה בקוד:** `goral-hachol/engine/kashf-topic-rules.js`, שורות 1397–1463 (בלוק `commerce:` בתוך `KASHF_TOPIC_RULES`).

**למה נבחר — לא ניחוש, נבדק בפועל על כל 29 הנושאים:**

בדיקה שיטתית (סקריפט-python על כל בלוקי-הנושא) גילתה שרוב-הנושאים (25/29) לא-משתמשים בכלל בטיפוס `count-quality` (הטיפוס שאוסף-נקודות-מכמה-בתים-ומצמצם ל-3-5 משפטים-קבועים בלבד — הדפוס-הכי-רובוטי, כי הוא **מטבעו** מתעלם-מאיזו-צורה-ספציפית-נחתה-בכל-בית). רק **4 נושאים** משתמשים ב-`count-quality`: `hiddenTreasure` (6 בתים), `lostAnimal` (2 בתים), `commerce` (**7 בתים**), `religion` (2 בתים). **`commerce` הוא בעל-מספר-הבתים-הגבוה-ביותר מבין ארבעתם** — כלומר הכי-הרבה-שילובי-צורות-אפשריים מצטמצמים ל-**5 משפטים-קבועים בלבד** (`allSaad`/`allNahs`/`mostSaad`/`mostNahs`/`mixed`, ראו סעיף 2).

**יתרון-אסטרטגי נוסף (לא-רק-מבני):** `commerce` **כבר קיים גם בחאווי** (`goral-hachol/data/sources/hawi/question-rules/hawi-question-commerce.js`) — **וכבר תוקן שם** (WORKPLAN #26, שכבת-הסינתזה `narrative-fact-phrasing.js` יושמה על נושא-מסחר-בחאווי כהוכחת-היתכנות-ראשונה). זה מאפשר **השוואה-ישירה-לפני/אחרי** מול תקדים-מוצלח-ומתועד, ושימוש-חוזר בלקחים שכבר-נלמדו (למשל תיקון-הבאג "נלחם" — פסיקה-ממוזגת שלא-נוסחה-כמעורבת).

## 2. מה הבעיה היום

**איך המנוע בונה מסקנה היום** (מצוטט-ישירות מהקוד):
```
primaryFormula: count-quality על בתים [1, 2, 4, 7, 11, 15, 16]
→ סופר כמה מהם "מיטיבים" (saad) מול "מזיקים" (nahs)
→ מצמצם לאחת מ-5 קטגוריות: allSaad / allNahs / mostSaad / mostNahs / mixed
→ כל קטגוריה = משפט אחד קבוע-מראש (17-30 תווים בממוצע)

altFormula: house-quality על בית [15] בלבד
→ saad / nahs / mixed → משפט קבוע נוסף

supportingChecks: 4 בדיקות (2× house-in-house-check בינארי, 2× legacy-fn מחושב)
```

**למה זה יוצא רובוטי:** שני לוחות שונים-לגמרי (למשל: בית 1="כבוד נכנס", בית 2="חיבור" מול בית 1="דרך", בית 2="נשוא ראש" — שתי צורות-שונות-בכל-בית) — אם שניהם נופלים תחת `mostSaad`, **שניהם יקבלו את אותו המשפט המדויק**: "רוב הסימנים נוטים להשלמת העסקה בטוב". שם-הצורה, ה"מהות" שלה, וההבדל בין-מיטיבה-פשוטה-למיטיבה-ממוזגת — **אף אחד מהם לא בא לידי ביטוי בטקסט**. זו בדיוק ההפרה של `OREN_CONCLUSION_STYLE_SPEC.md` כלל 4 ("שני לוחות שונים לא אמורים להפיק טקסט זהה").

**אילו פרמטרים קיימים אבל לא-מדורגים:** כל 7 הבתים (1,2,4,7,11,15,16) נספרים **באותו משקל** — אין הבחנה בין בית-1 (המבקש עצמו) לבית-11 (תקווה/רווח, בית-משני-יותר-בהקשר-מסחרי). שער 6 בספר (עמ' 218) עצמו-כן-מבחין בין-תפקידי-הבתים ("עשה את הראשון והשני לקונה, את השמיני והשביעי למוכר, את העשירי למתווך, ואת החמישה־עשר לאחרית") — **ההבחנה קיימת במקור, אך לא-מיושמת בניקוד-בפועל**.

**האם יש סימנים חזקים/חלשים שלא-מופרדים:** כן, בבירור. שני ה-`supportingChecks` שהם `legacy-fn` (`computeGoodsProfitLossKashf`, `computeRequesterCircleStrengthKashf`) **כן מחשבים ממצא-אמיתי-ומפורט** — אך הם מוצגים כשווים-בערך לשני בדיקות ה-`house-in-house-check` הבינאריות (קונה-מול-מוכר, נדיבות-הקונה). אין שום `strongestSignal`/`weakSignal` שמבדיל ביניהם.

## 3. למה זו בעיה לפי הספר / הקוד

- **הספר עצמו** (`kashf-al-asrar.html`/`kashf-al-asrar-book.js`, מצוטט ב-`sourceRef: 'כשף אל-אסרר, עמ׳ 218, 223'`) מתאר תפקידי-בתים-שונים (קונה/מוכר/מתווך/אחרית) — לא "ספירת-נקודות-שווה". הקוד **כבר מצטט** את זה (`sourceText` בקוד) אך **לא-מיישם** את ההבחנה בפועל בחישוב-הפסיקה.
- **קבצי-מנוע קשורים:** `kashf-topic-rules.js` (מקור הכללים), `kashf-formula-engine.js` (מבצע `executeFormula`/`count-quality`), `kashf-narrative-writer.js::writeConclusionPara` (מנסח את הפלט-ללקוח מ-`reading`).
- **ידע קיים שלא-מנוצל:** `kashf-figure-names.js` (בתיקיית `data/sources/kashf-al-asrar/`) מכיל שדה `fortuneHebrew` בסולם-4-דרגות (מיטיב / ממוזג-מיטיב / ממוזג-מזיק / מזיק) לכל 16 הצורות — **בדיוק סוג-הנתון שכבר-שימש לבניית `narrative-fact-phrasing.js` בחאווי** — קיים, אך לא-מיושם-על-כשף.
  **הערת-זהירות שנמצאה תוך-כדי הבדיקה:** קבוע היצוא בקובץ הזה נקרא בטעות `HAWI_FIGURE_NAMES` (לא `KASHF_FIGURE_NAMES`) למרות שהוא יושב תחת `data/sources/kashf-al-asrar/` ומצטט את הספר הערבי-כשף כמקור (`كشف الأسرار المصونة...، pages 43-67`) — **כנראה שריד-שם מתקופת-טרום-הפרדת-החאווי/כשף** (ראו WORKPLAN #16). זו הערת-תיוג-בלבד, לא נוגעת-לתוכן — אך רלוונטית-ישירות לכלל "אין לערבב חאווי וכשף" (סעיף 5) ולכן מסומנת-כאן כדי שלא-תתפספס בעת מימוש-עתידי.

## 4. איזה שינוי ארכיטקטוני מוצע (תכנון בלבד, לא קוד)

| שכבה | נדרשת לנושא הזה? | הנימוק |
|---|---|---|
| **signWeighting** | **כן** | בית 1 (המבקש) ובית 15 (אחרית) צריכים משקל-גבוה-יותר מבית-11 (תקווה, משני) — הספר עצמו מבחין בתפקידים (סעיף 3), רק לא מנוצל |
| **certaintyLevel** | **כן** | `count-quality` **כבר** מייצר אות-חלקי (allSaad=ודאות-גבוהה, mostSaad=ודאות-בינונית, mixed=נמוכה) — צריך רק **לחשוף** את זה כשדה-מפורש, לא לחשב-מחדש |
| **advisorDiagnosis** | **כן** | כל 4 ה-`supportingChecks` + `sourceText` המצוטט — צריכים להיאסף לאובייקט-נפרד-ומוגדר, לא רק "קטע-HTML-אחרי-toggle" |
| **clientWording** | **כן** | להחליף את 5 המשפטים-הקבועים (`verdictByCountQuality`) בבנייה-מ-שם-הצורה+`fortuneHebrew`+תפקיד-הבית, **באותה שיטה שכבר עבדה בחאווי** (`narrative-fact-phrasing.js`, עם גרסה-מותאמת-לנתוני-כשף — לא לייבא ישירות מ-Hawi, ראו סעיף 5) |
| **strongestSignals / weakSignals** | **כן** | ה-2 בדיקות `legacy-fn` (מחושבות, מפורטות) הן strongestSignals מובנים-מאליהם; ה-2 בדיקות `house-in-house-check` (בינאריות) הן weakSignals |
| **contradictions** | **כן** | אם `primaryFormula` (7-בתים) ו-`altFormula` (בית-15-בלבד) חולקים — צריך גילוי-מפורש, **בדיוק כמו שכבר קיים** ב-`writeShortVerdictBox`/`writeConclusionPara` הכלליים (WORKPLAN #18) — כאן זה רק-לוודא-שהמנגנון-הכללי-מכסה-גם-את-commerce, לא לבנות-חדש |
| **practicalGuidance** | **חלקית** | כרגע אין המלצה-מעשית-מפורשת בנושא-הזה בכשף (בניגוד-לחאווי, ששם יש "הדיין תומך... מומלץ להמשיך" — WORKPLAN #26). שווה-לשקול, אך **לא-חובה** לגרסה-ראשונה |

## 5. מה אסור לשנות

- **לא לשנות את חישוב הגורל עצמו** — `executeFormula`/`count-quality`/הלוגיקה שקובעת `allSaad`/`mostSaad`/וכו' **נשארת בדיוק כמו-שהיא**. השינוי הוא **רק** בשכבת-הניסוח (איך המשפט-הסופי נבנה), לא בשכבת-הפסיקה (מה נחשב-חיובי/שלילי).
- **לא לשנות את מקור-הנתונים** — `sourceRef`/`sourceText` המצוטטים ב-`kashf-topic-rules.js` נשארים-כמות-שהם.
- **לא לערבב חאווי וכשף** — **אסור** לייבא `narrative-fact-phrasing.js` הקיים (בנוי-על `FIGURE_ESSENCE` של חאווי) ישירות לתוך קוד-כשף. אם נדרשת שכבת-סינתזה-דומה לכשף — היא נבנית **כקובץ-נפרד** (או פונקציה-חדשה, שם-מפורש-שונה) שמבוססת על `kashf-figure-names.js` (עם התיקון-לשם-הקבוע-שצוין-בסעיף-3), לא-על נתוני-חאווי.
- **לא להשתמש בקובץ הערבי כמקור מאושר** — `kashf-al-asrar-arabic-only.html` נשאר בלתי-מאושר (`OREN_SMART_ADVISOR_KASHF_HTML_CONTENT_AUDIT.md`) — כל ציטוט-חדש נשען אך-ורק על `kashf-al-asrar-book.js`/`kashf-al-asrar.html` (התרגום העברי המאושר).
- **לא למחוק `verdict` קיים לפני שיש fallback** — 5 המשפטים-הקבועים ב-`verdictByCountQuality` נשארים-בקוד כ-fallback (בדיוק כמו ש-`anthropic-provider.js` שכבר-נבנה תמיד-נופל-חזרה-לפלט-דטרמיניסטי) — לא נמחקים לפני שהניסוח-החדש מוכח-עובד.
- **לא לשבור נושאים אחרים** — השינוי מוגבל ל-`commerce` בלבד; `kashf-topic-rules.js` הוא קובץ-משותף-ל-29-נושאים, כל עריכה בו חייבת להיות ממוקדת-ומאומתת-שלא-נוגעת-בבלוקים-האחרים.

## 6. בדיקות חובה אחרי שינוי עתידי

1. **מקרה פשוט (allSaad ברור):** לוח שבו 6-7 מתוך 7 הבתים מיטיבים-פשוטים (לא ממוזגים) — לוודא שהמשפט-החדש מזכיר את שמות-הצורות-הספציפיים בבתים-המרכזיים (1, 15) ולא רק "allSaad" גנרי.
2. **מקרה מורכב (מעורב, mixed):** לוח עם ~3-4 מיטיבים ו-3-4 מזיקים — לוודא שהניסוח-החדש משקף-בבירור "לא ודאי" ולא-מכריע-לכיוון-מזויף (כמו שכבר-נדרש ב-`OREN_CONCLUSION_STYLE_SPEC.md` כלל 2).
3. **מקרה עם סימנים סותרים (primary מול alt):** לוח שבו `primaryFormula`=חיובי אך בית 15 (`altFormula`) שלילי — לוודא שגילוי-הסתירה (WORKPLAN #18) עדיין-מופיע, ושלא-מודחק על-ידי-הניסוח-החדש.
4. **מקרה עם ודאות-נמוכה (רוב-הבתים-ממוזגים):** לוח שבו רוב-7-הבתים הם צורות-`ממוזג-מיטיב`/`ממוזג-מזיק` (לא-נקיות) — לבדוק שהניסוח-החדש **אומר "ממוזגת" במפורש** (בדיוק כמו תיקון-הבאג "נלחם" בחאווי, WORKPLAN #26) ולא מתעלם-מהניואנס.
5. **מקרה שבו צריך fallback:** קלט-פגום/חסר (לוח-לא-שלם, שגיאה ב-`executeFormula`) — לוודא שהמנוע חוזר-למשפט-הקבוע-הישן (`verdictByCountQuality`) בלי לקרוס, בדיוק כמו שכל שאר-28-הנושאים כבר-מתנהגים היום.

## 7. נוסח מדויק להעברה ל-Claude Code

> **אורן משה, ההוראה המוכנה להעברה (רק כשתחליט להתקדם בפועל):**
>
> "תרחיב את שכבת-הסינתזה לנושא `commerce` בכשף אל-אסראר.
>
> **קובץ לפתוח:** `goral-hachol/engine/kashf-topic-rules.js`, בלוק `commerce:` (שורות ~1397-1463), ו-`goral-hachol/engine/kashf-narrative-writer.js` (פונקציית `writeConclusionPara`).
>
> **מה לשנות:** במקום שהניסוח-הסופי ישתמש ישירות ב-5 המשפטים-הקבועים תחת `verdictByCountQuality`, תבנה משפט שמזכיר את שם-הצורה-הספציפית שנחתה בבית 1 ובבית 15 (הכי-משמעותיים לפי הספר, עמ' 218) + ה-`fortuneHebrew` שלה (מ-`kashf-figure-names.js`), ומחבר אליו את ההקשר-הכללי (allSaad/mostSaad/וכו') כרקע-תומך, לא כמשפט-יחיד. תבנה קובץ-עזר נפרד (לא לייבא את `narrative-fact-phrasing.js` של חאווי — ראו איסור בסעיף 5 של `KASHF_FIRST_TOPIC_SMART_REWRITE_PROPOSAL.md`) שמבוסס על מבנה-הנתונים של כשף.
>
> **מה לא לשנות:** `executeFormula`/הלוגיקה שקובעת `allSaad`/`mostSaad`/`mixed` עצמה. `sourceRef`/`sourceText` המצוטטים. שום נושא אחר מתוך 29-הנושאים ב-`kashf-topic-rules.js`. `kashf-figure-appearance.js` (אסור-לגעת, `AGENTS.md` כלל 13).
>
> **לפני שתתחיל לכתוב קוד — הצג לי תוכנית:** אילו שדות תוסיף, איך ייראה הפלט-לפני/אחרי על 2-3 דוגמאות-לוח שונות, ואיזה קובץ-עזר-חדש (אם בכלל) תיצור.
>
> **בדיקות שחובה להריץ אחרי המימוש** (לפני כל commit): 5 המקרים שמפורטים בסעיף 6 של `KASHF_FIRST_TOPIC_SMART_REWRITE_PROPOSAL.md` (פשוט/מורכב/סותר/ודאות-נמוכה/fallback) — לרוץ על-לוחות-אמיתיים, לא-רק-תיאורטית.
>
> **עצור לפני commit/push** — תציג לי את הפלט-בפועל על כל 5 המקרים, ותחכה לאישורי לפני שאתה שומר או דוחף משהו."

---

## הצהרות

- שום קוד לא נכתב, שום מנוע/HTML לא נערך, שום קובץ לא חוּוַט.
- שום AI חי, secret, deploy, storage/database/embeddings/UI — לא נוצרו.
- שום נגיעה ב-`inner-compass`.
- שום commit/push — רק יצירת קובץ המסמך הזה.
- הצעד הבא — לפי החלטת אורן משה בלבד.
