# OREN_SMART_ADVISOR_OPEN_DECISIONS — שאלות/החלטות פתוחות לפני בניית הבינה הפנימית

> **מסמך בירור בלבד. אין כאן קוד, אין HTML, אין מנוע, אין AI חי, אין secret, אין deploy, אין storage, אין embeddings, אין UI. לא בוצע commit/push — רק יצירת הקובץ.**
> תאריך: 2026-07-08. ריכוז ישיר של סעיף 6 ("שאלות פתוחות") מתוך `OREN_SMART_ADVISOR_BRAIN_MAP.md` — לצורך החלטה, לא לצורך ביצוע. שלב 8 (`OREN_SMART_ADVISOR_BRAIN_PLAN.md`) **לא יתחיל** לפני שיש תשובות כאן.

---

## 1. איפה יאוחסן הידע הפרטי

שלוש אפשרויות (לא-בלעדיות זו-את-זו) שכבר עלו ב-`OREN_SMART_ADVISOR_BRAIN_PLAN.md` סעיף 6, לתיעדוף כאן:

| אפשרות | יתרון | חיסרון |
|---|---|---|
| **Supabase Storage** (private bucket) | הפרויקט כבר משתמש ב-Supabase (`supabase-client.js` הקיים לאימות) — אין ספק-ענן חדש; טבעי לקבצים גולמיים (סריקות/PDF) | לא-מובנה לחיפוש-לפי-שדה (עמוד/נושא) בלי שכבה נוספת |
| **Supabase Database** (טבלה למטא-דאטה + יחידות-ידע מפורקות) | חיפוש-מובנה לפי ספר/עמוד/נושא/שפה; תואם למבנה הקיים כבר בקוד (`KASHF_AL_ASRAR_PAGES` — מערך-רשומות עם אותם שדות בדיוק) | דורש הגדרת-סכמה; לא מתאים-לבד לקבצים-גולמיים גדולים |
| **קבצים פרטיים מחוץ לריפו** (למשל דיסק-ענן נפרד, לא Supabase) | הפרדה מוחלטת מכל תשתית-האתר הקיימת | מערכת נוספת לתחזק; לא משולב עם login הקיים; פחות טבעי לגישה מ-Edge Function |

**שאלה להכרעה:** האם לשלב Storage (לקבצים גולמיים/סריקות) + Database (למטא-דאטה/יחידות-ידע-מפורקות) יחד — בדומה למבנה שכבר קיים ל-`kashf-al-asrar-book.js` אך בפרטיות (לא בריפו)? או להתחיל צר יותר?

**אפשרות עתידית ל-vector search / embeddings:** נשארת **מפורשות שלב-מאוחר** (לא בשלב-ההחלטה הזה, ולא בשלב-הבנייה-הראשון) — רק כשמאגר-הידע גדול-מספיק שחיפוש-מילות-מפתח כבר לא מספיק. אין צורך להחליט עליה עכשיו.

## 2. איך מכניסים ספרים

מדיניות-המקורות **שונה לפי שיטה** — אין כלל-כללי אחד של "ערבית+עברית תמיד יחד" לכל המערכת:

- **בכשף אל-אסראר:** עובדים עם שני המקורות יחד:
  1. המקור הערבי — كشف الأسرار المصونة في إخراج الضمائر المخزونة.
  2. התרגום העברי של אורן משה — חשיפת הסודות הנצורים.
  (כמו `arabicText`+`hebrewTranslation` הקיימים כבר ב-`kashf-al-asrar-book.js`.)
- **בחאווי:** עובדים רק עם מקורות-חאווי שאורן משה אישר במפורש לשיטת חאווי — לא מוחל עליהם אותו מבנה-שני-מקורות של כשף אלא אם יאושר בנפרד.
- **בכל מקור אחר:** לא משתמשים בלי אישור מפורש חדש מאורן משה — לכל מקור בנפרד.
- **כלל-העל:** אם יש מקור בשפת-המקור וגם תרגום מאושר של אורן משה — שפת-המקור נשארת מקור-יסוד, והתרגום המאושר משמש מקור-עבודה-והבנה. (זה מה שחל בפועל כרגע רק על כשף, כי זהו המקור היחיד שיש לו כרגע גם-מקור-וגם-תרגום מאושרים.)
- **חלוקה לפי ספר / עמוד / נושא / שיטה** — יחידת-ידע אטומית = {ספר, עמוד, נושא/topics, שיטה (חאווי/כשף/אחר)}, לא בלוק-טקסט-ענק. תואם למבנה-הקיים.
- **שמירת איכות OCR** — שדה חדש לכל יחידה (למשל `ocrQuality: "סרוק-נקי" | "סרוק-מטושטש" | "תמלול-ידני-מאומת"`) — כדי שהבינה תדע מתי-להזהיר "כדאי לאמת ידנית" במקום להציג ודאות-שווא.
- **שמירת מונחים בערבית** — לא לתרגם-ולאבד את המקור; מונחים טכניים (שמות-צורות, שמות-שיטות) נשמרים בערבית לצד התרגום, כפי שכבר נהוג בטבלת 16 הצורות ב-`CLAUDE.md`.

**שאלה להכרעה:** מי מבצע את הפירוק-ליחידות בפועל — הזנה-ידנית של אורן משה, או תהליך-הזנה-מודרך (טופס/סקריפט שמסייע לפרק ולתייג, בלי AI-חי בשלב הזה)? זה קובע את היקף-העבודה בפועל של שלב 8.

## 3. איך הבינה תדע לצטט מקור

כל תשובה שמתבססת על ספר חייבת לכלול, בלי יוצא-מן-הכלל:

- **שם ספר** — במלואו, לא קיצור.
- **עמוד** — מספר-עמוד מדויק כפי שנרשם ביחידת-הידע.
- **שיטה** — חאווי / כשף (לעולם לא "גורל החול" סתמי — ראו סעיף 6 להלן, "לא לערבב").
- **שפה** — ערבית/עברית/שתיהן — כדי שאורן יידע אם הוא רואה תרגום או מקור.
- **רמת ביטחון** — נגזרת מ-`ocrQuality`+האם התרגום אושר-ידנית או עדיין-ראשוני. תואם לעיקרון `sourceStatus` הקיים כבר בכל רשומת-נתונים באפליקציה.

**שאלה להכרעה:** פורמט-ציטוט קבוע אחד (תבנית-טקסט אחידה בכל תשובה), או תלוי-הקשר? מומלץ תבנית-אחידה כדי שאורן ילמד-לזהות-אותה מהר.

## 4. איך מגינים על הממשק הפנימי

- **רק משתמש מחובר** — login הקיים כבר (`index.html`/`supabase.auth`) הוא הבסיס הטבעי, לא מסך-אימות נפרד.
- **רק אורן משה** — login-בלבד לא מספיק (כל מי שיש-לו-חשבון-לקוח יכול היום להתחבר לאתר) — נדרשת בהכרח שכבת-הרשאה נוספת (למשל דגל `isAdmin`/`role` על המשתמש, נבדק לפני שהמסך-הפנימי בכלל נטען).
- **לא פתוח ללקוחות** — לא רק "לא-מקושר-מהתפריט" (הסתרה-דרך-אי-קישור היא לא אבטחה) — חייבת חסימה אמיתית ברמת-קוד/הרשאות, לא רק ב-UI.
- **לא מופיע במסכי-לקוח** — אין קישור/כפתור לממשק-הזה בשום מסך שלקוח-קצה יכול להגיע אליו (`goral-hachol.html`, `calculator.html`, `myseal.html` וכו').

**שאלה להכרעה:** האם קיים כבר מנגנון-הרשאות/roles בפרויקט (מעבר ל-login פשוט), או שצריך להוסיף אחד — וזה כשלעצמו שינוי-סכמה ב-Supabase שדורש תכנון נפרד לפני שלב 8?

## 5. מעמד מסמכים ישנים

נבדק ישירות (git log, תאריכי-שינוי-אחרון):

| מסמך | תאריך-עדכון-אחרון | ממצא | סימון |
|---|---|---|---|
| `GORAL_WORKPLAN.md` | 2026-06-30 | 12/12 משימות מסומנות ✅ הושלמו; הקובץ לא עודכן מאז, בעוד `WORKPLAN.md` המשיך והתרחב עד 2026-07-08 (סעיפים #15–#29, היקף גדול-בהרבה) | **מיושן / דורש בדיקה** — נראה כתמונת-מצב-קפואה שקדמה ל-`WORKPLAN.md` הנוכחי, לא עודכנה לצדו |
| `GORAL-SYSTEM-RULES.md` | 2026-06-30 | 613 שורות "זיכרון-מערכת מלא", גם הוא קפוא מאז — לא נבדק שורה-שורה מול `CLAUDE.md`/`OREN_APP_STRUCTURE.md` הנוכחיים בסבב הזה (מחוץ להיקף מסמך-בירור זה) | **דורש בדיקה** — לא ניתן לקבוע כרגע אם הוא סותר, משלים, או מיותר מול `CLAUDE.md` בלי השוואה שורה-שורה |
| `CLAUDE.md`, `WORKPLAN.md` | פעילים עד 2026-07-08 | ממשיכים להתעדכן באופן שוטף | **תקף** — אלה המסמכים השולטים כיום |

**לא נמחק שום דבר** — הטבלה היא סימון-בלבד. **שאלה להכרעה:** האם לאחד את `GORAL_WORKPLAN.md`/`GORAL-SYSTEM-RULES.md` לתוך `WORKPLAN.md`/`CLAUDE.md` (ולסמן-כמוחלף, לא למחוק-פיזית), להשאיר כהיסטוריה-בלבד עם הערת-ראש מפורשת "מוחלף על-ידי X", או לבדוק-תוכן-מלא לפני כל החלטה?

## 6. מה אסור לבינה לעשות

רשימה סגורה, ללא יוצא-מן-הכלל:

- **לא לשנות קוד לבד** — שום כתיבה/עריכה של קובץ-מנוע/HTML, בשום נסיבות, ללא אדם (אורן משה) שמריץ ומאשר בפועל.
- **לא להמציא ידע** — עקרון-הברזל של `CLAUDE.md` ("No Invented Data") חל במלואו; אין מקור → "לא נמצא במקורות המאושרים".
- **לא לערבב חאווי וכשף** — כל שיטה נשארת במסלול-המקורות שלה (`OREN_SMART_ADVISOR_BRAIN_PLAN.md` סעיף 4).
- **לא לתת מקור בלי ציטוט** — כל טענה-ממוקרת מגיעה עם שם-ספר+עמוד+שיטה (סעיף 3 למעלה); אין "עובדה כללית" בלי ייחוס.
- **לא להשתמש בספר שלא אושר** — רק מקורות מהרשימה-המאושרת-לפי-שיטה; כל ספר חדש דורש אישור-נפרד לפני שהוא נכנס בכלל למאגר.
- **לא לשנות התנהגות ללקוח** — הבינה הזו לא נוגעת בשכבת-הלקוח בכלל (`OREN_SMART_ADVISOR_BRAIN_PLAN.md` סעיף 1 — הפרדה מוחלטת מ-Runtime AI); אם אי-פעם תעלה הצעה כזו, זה כשל-בעיצוב שדורש עצירה מיידית.

## 7. המלצה — מה צריך להחליט לפני שמתחילים לבנות בפועל את שלב 8

לפי סדר-התלות (מה חוסם את מה):

1. **סעיף 4 (הגנה על הממשק)** — קודם-כל, כי אם צריך מנגנון-הרשאות חדש ב-Supabase, זו החלטת-תשתית שמשפיעה על כל שאר השלבים ועדיף לדעת עליה מוקדם.
2. **סעיף 1 (מיקום-אחסון)** — נגזר חלקית מהחלטה 4 (אם כבר בונים שכבת-הרשאות ב-Supabase, טבעי להמשיך שם גם לאחסון).
3. **סעיף 5 (מעמד-מסמכים-ישנים)** — לא חוסם טכנית, אבל כדאי לסגור לפני שהבינה "קוראת" את כל המסמכים כמקור-אמת שווה — אחרת היא עלולה לצטט מסמך-מיושן כאילו הוא עדכני.
4. **סעיפים 2+3 (הכנסת-ספרים + ציטוט)** — הכי-אופרטיביים, הכי-קרובים-לביצוע-בפועל; הגיוני לסגור אחרונים, כשההחלטות-התשתיתיות (1, 4) כבר ידועות.

**המלצה מפורשת:** להתחיל את הדיון בסעיף 4 (הגנת-הממשק) — הוא היחיד מבין הארבעה שבלעדיו שום שלב-בנייה עתידי (8 או 9) לא יכול להתחיל באחריות, ותשובתו משפיעה על כל שאר ההחלטות.

---

## הצהרות

- שום קוד לא נכתב, שום HTML לא שונה, שום מנוע לא נגע.
- שום AI חי, secret, deploy, storage, embeddings, או UI — לא נוצרו.
- שום נגיעה ב-`inner-compass`.
- שום commit/push — רק יצירת קובץ המסמך הזה.
- שלב 8 לא התחיל ולא יתחיל בלי תשובות לסעיפים למעלה.
- הצעד הבא — לפי החלטת אורן משה בלבד.
