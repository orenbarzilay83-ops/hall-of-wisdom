# KASHF_ARCHITECTURE_ADVISOR_BRAIN_SPEC — תכנון "Kashf Architecture Advisor Brain"

> **מסמך תכנון בלבד. לא נכתב קוד, לא שונה HTML, לא שונה מנוע, לא נוצר storage/database/embeddings, לא חובר AI חי, לא נוסף secret, לא בוצע deploy, לא נגע ב-`inner-compass`. לא בוצע commit/push — רק יצירת הקובץ.**
> תאריך: 2026-07-08. שינוי-מיקוד מפורש מהמשתמש: לא Runtime-AI-ללקוח, אלא **עוזר-ארכיטקטורה-פנימי** שעוזר לאורן משה לסדר את מנועי כשף אל-אסראר — נותן אבחון והוראות-מדויקות למה-לבקש-מ-Claude-Code, לא כותב קוד בעצמו.

---

## 1. הגדרת התפקיד

**הבינה הזו אינה מחליפה את Claude Code.** היא **לא** כותבת קוד, **לא** עורכת קבצים, **לא** מריצה שינויים. תפקידה: לקרוא את הספר ואת המנועים, **לאבחן** פערים/כפילויות/רובוטיות/חוסר-עקביות בארכיטקטורת-המסקנות, ולתת לאורן משה **הוראה מדויקת, מוכנה-להעברה**, שהוא יכול להעתיק-ולהדביק לתוך שיחה עם Claude Code. הבינה היא **שכבת-אבחון-והכוונה**, לא שכבת-ביצוע. הפלט שלה הוא תמיד: "אורן משה, תגיד לקלוד קוד: ...", לא קוד עצמו.

## 2. מקורות שהיא צריכה ללמוד

- **הספר העברי הקיים** — `kashf-al-asrar.html` (ספר-לקריאה/אימות, כבר-הוכח-כמקור-לקוד-חי לפי `OREN_SMART_ADVISOR_KASHF_HTML_CONTENT_AUDIT.md`) + `goral-hachol/data/sources/kashf-al-asrar/kashf-al-asrar-book.js` (data-מובנה, אותו תוכן).
- **קבצי-data של כשף** — כל 11 הקבצים ב-`goral-hachol/data/sources/kashf-al-asrar/` (`kashf-figure-names.js`, `kashf-hazz.js`, `kashf-shibutzim.js`, `kashf-figure-attributes-gate2.js`, `kashf-figure-classification-gate2.js`, `kashf-figure-descriptions-gate2.js`, `kashf-gate5-foundational-figures.js`, `kashf-topics-per-house.js`, `kashf-topic-house11-friends-hope.js`, `kashf-book-sections.js`).
- **קבצי-engine של כשף** — כל 12 קבצי `goral-hachol/engine/kashf-*.js` (סעיף 4 מפרט אותם).
- **מסמכי WORKPLAN רלוונטיים** — בעיקר `WORKPLAN.md` #15–#23 (היסטוריית-בניית-מנוע-כשף), #25–#26 (תיקוני-סתירות וקיום שכבת-סינתזה).
- **דוחות-audit שכבר נוצרו** — `OREN_SMART_ADVISOR_EXISTING_GORAL_KNOWLEDGE_AUDIT.md`, `OREN_SMART_ADVISOR_KASHF_HTML_CONTENT_AUDIT.md`, `OREN_CONCLUSION_STYLE_SPEC.md` (מגדיר את-הכללים-שהבינה-הזו-בודקת-אם-הם-מקוימים).

## 3. מה היא צריכה להבין מתוך הספר

- **מבנה שערים** — 6 שערים (בתים, צורות, שיבוצים, גילוי-כוונה-נסתרת/דמיר, ארבע-צורות-יסוד, 12-פרקי-בתים) — כבר-ממופה ב-`OREN_SMART_ADVISOR_KASHF_HTML_CONTENT_AUDIT.md` סעיף 5.
- **סוגי שאלות** — 29 נושאים ב-`KASHF_TOPIC_RULES`, כל אחד עם נוסחה-ראשית ולעיתים חלופית.
- **כללי מסקנה** — איך `primaryFormula`/`altFormula` נגזרות משילוב-בתים ומתפרשות ל-verdict.
- **סימנים חזקים/חלשים** — **לא קיים כרגע דירוג מפורש בקוד** (ראו סעיף 5 — "Sign Weighting Layer" חסר) — הספר עצמו כן מבחין (למשל דיין מול עדים, בית-מוקד מול בית-משני) אך זה לא-מקודד כניקוד.
- **מצבים של ודאות/ספק** — קיים באופן-חלקי: מנגנון-גילוי-מחלוקת (`primaryFormula.verdict.positive !== altFormula.verdict.positive`, WORKPLAN #18) הוא **בינארי** (מסכים/לא-מסכים), לא סולם-ודאות מדורג.
- **חומר רוחני רגיש** — כישוף/עין-הרע/ג'ין/שדים/קללות — קיים בקוד (`goral-spiritual-diagnostics-engine.js`), נשמר-במלואו לפי `AGENTS.md` כלל 4.
- **מה אומרים ליועץ ומה אומרים ללקוח** — קיימת הפרדת-UI (ראו סעיף 4) אך **לא הפרדת-data-מבנית** (ראו סעיף 5).

## 4. מה היא צריכה להבין מתוך המנועים — נבדק ישירות בקוד

| קובץ | מה מחשב | תפקיד ב-pipeline |
|---|---|---|
| `kashf-reading-engine.js` (645 שורות) | `buildKashfReading(board, topicId, clientContext)` — מריץ `primaryFormula`+`altFormula` דרך `executeFormula`, בונה אובייקט `reading` יחיד | **המקור-האחד לכל מסקנה** — כל שאר-הקבצים קוראים ל/מוזנים-על-ידי הפונקציה הזו |
| `kashf-topic-rules.js` (1,952 שורות) | `KASHF_TOPIC_RULES` — 29 הגדרות-נושא (נוסחאות+`verdict.text` מוכן-מראש) | מקור-הכללים שהמנוע-לעיל מבצע |
| `kashf-book-additions.js` (532 שורות) | פונקציות שנכתבו **ישירות מאימות מול `kashf-al-asrar.html`** (עמ' 174, 272-274) | תוספות-מאומתות-מהספר, מיובאות ל-`kashf-reading-engine.js` |
| `kashf-dhamir.js` (535 שורות) | 5 שיטות-חישוב-דמיר (3 פעילות, 1 עם-מקור-חיצוני-מגולה, 1 לא-מיושמת) | "הכוונה-הנסתרת" — שכבה נפרדת מהפסיקה-הראשית |
| `kashf-leshon-hainyan.js` (93 שורות) | ניסוח-לשוני (שם מרמז: "לשון העניין") | שכבת-ניסוח קטנה |
| `kashf-dhamir-type4-external.js` (139 שורות) | שיטת-דמיר-4, עם גילוי-מקור-חיצוני (אל-פלק אל-משחון) | מחושב, **לא מוצג ללקוח** (הוסר לגמרי מהתצוגה, WORKPLAN #25) |
| `kashf-narrative-writer.js` (704 שורות) | 10 פונקציות `write*` — **כל אחת בונה קטע-HTML נפרד** מאותו `reading` | **נקודת-הפיצול היחידה בפועל בין "ליועץ" ל"ללקוח"** — ראו למטה |

**איפה נוצרת מסקנה:** ב-`kashf-reading-engine.js::buildKashfReading` — יחיד. כל שאר-הקבצים או-מזינים-אותו (data/formulas) או-מנסחים-את-הפלט-שלו (`kashf-narrative-writer.js`).

**מה מקור כל פרמטר:** `reading.primaryFormula`/`reading.altFormula` (מ-`KASHF_TOPIC_RULES`), `reading.keyHouseReadings`/`reading.supportingFindings` (מ-`executeFormula` על הלוח), `reading.dhamir`/`reading.dhamirType4External` (מ-`kashf-dhamir.js`/`kashf-dhamir-type4-external.js`).

**מה נשלח לפלט — נבדק ישירות (`writeKashfReading`):**
```
shortVerdictHtml + clientReadingHtml  → תמיד גלוי (ללקוח)
9 קטעי-detail (writeHeader..writeWitnessJudgePara) → מאחורי "קרא עוד ▼" (ליועץ)
```
**זו הפרדה ברמת-UI-visibility (hidden/toggle), לא הפרדת-data-מבנית.** אין אובייקט `advisorDiagnosis` נפרד מאובייקט `clientWording` — יש `reading` יחיד, ו-10 פונקציות-פורמט שכל-אחת בוחרת-אילו-שדות-ממנו-להציג-ואיפה.

**מה לא מנוצל:** `kashf-figure-descriptions-gate2.js` (תיאור-16-הצורות-המלא, שער 2 עמ' 68-95) — מאומת מהמסמך הקודם, 0 מייבאים.

**איפה קיימת כפילות:** לא נמצאה כפילות-חישובית ממשית בסבב הזה (מחוץ להיקף מעמיק) — אך `kashf-al-asrar-book.js` ↔ `kashf-al-asrar.html` הם כפילות-תוכן מכוונת (data מול תצוגה, ראו מסמך קודם).

**איפה קיימת רובוטיות:** `KASHF_TOPIC_RULES` מכיל `verdict.text` **מוכן-מראש-כמחרוזת-קבועה** לכל נוסחה — זו בדיוק הבעיה שהמשתמש-זיהה-מוקדם-יותר-בסבב (WORKPLAN #26): "יש בחירת-משפט-קבוע, לא סינתזה אמיתית". `narrative-fact-phrasing.js` (הפתרון-המתוכנן) **קיים אך לא מיושם בכשף בכלל עדיין** — רק בחאווי, נושא-מסחר-בלבד.

## 5. שכבות שחסרות — נבדק בפועל, לא הנחה

| שכבה | קיימת? | ממצא |
|---|---|---|
| **Question Understanding Layer** | **חלקית, לא-NLP** | אין ניתוח-טקסט-חופשי. המשתמש בוחר משאלה-מוכנה מ-138 שאלות ב-`question-bank.js`, כל שאלה כבר-נושאת `kashfTopicId` קבוע-מראש. "ההבנה" קרתה-פעם-אחת בזמן-בניית-הבנק, לא בזמן-ריצה. |
| **Topic Routing Layer** | **קיימת, טובה** | `kashfTopicId` ישיר לכל שאלה (WORKPLAN #23) — לא תלוי-עוד ב-`HAWI_TO_KASHF_TOPIC`. עצמאות-מבנית אמיתית. |
| **Evidence Selector** | **קיימת חלקית** | `keyHouseReadings`/`supportingFindings` **כן** אוספים-ממצאים-רלוונטיים — אך **לא בוחרים "הכי-חזק"**, אוספים-הכל-שהנוסחה-מגדירה. |
| **Sign Weighting Layer** | **חסרה — נבדק, 0 מנגנון-ניקוד בקוד** (`grep` ל-`weight`/`score`/`strength`/`priority`/`rank` — 0 התאמות-רלוונטיות בשלושת-הקבצים-המרכזיים). כל סימן משפיע-שווה בערך; אין `strongestSignals`/ניקוד-משוקלל. |
| **Certainty Layer** | **חסרה-ברובה — קיים רק מנגנון בינארי** | `primaryFormula` מול `altFormula` — מסכימים/לא-מסכימים (WORKPLAN #18). **אין** סולם-ודאות (למשל "3 מתוך 4 סימנים תומכים"), רק דגל-אזהרה כן/לא. |
| **Advisor Diagnosis Layer** | **חסרה כ-data-structure, קיימת כ-UI-panel** | ראו סעיף 4 — 9 הפונקציות מאחורי "קרא עוד" **הן** האבחון-ליועץ בפועל, אך לא-מאוגדות כאובייקט-נפרד-ומוגדר. |
| **Client Wording Layer** | **קיימת חלקית (`writeShortVerdictBox`+`writeConclusionPara`), אך רובוטית** | קיימת ההפרדה-החזותית (סעיף 4), אך התוכן עצמו הוא ברובו `verdict.text` קבוע-מראש, לא ניסוח-דינמי לפי-`narrative-fact-phrasing.js` — הבעיה שהמשתמש-כבר-זיהה. |
| **Practical Guidance Layer** | **חסרה כמעט-לגמרי** | לא נמצא-בקוד-הכשף שדה/פונקציה-ייעודית ל"המלצה-מעשית" (השוו לחאווי, ששם יש `push('הדיין תומך... מומלץ להמשיך')` בסוף כל topic — לא נבדק-אם-לכשף-יש-מקבילה-שיטתית באותה סבב, מסומן-כשאלה-לבדיקה-נוספת). |
| **Source Citation Layer** | **קיימת בקוד/data, מכוונת-מוסתרת-מהלקוח** | `sourceStatus`/הפניות-עמוד קיימות בכל רשומת-data — אך **מוסרות-בפירוש** מהפלט-ללקוח (`stripInlineCitations`, WORKPLAN #25). לשכבת-היועץ-הפנימי (הבינה-הזו) — כן-אמורות-להיות-זמינות, זה בדיוק ייעודה. |

## 6. פורמט תשובה עתידי — כל המלצה חייבת לכלול

א. **מה הבעיה** | ב. **למה זו בעיה** (לפי הספר/הקוד, ציטוט-קונקרטי) | ג. **איזה קובץ מושפע** | ד. **מה צריך לשנות** | ה. **מה אסור לשנות** | ו. **איזו בדיקה לעשות אחרי השינוי** | ז. **נוסח מדויק להעברה ל-Claude Code**.

## 7. דוגמת תוצאה רצויה (לא קוד — טקסט-המלצה בלבד)

> **אורן משה, הבעיה כאן היא:** ב-`kashf-topic-rules.js`, כל 29 הנושאים משתמשים ב-`verdict.text` — מחרוזת-מסקנה-קבועה-מראש שלא-משתנה-בין-לוח-ללוח (אותה צורה-מיטיבה תמיד מקבלת אותה מילה-במילה תשובה). **זו בדיוק אותה בעיה שכבר תוקנה בחאווי-נושא-מסחר** (`narrative-fact-phrasing.js`, WORKPLAN #26) — אך **לא תוקנה בכשף בכלל עדיין**.
>
> **קובץ מושפע:** `goral-hachol/engine/kashf-narrative-writer.js` (פונקציית `writeConclusionPara`) + `goral-hachol/engine/kashf-topic-rules.js` (מקור ה-`verdict.text` הקבוע).
>
> **מה צריך לשנות:** להוסיף שימוש ב-`narrative-fact-phrasing.js` (הקובץ **כבר קיים**, נבנה לחאווי) בתוך `writeConclusionPara`, כך שהמסקנה תיבנה משם-הצורה-הספציפית+ה"מהות" שלה+הפסיקה, לא ממשפט-קבוע.
>
> **מה אסור לשנות:** `kashf-figure-appearance.js` (אסור-לגעת, `AGENTS.md` כלל 13). לא לגעת ב-`primaryFormula`/`altFormula` עצמן (החישוב נשאר כמו-שהוא — רק הניסוח-הסופי משתנה).
>
> **בדיקה אחרי השינוי:** להריץ 10 לוחות-מדגם על נושא אחד (כמו שנעשה לחאווי-מסחר, WORKPLAN #26) ולוודא שאין שתי-צורות-שונות מקבלות ניסוח-זהה, ושאין ניגודיות-פנימית (השוו לבאג "נלחם" שכבר תוקן בחאווי).
>
> **נוסח להעברה ל-Claude Code:** *"תרחיב את שכבת-הסינתזה (`narrative-fact-phrasing.js`) לנושא-אחד בכשף — [שם-נושא] — באותו אופן שכבר נעשה למסחר בחאווי (WORKPLAN #26). לפני שתתחיל, תציג לי תוכנית: איזה שדה ב-`kashf-topic-rules.js` יוחלף, ואיך ייראה הפלט לפני/אחרי על 2-3 דוגמאות. אל תיגע ב-`kashf-figure-appearance.js`."*

## 8. מה אסור לבינה לעשות

- **לא לשנות קוד לבד** — לעולם, בשום נסיבות.
- **לא להמציא חוק מהספר** — כל טענה-ממוקרת מצוטטת מ-`kashf-al-asrar.html`/`kashf-al-asrar-book.js` בפועל, לא מ"ידע כללי על גורל החול".
- **לא להשתמש במקור לא-מאושר** — הקובץ הערבי (`kashf-al-asrar-arabic-only.html`) נשאר-בלתי-מאושר עד החלטה נפרדת (`OREN_SMART_ADVISOR_KASHF_HTML_CONTENT_AUDIT.md`).
- **לא לערבב חאווי וכשף** — גם כשמזהה-דפוס-דומה (כמו הדוגמה בסעיף 7), ההמלצה נשארת בתוך-קבצי-כשף בלבד, לא "תעתיק את חאווי לתוך כשף".
- **לא לקבוע ודאות בלי סימנים מספיקים** — אם `Certainty Layer` חסרה (סעיף 5), הבינה לא-תמציא-רמת-ביטחון שלא-קיימת בקוד; תדווח "אין דירוג-ודאות היום" במקום לנחש.
- **לא להמליץ שינוי בלי לציין קובץ וסיכון** — כל המלצה עומדת בפורמט-סעיף-6 המלא, לא תקציר-חלקי.
- **לא לתת הוראה ל-Claude Code בלי בדיקות** — כל הוראה (סעיף 6-ז) כוללת שלב-אימות מוגדר.

## 9. שלב ראשון מומלץ לבנייה

**המלצה מעשית וממוקדת, לא כללית:**

**להתחיל מ: audit של מנועי כשף — אך ממוקד וממשיך-ישירות ממה-שכבר-נעשה, לא audit-כללי-חדש.** הנימוק:
- **לא** מפת-פרמטרים ו**לא** מפת-evidence-מהספר כצעד-ראשון — אלה **כבר קיימות-בחלקן** (סעיף 4-5 של המסמך הזה + `OREN_SMART_ADVISOR_KASHF_HTML_CONTENT_AUDIT.md`) — לבנות-אותן-מחדש-כמסמך-נפרד יהיה כפילות.
- **לא** test-cases כצעד-ראשון — אין-עדיין-מה-לבדוק (אין שינוי-מוצע-קונקרטי עדיין מעבר לדוגמה-בסעיף-7).
- **לא** prompt-advisor-פנימי (שלב 8 ב-`OREN_SMART_ADVISOR_BRAIN_PLAN.md`) — זה תלוי-בהחלטות-אחסון/הרשאות שכבר-תוכננו-בנפרד ועדיין-לא-הוכרעו.
- **לא** חיבור-AI-חי — מוקדם-מדי, אין-עדיין-אפילו-הצעת-שינוי-ראשונה-מאושרת.

**הצעד הנכון:** לקחת את **הדוגמה הקונקרטית בסעיף 7** (רובוטיות ב-`kashf-topic-rules.js` דרך `verdict.text`) ולהשלים אותה ל**הצעת-שינוי-אחת-שלמה-ומוכנה-להעברה** — כולל בדיקה-אילו-מ-29-הנושאים-הכי-בעייתיים (למשל: הכי-הרבה-`verdict.text`-כפולים/הכי-פחות-מגוונים), לא רק-דוגמה-מומצאת. זהו **ניצחון-קטן-ומיידי** שמוכיח שהבינה-הזו-כבר-שימושית **לפני** שמשקיעים בבניית-תשתית-AI-חיה-לה.

---

## מה חסר היום בארכיטקטורת-המסקנות של כשף — תמצית

1. **אין שכבת-ניקוד-סימנים** (Sign Weighting) — כל ממצא משפיע "שווה", לא לפי-חוזק-אמיתי-מהספר.
2. **אין סולם-ודאות** (Certainty) — רק דגל-בינארי-מחלוקת.
3. **אין הפרדת-data מבנית בין אבחון-יועץ לניסוח-לקוח** — יש רק הפרדת-UI (hidden/toggle) על אותו `reading`.
4. **שכבת-הסינתזה (`narrative-fact-phrasing.js`) לא מיושמת בכשף בכלל** — 29/29 נושאים עדיין-על `verdict.text` קבוע.
5. **תוכן-מאומת-קיים לא-מנוצל** (`kashf-figure-descriptions-gate2.js`) — לא-חסר-ידע, חסר-חיווט.
6. **אין Practical Guidance Layer שיטתית** (טרם-אומת מול חאווי בסבב הזה).

---

## המלצה מעשית לשלב הבא (בלי לבצע)

לקחת את נושא-אחד-מ-29 (המועמד-הטוב-ביותר לפי חזרתיות-`verdict.text`, לא נבחר-עדיין בסבב-הזה) ולהכין **הצעת-שינוי-מלאה-בפורמט-סעיף-6** — כולל ה"נוסח-להעברה-ל-Claude-Code" המוכן — כדוגמה-ראשונה-אמיתית (לא-דמיונית כמו סעיף 7) של איך הבינה-הזו-אמורה-לפעול בפועל. **זו הצעה בלבד — לא בוצעה.**

---

## הצהרות

- שום קוד לא נכתב, שום HTML/מנוע לא נערך, שום storage/database/embeddings/AI-חי לא נוצרו.
- שום secret/deploy לא נוספו. שום נגיעה ב-`inner-compass`.
- שום commit/push — רק יצירת קובץ המסמך הזה.
- הצעד הבא — לפי החלטת אורן משה בלבד.
