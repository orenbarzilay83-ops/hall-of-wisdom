# OREN_SMART_ADVISOR_ACCESS_CONTROL_AUDIT — ביקורת מנגנון-ההרשאות הקיים

> **מסמך בדיקה בלבד. לא נכתב קוד, לא שונה HTML, לא שונה מנוע, לא נוצר UI, לא נוצר storage, לא נוצרו embeddings, לא נוסף AI חי, לא נוסף secret, לא בוצע deploy, לא נגע ב-`inner-compass`. לא בוצע commit/push — רק יצירת הקובץ.**
> תאריך: 2026-07-08. המשך ישיר מ-`OREN_SMART_ADVISOR_OPEN_DECISIONS.md` סעיף 4 ("הגנת-הממשק"), שסומן שם כהחלטה-חוסמת-ראשונה. כל ממצא כאן נבדק ישירות בקוד (קריאת-קבצים בפועל), לא משוער.

> **עדכון (אותו יום):** אורן משה אישר שב-Supabase Dashboard **כן** קיים Supabase Auth פעיל עם משתמשים קיימים, ולכל משתמש יש UID. אורן משה הוא **admin של פרויקט-ה-Supabase** (גישת-dashboard מלאה).
>
> **הבחנה קריטית שהמסמך הזה בודק במפורש:** admin-של-פרויקט-Supabase (יכולת לנהל את כל הפרויקט מה-dashboard החיצוני) **אינו** אותו דבר כמו app-level-admin **בתוך קוד-האתר עצמו**. השאלה שהמסמך הזה עונה עליה היא לא "האם לאורן יש גישת-Supabase" (יש, ידוע) — אלא **"האם קוד-האתר עצמו יודע להבחין בין אורן לבין כל משתמש-מחובר-אחר"**. התשובה, כמפורט למטה: לא.

---

## 1. מה קיים היום — לפי 7 הבדיקות הנדרשות

### 1. איפה בקוד האתר משתמשים ב-Supabase Auth
- `index.html` — מסך-login יחיד. `supabase.auth.signInWithPassword({email, password})` (Supabase Auth אמיתי, לא סיסמה-קשיחה-בקוד).
- `supabase-client.js` — יוצר את ה-client (`createClient` עם URL-פרויקט + מפתח-publishable, לא-סודי מטבעו — מפתחות `sb_publishable_...` מיועדים-לחשיפה בצד-לקוח).
- `calculator.html`, `goral-hachol.html` — **שני הקבצים היחידים** (מלבד `index.html` עצמו) שמייבאים את `supabase-client.js` ובודקים session בפועל.
- לפי `CLAUDE.md`, המשתמש-ברירת-המחדל הוא "oren moshe / 1983" — זהו חשבון-Supabase-אמיתי (עכשיו ידוע: אחד מתוך כמה משתמשים קיימים בפרויקט, לפי אישור אורן משה), לא קבוע בקוד.

### 2. האם יש `getUser`/session/auth-state
**כן.** `calculator.html`/`goral-hachol.html` כל אחד מבצע שתי בדיקות: (א) בדיקה-מיידית-סינכרונית — `if (!sessionStorage.getItem('userId')) window.location.replace('/index.html')` (מונעת הבזק-תוכן לפני טעינת-הדף), (ב) אימות א-סינכרוני מול Supabase בפועל — `getSession()`; אם אין session תקף בשרת, ה-`sessionStorage` מנוקה וההפניה חוזרת ל-login. זו הגנה-אמיתית ברמת "יש/אין session תקף" — **לא** נמצא שימוש ב-`getUser()` או בדיקת-תוכן-של-ה-JWT מעבר ל"קיים session".

### 3. האם יש בדיקה לפי email
**לא, לא לצורך הרשאה.** `userEmail` נשמר ב-`sessionStorage` (מוצג למשתמש/לתיעוד), אבל שום קובץ לא בודק "האם ה-email שווה ל-X" כתנאי-גישה לשום מסך או פעולה.

### 4. האם יש בדיקה לפי UID
**לא, לא לצורך הרשאה.** `userId` (=ה-UID מ-Supabase Auth) נשמר ומשמש **רק כסקופ-נתונים** — לדוגמה `goral-client-archive.js`: `STORAGE_KEY = \`goralHacholClientReadingsArchive_v1_${userId}\`` (כדי שארכיון-לקוחות יהיה נפרד-לכל-חשבון). שום קובץ לא משווה UID מול ערך-קבוע כדי להחליט מי-מורשה-למה. כל UID תקף (כל משתמש קיים בפרויקט, ואישרת שיש כמה) מקבל גישה זהה לכל מסך-מוגן.

### 5. האם יש טבלת `profiles`/`users`/`admin_users`/`roles`
**לא אותרה בקוד.** נבדק בחיפוש-רוחב (`is_admin`, `admin_users`, `permission`, `role`-במובן-הרשאות, `profiles`-כטבלת-משתמשים) על כל קבצי ה-JS/HTML בריפו — כל ההתאמות שנמצאו הן שימושים לא-קשורים (`role` כתכונת-בית-אסטרולוגי ב-`raml.js`, `profileState`/`RAML_FORMS_PROFILES` כפרופיל-לקוח/צורה, לא פרופיל-הרשאות-משתמש). **הערה חשובה:** זו בדיקה של **הריפו בלבד** — אם קיימת טבלה כזו ב-Supabase database מבלי שקוד-האתר עדיין קורא אליה, הביקורת הזו לא הייתה מזהה אותה (אין גישת-DB ישירה בסביבה הנוכחית). זו שאלה-פתוחה (סעיף 7).

### 6. האם יש RLS policies רלוונטיות
**לא ניתן לקבוע מהריפו.** נבדק — אין קובצי-migration, אין קובצי-SQL, אין הגדרת-policies בשום מקום בקוד (`find` לקבצי `.sql`/`supabase/migrations` — 0 תוצאות). RLS policies, אם קיימות, מוגדרות ישירות ב-Supabase dashboard ואינן גלויות/ניתנות-לביקורת מתוך הריפו. **דורש בדיקה ישירה מול הדשבורד** (מחוץ להיקף-הביקורת-הזו).

### 7. האם קיימת הפרדה בין user רגיל לבין admin פנימי
**לא, לא ברמת-קוד-האתר.** זו התשובה המרכזית של המסמך הזה (ראו "הבחנה קריטית" למעלה): admin-Supabase-דשבורד ≠ app-level-admin. בקוד-האתר עצמו:
- `index.html`, `calculator.html`, `goral-hachol.html` — מוגנים (session-check אמיתי כמתואר בסעיף 2) — אבל **מוגן-לכל-מי-שמחובר**, לא מוגן-לאורן-בלבד.
- `myseal.html` — **נבדק ונמצא ללא שום session-check** (`grep` על `supabase`/`getSession`/`sessionStorage` — 0 התאמות). ממצא-לוואי, לא תוקן (מחוץ להיקף).
- אין היום שום מושג-קוד של "מסך פנימי בלבד לאורן" — אין הבדל-קוד בין "אורן מחובר" ל"כל משתמש-אחר-בפרויקט מחובר".

## 2. האם login רגיל מספיק

**לא מספיק לבינה הפנימית.** הסיבה, מבוססת-ממצא (לא תיאורטית, ועכשיו מאושרת גם על-ידי אורן משה): login רגיל מבחין בין "מחובר" ל"לא-מחובר" — **לא** בין "אורן" ל"כל חשבון-Supabase אחר בפרויקט". **כבר עכשיו קיימים כמה משתמשים בפרויקט-ה-Supabase** (אושר על-ידי אורן משה מול הדשבורד) — לכל אחד מהם, כרגע, יש **בדיוק אותה גישה** לכל מה שמוגן-רק-ב-login, כי שום דבר בקוד לא אוכף הבדל ביניהם. עוד יותר בעייתי כי, בניגוד ל-`calculator.html`/`goral-hachol.html`, ה-Smart-Advisor-Brain אמור להכיל מידע-פיתוח-רגיש (מבנה-מנועים, ידע-מקורות-פרטי) — login-בלבד הוא רמת-הגנה נמוכה-מדי לתוכן כזה. **הגישה של אורן ל-Supabase dashboard (project-admin) לא פותרת את זה** — היא נותנת לו שליטה-על-התשתית, לא הבחנת-קוד בין המשתמשים שכבר קיימים בה.

## 3. מה אסור לעשות

- **לא לפתוח את Smart Advisor לכל משתמש מחובר** — נדרשת בדיקת-זהות ספציפית מעבר ל-session תקף.
- **לא להסתמך רק על הסתרת-כפתור ב-HTML** — הסתרה ויזואלית (`display:none`, אי-קישור מהתפריט) היא לא הגנה; כל מי שיודע/מנחש URL יכול להגיע לדף ישירות (בדיוק כמו `myseal.html` שנמצא היום ללא הגנה כלל — תזכורת חיה לסיכון הזה).
- **לא לשים secret בדפדפן** — כל בדיקת-הרשאה אמיתית (לא רק "יש session") חייבת להתבצע בצד-שרת (Edge Function), לא ב-JS-שרץ-בדפדפן שכל אחד יכול לקרוא-ולעקוף.
- **לא לשים מפתח-AI בקוד** — כבר תועד כממצא-אמיתי-שקרה (`OREN_EXISTING_AI_API_AUDIT.md`, מפתח-Anthropic שנמצא מחויב-לגיט ב-`inner-compass`), לא סיכון-תיאורטי.
- **לא להכניס ספרים פרטיים לריפו הציבורי** — נשאר כמדיניות-קבועה (`OREN_SMART_ADVISOR_BRAIN_PLAN.md` סעיף 5).
- **לא לעשות hardcode גלוי של מידע-רגיש בקוד ציבורי** — כולל לא `userId`/email של אורן כתנאי-הרשאה מוטמע-בקוד-הלקוח (גם אם "רק אורן יודע את זה", זה עדיין גלוי לכל-מי-שקורא את קוד-המקור של אתר-סטטי).

## 4. אפשרויות-הגנה עתידיות

| אפשרות | איך עובד | הערה |
|---|---|---|
| **בדיקה לפי Supabase user id** | Edge Function משווה את ה-`user.id` מה-JWT-המאומת מול ID-ידוע-מראש (בצד-שרת בלבד) | פשוט-ליישום, אך "ID-ידוע-מראש" חייב לחיות כ-secret/קונפיג בצד-שרת, לא בקוד-לקוח |
| **טבלת `admin_users`/`roles` ב-Supabase** | טבלה חדשה, נבדקת בצד-שרת (RLS policy או בדיקה מפורשת ב-Edge Function) האם `user.id` הנוכחי מופיע בה | הכי-גמיש (תומך בעתיד גם ביותר-ממורשה-אחד בלי לשנות קוד), אך דורש שינוי-סכמה |
| **Allowlist בצד-שרת בלבד** | רשימת-emails/IDs קבועה בקונפיג-של-ה-Edge-Function (לא בטבלה, לא בקוד-לקוח) | ביניים — פשוט מ-טבלה, בטוח-יותר מ-hardcode-בקוד-לקוח, אך פחות-גמיש מטבלה |
| **Edge Function שבודקת הרשאה לפני כל תשובה** | לא רק "לפני טעינת-הדף" — כל קריאת-AI עצמה עוברת דרך בדיקת-הרשאה בצד-שרת, גם אם מישהו עקף את בדיקת-ה-UI | **חובה בכל אחת מהאפשרויות למעלה** — לא תחליף להן, שכבת-הגנה נוספת-תמיד |
| הסתרת-UI (תזכורת) | להסתיר כפתור/קישור | **רק שכבת-נוחות משנית** — לעולם לא ההגנה עצמה, כפי שממצא `myseal.html` ממחיש בדיוק למה |

## 5. המלצה — ההגנה הכי פשוטה ובטוחה לשלב ראשון

**עקרון-העל, שלא משתנה גם אם בוחרים allowlist-UID או `admin_users`:** להשתמש ב-Supabase Auth הקיים (לא לבנות מנגנון-אימות חדש), ולהוסיף **בדיקת-הרשאה בצד-שרת בלבד** — כל קריאה ל-Edge Function של המוח-הפנימי חייבת לבדוק הרשאה **לפני** שהיא עונה, לא רק לפני-שה-UI-מציג-כפתור.

**שתי הוריאציות, שתיהן עומדות בכל האיסורים בסעיף 3 — הראשונה פשוטה-יותר-להתחיל, השנייה גמישה-יותר-לטווח-ארוך:**

1. **Allowlist לפי UID (הפשוטה ביותר, מומלצת לשלב-הראשון):**
   - Edge Function ייעודית למוח-הפנימי (`oren-advisor-brain`, נפרדת לגמרי מ-`oren-smart-ai` של Runtime AI — עקרון-ההפרדה מ-`OREN_SMART_ADVISOR_BRAIN_PLAN.md` סעיף 1).
   - הפונקציה מקבלת את ה-JWT של המשתמש-המחובר (Supabase מספק את זה אוטומטית לכל קריאה מאומתת), מאמתת אותו מול Supabase, ומשווה את ה-`user.id`/UID שיצא ממנו מול **UID קבוע אחד של אורן משה, מוגדר כ-secret בסביבת-ה-Edge-Function** (לא בקוד-לקוח, לא בטבלה — הכי-פשוט להתחיל).
   - אם ה-UID לא תואם — הפונקציה מסרבת (403), **בלי קשר** לשאלה האם ה-UI בכלל מציג כפתור למשתמש הזה.
2. **טבלת `admin_users` (גמישה-יותר, לכשיהיה יותר-ממורשה-אחד):** אותו עיקרון בדיוק, אבל ההשוואה היא מול-טבלה (עם/בלי RLS policy) במקום מול-ערך-קבוע-יחיד. שדרוג-טבעי מ-1, לא ארכיטקטורה-שונה.

**בשני המקרים:** UI (כשייבנה, שלב-נפרד ומאושר-בנפרד) יכול להראות/להסתיר כפתור לפי-נוחות — אבל זו **רק** שכבת-נוחות; ההגנה-האמיתית תמיד ורק בצד-השרת, בכל קריאה, לא רק בטעינת-הדף.

## 6. רשימת קבצים שנבדקו בפועל

- `index.html` — נקרא במלואו.
- `supabase-client.js` — נקרא במלואו.
- `calculator.html` — נבדקו שורות login/auth-guard (`grep` ממוקד + קריאת ההקשר).
- `goral-hachol.html` — נבדקו שורות login/auth-guard (`grep` ממוקד + קריאת ההקשר).
- `myseal.html` — נבדק לחיפוש session-check (לא נמצא).
- `goral-hachol/engine/goral-client-archive.js` — נבדק לאישור דפוס `userId`-כסקופ-נתונים (לא הרשאה).
- `raml.js`, `goral-hachol/ui/goral-app.js`, `raml-data/raml-forms-profiles.js`, `raml-data/raml-houses-profiles.js`, `raml-data/raml-foundation-closure-audit.js`, `raml-data/raml-houses-basic.js`, `raml-data/raml-foundation-bulugh-al-amal.js` — נבדקו כתוצאות-חיפוש `role`/`profile`/`admin` (כולם אומתו כלא-קשורים להרשאות-משתמש).
- חיפוש-רוחב על כל קבצי `*.js`/`*.html` בריפו למונחים: `is_admin`, `isAdmin`, `admin_users`, `permission`, `role`, `profiles` — 0 התאמות רלוונטיות להרשאות.
- חיפוש-רוחב לקובצי-migration/SQL/RLS: `find` ל-`*.sql`/`supabase/migrations` — 0 תוצאות; `grep` ל-"RLS"/"row level security" — 0 התאמות מחוץ למסמך הזה עצמו.
- אישור: רק 3 קבצי-HTML בריפו (`index.html`, `calculator.html`, `goral-hachol.html`) מייבאים בכלל את `supabase-client.js`.

## 7. שאלות פתוחות

- **האם יש כבר UID קבוע לאורן משה?** אורן משה אישר שיש UID לכל משתמש ב-Supabase Auth — אך ה-UID הספציפי של אורן **לא אומת מול קוד/ריפו** בסבב הזה (אין גישת-DB ישירה בסביבה הנוכחית). ידוע רק ש-`userEmail` הנוכחי בהקשר-הסשן הזה הוא `orenbarzilay83@gmail.com` (מהקשר-המערכת, לא מבדיקת-קוד/DB). **צריך העתקה-ידנית של ה-UID המדויק מ-Supabase dashboard** לפני שאפשר להשתמש בו כ-allowlist בפועל.
- **האם קיימת טבלת `profiles`/`users` מעבר לטבלת-האימות-המובנית של Supabase (`auth.users`)?** לא אותרה בקוד, ואין קובצי-migration/SQL בריפו שהיו חושפים אותה (סעיף 6). ייתכן שקיימת ב-DB בלי שיש לה שימוש-קוד עדיין — דורש בדיקה ישירה מול Supabase dashboard.
- **האם יש roles קיימים ב-DB (לא רק בקוד)?** לא ידוע — הריפו לא חושף RLS policies/roles שמוגדרים ישירות ב-Supabase (סעיף 6, בדיקה 6). דורש בדיקה ישירה מול הדשבורד.
- **האם יש roles-ברמת-קוד-האתר?** לא — נבדק ואושר בסעיפים 5+7 של "מה קיים היום" למעלה.
- **האם צריך ליצור `admin_users` בעתיד?** לא בשלב-הראשון לפי ההמלצה (סעיף 5, וריאציה 1) — allowlist-UID-קבוע-בצד-שרת מספיק להתחלה; טבלה (וריאציה 2) רק כשיהיה יותר-ממורשה-אחד.
- **כמה משתמשים קיימים היום בפועל בפרויקט?** ידוע שיש "כמה" (אישור-אורן), אך המספר המדויק וזהות-כל-אחד מהם לא ידועים מהריפו — דורש בדיקה ישירה מול הדשבורד לפני קביעת-allowlist סופית (חשוב לוודא שאין כבר משתמש-נוסף עם UID דומה-בטעות, או חשבון-בדיקה ישן שצריך להוציא מהחשבון).
- **האם הממשק הפנימי יהיה במסך נפרד או בתוך dashboard קיים?** לא הוכרע — אם בתוך `calculator.html` הקיים, נדרש עדיין access-control נפרד-מ-session-רגיל (login-להיכנס-לדשבורד ≠ הרשאה-לראות-את-הבינה-הפנימית בתוכו); אם מסך-נפרד-לגמרי, הוא עדיין צריך לעבור דרך אותו login-guard הקיים כשלב-ראשון.

---

## הצהרות

- שום קוד לא נכתב, שום HTML לא שונה, שום מנוע לא נגע.
- שום UI, storage, embeddings, AI חי, secret, deploy — לא נוצרו.
- שום נגיעה ב-`inner-compass`.
- שום commit/push — רק יצירת קובץ המסמך הזה.
- הצעד הבא — לפי החלטת אורן משה בלבד.
