# דוח היתכנות: מיזוג inner-compass (קריאת קלפים) לתוך hall-of-wisdom

> **דוח בלבד. שום דבר לא מוזג, שום קובץ לא הוזז, שום קוד לא שוכתב, שום דבר לא נפרס.**
> תאריך: 2026-07-07. מוכן עבור אורן משה, לפי תהליך שער-האישור (`OREN_AI_SUPERVISOR_SPEC.md`).

> ⚠️ **הסתייגות שקיפות:** הצד של `hall-of-wisdom` מתועד כאן מקריאת-קוד ישירה ומלאה. הצד של `inner-compass` מבוסס על תיאורו של אורן משה (Next.js / React / TypeScript, פרויקט קריאת קלפים) — **טרם אומת בקריאת קוד**, כי הוספת הריפו לסשן דורשת אישור גישה שטרם הושלם. לאחר אישור גישה — הדוח יעודכן בסעיפי-הארכיטקטורה המדויקים. ההמלצה בסוף אינה תלויה בפרטים החסרים: היא נובעת מהפרש-הטכנולוגיות עצמו, שמאושר.

---

## 1. ארכיטקטורה נוכחית של שני הריפוזיטוריז

### 1.1 hall-of-wisdom (מאומת מקריאת קוד)

- **טכנולוגיה:** HTML/CSS/JS סטטי טהור. ES modules בדפדפן (`<script type="module">`). **אין** build, **אין** framework, **אין** תלות-חבילות. כלל מפורש ב-`CLAUDE.md`: אסור להכניס React/Vue/TypeScript/webpack.
- **פריסה:** קבצים סטטיים; `main` = האתר החי.
- **persistence:** `localStorage` בלבד. אין backend.
- **מודולים:** גורל החול (חאווי + כשף — המוצר המרכזי, ~20,000 שורות מנועים), גורל העשיריות, נומרולוגיה, זוגיות, הורים-ילדים, תהילים, גימטריה, יומן/ארכיון לקוחות, איסקאט 7×7, ראמאל שסטרה, קומילה, חותם.
- **שכבת ידע:** `goral-hachol/data/sources/` — ידע ממוקור-ספרים עם מערכת `sourceStatus` לכל רשומה.
- **UI:** RTL עברית, ערכת Navy/Gold, סדר לוח ימין-לשמאל.
- פירוט מלא: `OREN_APP_STRUCTURE.md`.

### 1.2 inner-compass (לפי תיאור אורן משה — טרם אומת בקוד)

- **טכנולוגיה (מדווח):** Next.js / React / TypeScript.
- **תחום:** קריאת קלפים (Cartomancy).
- **לא ידוע עדיין (יושלם לאחר אישור גישה):** מבנה התיקיות, היכן שכבת-הידע של הקלפים (data/JSON/TS), האם יש backend/API routes, איך נפרס (Vercel?), האם יש persistence, מצב ה-RTL.

## 2. הפרש הטכנולוגיות — למה זה העניין המרכזי

| | hall-of-wisdom | inner-compass (מדווח) |
|---|---|---|
| שפה | JavaScript טהור | TypeScript |
| רנדור | דפדפן ישירות, ללא build | React + Next.js (build חובה) |
| פריסה | העלאת קבצים סטטיים | שרת Node / Vercel (או static export) |
| תלות | אפס חבילות | node_modules מלא |
| מודל קוד | קבצי script + ES modules | קומפוננטות + JSX/TSX |

**המשמעות:** אי אפשר "להעביר קבצים" בין השניים. קומפוננטת React לא רצה בדף סטטי בלי build, וקובץ JS סטטי לא משתלב ב-Next בלי עטיפה. כל מיזוג אמיתי הוא **המרה**, לא העברה.

## 3. שלוש אפשרויות מיזוג — מהבטוחה למסוכנת

### אפשרות A — להשאיר שני ריפוזיטוריז נפרדים, ולקשר ביניהם

inner-compass נשאר פרויקט עצמאי (נפרס בכתובת משלו). ב-hall-of-wisdom מוסיפים פריט תפריט "קריאת קלפים" שפותח את הכתובת של inner-compass (כמו שראמאל שסטרה וקומילה כבר פתוחים כמסכים נפרדים).

- **יתרונות:** אפס סיכון לאתר החי; אפס שכתוב; כל פרויקט ממשיך להתפתח בכלים שלו; ניתן לביצוע היום; הפיך לחלוטין.
- **סיכונים:** חוויית משתמש של שני אתרים (מעבר-דף, אולי התחברות כפולה); ארכיון הלקוחות לא משותף (קריאות קלפים לא יישמרו ביומן של hall-of-wisdom בלי עבודה נוספת); שתי מערכות לתחזק.
- **מורכבות:** נמוכה מאוד — שעות בודדות.
- **קבצים מושפעים:** `goral-hachol.html` (פריט תפריט) או `calculator.html` — שורות ספורות. שום מנוע לא נוגע.
- **שבירת האתר החי:** לא. תוספת קישור בלבד.

### אפשרות B — להעביר רק את הידע/דאטה/מנועים של הקלפים, ולבנות UI סטטי ב-hall-of-wisdom

מחלצים מ-inner-compass את **התוכן** (משמעויות קלפים, פריסות, כללי פרשנות — בדיוק כמו ששכבת `data/sources/` בנויה היום לגורל החול), ממירים ל-JS טהור, ובונים מסך קלפים חדש בתוך hall-of-wisdom בסגנון הקיים.

- **יתרונות:** אפליקציה אחת אמיתית — ארכיון לקוחות משותף, עיצוב אחיד, תפריט אחד; שומר על כלל-הברזל של הריפו (בלי framework); שכבת-הידע של הקלפים נכנסת לאותה משמעת `sourceStatus`; שכבת-הסינתזה החדשה (`narrative-fact-phrasing.js`) תוכל לשרת גם מסקנות-קלפים בעתיד.
- **סיכונים:** ה-UI של הקלפים נבנה מחדש (העבודה שהושקעה ב-React UI לא עוברת); צריך המרה זהירה TS→JS של לוגיקת הפרשנות בלי לאבד כללים; היקף לא ידוע עד שנקרא את inner-compass בפועל.
- **מורכבות:** בינונית — תלוי בגודל שכבת-הידע (יוערך במדויק אחרי אישור גישה). עבודה של ימים, בשלבים עם אישור בכל שלב.
- **קבצים מושפעים:** תיקייה חדשה `goral-hachol/data/sources/cards/` (או דומה) + מסך HTML/JS חדש + פריט תפריט. **מנועי גורל החול לא נוגעים.**
- **שבירת האתר החי:** סיכון נמוך — הכל בתוספות מבודדות, לא בשינוי קיים. עם תהליך האימות הקיים (node --check, Playwright, קומיט לכל שלב) — נשלט.

### אפשרות C — להמיר את hall-of-wisdom ל-Next.js ולאחד הכל לפרויקט React אחד

- **יתרונות:** בסיס-קוד מודרני אחד; כלים עשירים; נוח למפתחי-React עתידיים.
- **סיכונים: גבוהים מאוד.** (1) מפר במישרין את כלל-הברזל של `CLAUDE.md` ("Do NOT introduce React/TS/webpack") שנקבע בדם — האפליקציה בנויה במכוון בלי build. (2) שכתוב מלא של ~20,000+ שורות מנועים ו-UI עובד ומאומת — כל שורה שמומרת היא הזדמנות לרגרסיה בפסיקות שאומתו מול ספרי-המקור לאורך חודשים. (3) שינוי מודל-פריסה (מאתר סטטי לשרת). (4) חודשי עבודה שבהם שתי גרסאות חיות במקביל. (5) כל התקלות שתוקנו היום (ציטוטים דולפים, סתירות, לכידות) ייפתחו מחדש.
- **מורכבות:** גבוהה מאוד — פרויקט של חודשים.
- **קבצים מושפעים:** כולם. ריפו חדש למעשה.
- **שבירת האתר החי:** סיכון ממשי וממושך.

## 4. השוואה מסכמת

| | A — קישור | B — העברת ידע בלבד | C — איחוד ל-Next.js |
|---|---|---|---|
| סיכון לאתר החי | אפס | נמוך | גבוה |
| מורכבות | שעות | ימים (בשלבים) | חודשים |
| ארכיון לקוחות משותף | לא (בלי עבודה נוספת) | כן | כן |
| שומר על כללי CLAUDE.md | כן | כן | **לא** |
| הפיכות | מלאה | גבוהה (תוספות בלבד) | נמוכה |

## 5. המלצה — הנתיב הבטוח לאורן משה

**מיידית: אפשרות A.** קישור מהתפריט — זמין ללקוחות כבר עכשיו, אפס סיכון.

**יעד אמיתי (מומלץ): אפשרות B, בשלבים ובאישור בכל שלב.** זה הנתיב היחיד שנותן אפליקציה אחת אמיתית (ארכיון משותף, מסקנות באותו סגנון ואותן שלוש שכבות של `OREN_CONCLUSION_STYLE_SPEC.md`) **בלי** להפר את הארכיטקטורה שהוכיחה את עצמה. שלב ראשון בו: קריאת inner-compass בפועל (דורש אישור גישה) → מיפוי שכבת-הידע של הקלפים → דוח-המשך עם היקף מדויק.

**לא מומלץ: אפשרות C.** מנוגדת לכלל מפורש בריפו, מסכנת חודשי אימות-מקורות, ואין לה תועלת ללקוח שמצדיקה זאת. אם אי-פעם תוחלט — רק כפרויקט נפרד מקביל, לא כשכתוב של האתר החי.

## 6. הצהרות

- שום התנהגות של האפליקציה לא שונתה בדוח זה.
- שום קובץ לא הוזז בין ריפוזיטוריז.
- שום מיזוג לא בוצע.
- שום פריסה לא בוצעה.
- הצעד הבא — לפי החלטת אורן משה בלבד.
