# OREN_SMART_ADVISOR_AUTH_ALLOWLIST_PLAN — תכנון מנגנון-הרשאה, ללא-קוד

> **מסמך תכנון בלבד. לא נכתבה Edge Function חדשה, לא שונה `supabase/functions/`, לא נוסף secret, לא חובר API חי, לא בוצע deploy, לא שונה HTML/UI/מנועים, לא מוזג ל-main, לא נגע ב-`inner-compass`. לא בוצע commit/push — רק יצירת הקובץ.**
> תאריך: 2026-07-08. ממשיך את `OREN_SMART_ADVISOR_PANEL_PLACEMENT_DECISION.md` (`da58d4a`) — מתכנן את מנגנון-ההרשאה **לפני** UI/חיבור-חי, מבוסס-ישירות על `OREN_SMART_ADVISOR_ACCESS_CONTROL_AUDIT.md` (הביקורת-הקיימת שכבר-מצאה שאין-הפרדת-הרשאות-ברמת-קוד היום).

---

## 1. עקרון אבטחה

- הפאנל הוא **advisor-only** — Oren Smart Advisor Brain מיועד לאורן משה בלבד, לא לשום לקוח, לא לשום משתמש-Supabase-אחר.
- **אין להסתמך על הסתרת-UI בלבד.** כפתור/פאנל-מוסתר-ב-HTML הוא נוחות-בלבד — כל-מי-שיודע/מנחש URL/endpoint יכול-לפגוע-בו-ישירות (בדיוק כפי-ש-`myseal.html` מדגים היום — נמצא ללא-session-check-כלל, ממצא-חי מ-`OREN_SMART_ADVISOR_ACCESS_CONTROL_AUDIT.md`).
- **אין להסתמך על email בלבד** — email ניתן-לזיוף/שינוי בקלות-יחסית ברמת-client, ואינו-מזהה-יציב כמו UID מ-Supabase Auth.
- **אין לחשוף מפתח-Anthropic/OpenAI בדפדפן** — בהתאמה-מלאה לעיקרון הקיים-כבר ב-`ai/provider/anthropic-provider.js` (מפתח נקרא-רק-מ-`Deno.env.get`, אף-פעם-לא-בקוד-לקוח).
- **כל קריאה לבינה חייבת לעבור דרך Edge Function מוגנת** — שום קריאה-ישירה-מהדפדפן ל-Anthropic API, שום "בדיקת-הרשאה" שרצה-רק-בצד-לקוח.

## 2. Edge Function מוצעת — המלצה

**מומלץ: Edge Function ייעודית-וחדשה, נפרדת-לגמרי מ-`oren-smart-ai`.** שם מוצע: `supabase/functions/oren-smart-advisor/index.ts` (או `oren-advisor-brain` — שני השמות שקולים-פונקציונלית; `oren-smart-advisor` עקבי-יותר עם השם שכבר-נקבע "Oren Smart Advisor Brain" בכל-המסמכים-הקודמים, ולכן מועדף).

**למה לא להרחיב את `supabase/functions/oren-smart-ai/index.ts` הקיים:**
1. **עקרון-הפרדה כבר-נקבע-מראש** — `OREN_SMART_ADVISOR_ACCESS_CONTROL_AUDIT.md` (סעיף-ההמלצות) כבר-קבע במפורש: "Edge Function ייעודית למוח-הפנימי, נפרדת-לגמרי מ-`oren-smart-ai` של Runtime AI" (מצוטט מהמסמך עצמו) — זו-לא-החלטה-חדשה, זו-הצמדה-להחלטה-קיימת.
2. **`oren-smart-ai` הוא client-facing** (בעתיד, כשיחובר) — הוא-מיועד-לניסוח-ללקוח (מודול `cartomancy`, ובעתיד חאווי/כשף), **נגיש לכל-משתמש-מחובר-שמקבל-קריאה**. Advisor Brain הוא **הפוך-לגמרי** — מוגבל-לאורן-בלבד. ערבוב-שני-התפקידים-בפונקציה-אחת מגדיל-דרסטית את-שטח-התקיפה (טעות-קטנה-אחת בבדיקת-`module` יכולה-לחשוף-תוכן-advisor-only ללקוח).
3. **מדיניות-הרשאה שונה-מהותית** — `oren-smart-ai` (כשיחובר) לא-דורש-UID-allowlist (כל-משתמש-מחובר יכול-לקבל-ניסוח-קריאה-רגילה); Advisor Brain דורש **בדיקת-UID-ספציפית**. שילוב-שני-הדפוסים-בקוד-אחד מגדיל-סיכון-לבאג-הרשאות.

## 3. זרימת הרשאה

```
1. המשתמש מתחבר דרך Supabase Auth (login רגיל, קיים-כבר-באתר)
2. הדפדפן שולח בקשה ל-Edge Function (oren-smart-advisor) עם access token (JWT)
3. ה-Edge Function מאמתת את הטוקן מול Supabase (supabase.auth.getUser(token) או שקול)
4. הפונקציה קוראת את user.id מהתוצאה
5. הפונקציה משווה את user.id מול ALLOWED_OREN_UID (secret)
6. יש התאמה?  → ממשיכים לקריאת AI (callAnthropic, עם fallback-קיים כבר)
   אין התאמה? → 403 (מחובר, אבל לא-אורן)
   אין session בכלל? → 401 (לא-מאומת)
```

**עקרון-קריטי:** הבדיקה (שלבים 3-5) **חייבת לרוץ בכל-קריאה**, לא-רק-פעם-אחת-בטעינת-הדף — כי Edge Functions הן stateless, וכל-בקשה חייבת-להוכיח-הרשאה-מחדש.

## 4. שמירת UID

- **`ALLOWED_OREN_UID`** — secret/env-var בסביבת-ה-Edge-Function (Supabase), **לא-בקוד, לא-ב-git, לא-בקובץ-.env בריפו**. שלב-ראשון: UID-אחד-בלבד.
- **עתידי (לא-עכשיו):** אם יהיו-כמה-advisors, שדרוג ל-`ALLOWED_ADVISOR_UIDS` (רשימה, מחרוזת-מופרדת-פסיקים או טבלת-`admin_users` — כפי-שכבר-הוצע ב-`OREN_SMART_ADVISOR_ACCESS_CONTROL_AUDIT.md` כשלב-2 טבעי) — **לא-מתוכנן-לביצוע-כרגע**, רק-מצוין-שהמבנה-לא-חוסם-את-זה.
- **איך משיגים את ה-UID בפועל** (לביצוע-עתידי, לא-עכשיו): אורן-משה מתחבר-פעם-אחת דרך-הלוגין-הקיים, ובודק-את-ה-UID-שלו מול-Supabase-dashboard (יש-לו-כבר project-admin-access, מאושר) — לא-דורש-קוד-חדש, פעולה-חד-פעמית.

## 5. מה אסור

לא לשמור UID בקוד-ציבורי | לא לשמור API-key בקוד | לא לבדוק-הרשאה-רק-ב-JavaScript-בצד-לקוח (בדיקת-client היא-לכל-היותר UX-נוחות, לעולם-לא-ההגנה) | לא-לאפשר-לכל-משתמש-Supabase-מחובר להשתמש-בבינה | לא-להציג-פאנל-יועץ לפי `localStorage` בלבד (בדיוק-כמו-ש-`sessionStorage`/`localStorage` היום **לא-משמשים** לבדיקת-הרשאה אמיתית באתר, רק-ל-caching — ראו `OREN_SMART_ADVISOR_ACCESS_CONTROL_AUDIT.md` סעיף 2) | **לא-לאפשר fallback-פתוח במקרה-של-שגיאת-הרשאה** — כישלון-בבדיקת-UID חייב-לחסום (`fail closed`), לעולם-לא-להמשיך-כברירת-מחדל.

## 6. תגובות שגיאה

| קוד | משמעות | מתי |
|---|---|---|
| **401** | unauthenticated | אין session תקף / טוקן חסר-או-לא-קריא |
| **403** | unauthorized | session תקף, אך `user.id` לא-תואם `ALLOWED_OREN_UID` |
| **500** | internal error | תקלה-פנימית — **בלי לחשוף פרטי-שגיאה-פנימיים** ללקוח (הודעה גנרית בלבד, לא stack-trace/שם-קובץ) |
| **503** | AI unavailable | הבדיקה-עברה (401/403 לא-רלוונטיים), אך ספק-ה-AI (Anthropic) לא-זמין/נכשל — **שונה-במפורש מ-403**: זו-לא-בעיית-הרשאה, זו-בעיית-זמינות |

**סדר-הבדיקה חשוב:** 401/403 **תמיד** נבדקים **לפני** כל-קריאה-ל-AI — אסור-לקרוא-ל-Anthropic ורק-אז-לבדוק-הרשאה (זה-היה-מבזבז-קריאת-API-בתשלום על-בקשות-לא-מורשות, ומגדיל-חשיפה מיותרת).

## 7. בדיקות עתידיות (לא-נכתבות-כעת, לתכנון-בלבד)

- ללא-token → `401`
- token לא-תקף (פג-תוקף/מזויף) → `401`
- משתמש-מחובר-שאינו-אורן (UID-לא-תואם) → `403`
- UID-של-אורן (תואם) → ממשיך-הלאה (allowed)
- **`ALLOWED_OREN_UID` חסר בסביבה** → **fail closed** (חוסם-הכל, לא-פותח-גישה-חופשית) — זה-מקרה-קריטי: קונפיגורציה-שגויה **אסור** שתיצור-פרצה
- מפתח-AI חסר (`ANTHROPIC_API_KEY`) — **לאחר** שההרשאה-כבר-אושרה — לא-קורא-ל-AI, מחזיר `503` בטוח (לא-500 עם-פרטים)
- אין-דליפת-פרטי-שגיאה-פנימיים בשום-תגובה (401/403/500/503 כולם-מחזירים הודעה-גנרית-קבועה, לא-מבנה-פנימי/stack)

כל-אלה **בדיקות-mock עתידיות** (fetch-מזויף, ללא-Edge-Function-אמיתית-רצה) — באותו-דפוס שכבר-הוכיח-את-עצמו ב-`_test-anthropic-provider.mjs` ו-`_test_oren_smart_advisor_site_brain_poc.mjs`.

## 8. איך זה משתלב עם הפאנל

- הפאנל (`OREN_SMART_ADVISOR_PANEL_PLACEMENT_DECISION.md`) יכול **להופיע ב-UI** רק-אחרי-login — אבל זו **רק** שכבת-נוחות-ראשונה.
- **ההגנה-האמיתית היא תמיד ב-Edge Function**, לא-ב-UI — גם-אם-מישהו-ידלג-על-הפאנל-לגמרי ויקרא **ישירות** ל-endpoint (curl/Postman/DevTools), הוא **ייחסם** ב-401/403 באותה-מידה.
- כפתור-"הפעל-בינה" בפאנל (כשייבנה) קורא **רק** ל-`oren-smart-advisor` Edge-Function המוגנת — לעולם-לא-ישירות ל-Anthropic, לעולם-לא-עוקף את-שכבת-האימות.

## 9. מה אפשר לעשות בלי secret עכשיו

לתכנן-את-המבנה (בוצע, מסמך זה) | להכין בדיקות-mock (עתידי, כמו §7) | לבדוק response-shapes (מבנה-401/403/500/503, עתידי) | **לא לחבר AI חי** — כל-אלה-נשארים-תרגילי-מבנה-בלבד עד-שיש `ALLOWED_OREN_UID` אמיתי.

## 10. מה דורש secret/deploy בעתיד

הגדרת `ALLOWED_OREN_UID` (Supabase secret) | הגדרת `ANTHROPIC_API_KEY` (או ספק-אחר, אם-ייבחר) | `supabase functions deploy oren-smart-advisor` בפועל | בדיקה-אמיתית מול `user.id` האמיתי-של-אורן-משה (לא-mock) — **כל-ארבעת-אלה טרם-בוצעו, לא-יבוצעו-בלי-אישור-נפרד-ומפורש**.

---

## הצהרות

- שום Edge Function חדשה לא נכתבה. שום שינוי ל-`supabase/functions/`. שום secret לא נוסף. שום API חי לא חובר.
- שום deploy. שום שינוי HTML/UI/מנועים. שום נגיעה ב-`inner-compass`. לא מוזג ל-main.
- שום commit/push — רק יצירת קובץ המסמך הזה.
- הצעד הבא — לפי החלטת אורן משה בלבד.
