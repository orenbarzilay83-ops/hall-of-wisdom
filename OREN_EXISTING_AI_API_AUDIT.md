# OREN_EXISTING_AI_API_AUDIT — ביקורת אינטגרציית ה-AI הקיימת (inner-compass)

> **מסמך ביקורת בלבד. שום קוד לא הועתק, שום מפתח לא נוסף, שום AI לא חובר בפועל.**
> תאריך: 2026-07-08. מוכן עבור אורן משה, לפי תהליך שער-האישור (`OREN_AI_SUPERVISOR_SPEC.md`).
> הפעם — בניגוד לדוח המיזוג הקודם — **הגישה ל-`inner-compass` הצליחה**. כל הממצאים כאן מאומתים מקריאת-קוד ישירה, לא מתיאור בלבד.

---

## 🚨 ממצא דחוף — מפתח API שהיה חשוף בריפו (עודכן: המפתח בוטל)

> **עדכון אבטחה (2026-07-08, אחרי פרסום הביקורת):** אורן משה ביטל/מחק את המפתח הזה בקונסולת Anthropic באותו בוקר. **המפתח מת ואינו פעיל יותר.** הסעיף הבא נשאר בתיעוד כמו שהיה בזמן-הגילוי, לתיעוד היסטורי — אך אין עוד סיכון פעיל ממנו. **היסטוריית ה-git של `inner-compass` עדיין מכילה את המחרוזת של המפתח המבוטל** (מחיקת קובץ בקומיט הבא לא מוחקת אותה מההיסטוריה) — ניקוי-היסטוריה (`git filter-repo`/BFG) יכול להתבצע בהמשך אם ירצה אורן, אך אינו דחוף כי המפתח כבר מת.

בזמן הביקורת נמצא **מפתח Anthropic API, מחויב (`git commit`) לתוך ריפו `inner-compass`** (מאז בוטל — ראו למעלה):

- קובץ: `.env.local` (בשורש הריפו)
- מצב: **עוקב אחר git** (`git ls-files` מראה אותו כקובץ מתועד, לא מקומי-בלבד) — כלומר הוא חלק מהיסטוריית ה-commits בגיטהאב, לא רק בדיסק המקומי.
- `.gitignore` של הריפו מכיל רק `node_modules` ו-`.next` — **אין שורת `.env*`** שהייתה מונעת את זה.
- המפתח מתחיל ב-`sk-ant-api03-...` (לא מודפס כאן במלואו בכוונה, כדי לא להוסיף עוד עותק שלו לקובץ נוסף).

**זו לא שאלה של ארכיטקטורה עתידית — זה סיכון אבטחה פעיל עכשיו, בריפו הקיים, בלי קשר להחלטה על hall-of-wisdom.** מפתח שנכנס פעם אחת ל-git history נשאר שם גם אם מוחקים את הקובץ בקומיט הבא (חוץ אם עושים history rewrite).

**המלצה מיידית (לא תלויה בשום החלטת-ארכיטקטורה בהמשך המסמך):**
1. סבב (rotate) את המפתח הזה מול הדשבורד של Anthropic בהקדם האפשרי — מפתח שראה אור-יום ב-git נחשב feted-compromised גם אם הריפו פרטי.
2. אחרי הסבב: הסר את `.env.local` ממעקב-git (`git rm --cached .env.local`), הוסף `.env*` ל-`.gitignore`, והגדר את המפתח החדש כ-secret בסביבת-הפריסה (Vercel וכו') ולא בקובץ בריפו.
3. זה לא בוצע כאן — רק דווח. כל פעולה בריפו `inner-compass` דורשת אישור נפרד ממך, בדיוק כמו שכל שינוי ב-`hall-of-wisdom` דורש.

---

## 1. נתיבי ה-API הקיימים ב-inner-compass

אומת ישירות בקוד:

| נתיב | קובץ |
|---|---|
| `POST /api/reading` | `app/api/reading/route.ts` (401 שורות) |
| `POST /api/reading-ppf` | `app/api/reading-ppf/route.ts` (88 שורות) |

שני הנתיבים הם **Next.js Route Handlers** (מוסכמת `export async function POST(request: NextRequest)` + `NextResponse.json(...)`) — תלויים מלאים בזמן-ריצה של Next.js (שרת Node / Vercel), לא קבצים סטטיים.

## 2. מה כל נתיב עושה

### 2.1 `/api/reading` (`route.ts`)
נתיב-מטריה יחיד עם 4 ענפים פנימיים, לפי צורת ה-body שמתקבל:

1. **פריסת 21 קלפים** (`cardCodes.length === 10`, 7 ראשיים + 3 הפתעה) — טוען מודולי-ידע (`lib/knowledge/{clubs,hearts,spades,diamonds,court-cards,numbers}`), בונה טקסט-הקשר לכל קלף (איך-לקרוא + השפעת-סדרה + משמעות-ספרה), שולח ל-Claude עם system prompt קבוע (`READER_IDENTITY_21`), מחזיר `{ conclusion, knowledgeContext }`.
2. **פריסת 3 קלפים** (`cardCodes.length === 3`) — בונה `knowledgeContext` דרך `buildKnowledgeContext` (רק לחילוץ יחסי-גומלין, **לא נשלח כמות שהוא ל-AI**), מזהה נושא (`detectTopic`) וזיהוי "שאלה על עצמי מול על אחרים" (regex), אוסף חומרי-מסקנה לכל קלף (נומרולוגיה + השפעת-סדרה + משמעות-בהקשר), מזהה כללים-מיוחדים (קונפליקט בין קלפי-חצר, קלף-משאלה), בונה `userMessage` מובנה, שולח ל-Claude עם `READER_IDENTITY`, מחזיר `{ conclusion, knowledgeContext }`.
3. **פריסת עבר/הווה/עתיד — 32 קלפים** (`body.pastCards && presentCards && futureCards && surpriseCards`) — בונה טקסט לכל ערימה (`buildPileText`) כולל זיהוי-קבוצות, שולח ל-Claude עם `READER_IDENTITY_21`.
4. **נתיב legacy** (מגן דוד ופריסות אחרות, `spreadType`) — מקבל `rawConclusion` מהמנוע הדטרמיניסטי כבר-מחושב, עוטף אותו בהוראה ("נסח לטקסט זורם, שמור על כל הפרטים") ושולח ל-Claude.

בכל הענפים: אם ה-body לא תואם אף תבנית וגם `rawConclusion` ריק — מחזיר `{ conclusion: "" }` בלי לקרוא ל-AI כלל.

### 2.2 `/api/reading-ppf` (`route.ts`)
נתיב ייעודי לפריסת עבר/הווה/עתיד (32 קלפים) בלבד — נראה כגרסה-מקבילה/מאוחרת יותר לענף מס' 3 שב-`/api/reading`. ההבדל המרכזי: **קודם בונה מסקנה דטרמיניסטית מלאה דרך `lib/ppf-engine.ts::buildPPFEngineConclusion`** (מנוע JS טהור, ללא AI — מחשב valence לכל קלף, קושר בין קלפים במחברים כמו "אבל"/"ולמרות זאת"/"ולכן" לפי היגיון-קבוע, לא לפי בחירת-מודל), ורק **אז** שולח את הפלט הדטרמיניסטי הזה ל-Claude עם הוראה מפורשת: "ארוג את הקטעים לעיל לטקסט זורם בעברית... אל תוסיף מילה שלא כתובה כאן." כלומר: **ה-AI כאן הוא שכבת-ניסוח בלבד, לא שכבת-החלטה** — המנוע הדטרמיניסטי כבר קבע את כל התוכן והפולריות.

## 3. הספק (Provider) בשימוש

**Anthropic**, ישירות דרך ה-SDK הרשמי:
```
"@anthropic-ai/sdk": "^0.39.0"
```
אתחול ב-שני הקבצים:
```ts
const client = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY });
```
המודל **מקובע (hardcoded)** בכל ארבע קריאות ה-`client.messages.create` (3 ב-`route.ts`, 1 ב-`reading-ppf/route.ts`): `model: "claude-sonnet-4-6"`. אין קובץ-קונפיגורציה מרכזי למודל — אם צריך לשנות, משנים ב-4 מקומות בנפרד.

## 4. משתנה-הסביבה הצפוי

`ANTHROPIC_API_KEY` — מאושר, ומדובר בדיוק כפי שתואר. **אך (ראו סעיף "ממצא דחוף" למעלה) הוא כרגע לא רק "צפוי כמשתנה-סביבה" — הוא גם מחויב-לגיט בפועל בקובץ `.env.local`, מה שסותר את עצם הרעיון של משתנה-סביבה סודי.**

## 5. חלקים שניתן לעשות בהם שימוש-חוזר

1. **תבנית קריאת-הספק (provider call pattern):**
   ```ts
   const client = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY });
   const response = await client.messages.create({
     model, max_tokens, system, messages: [{ role: "user", content: userMessage }],
   });
   const text = response.content[0]?.type === "text" ? response.content[0].text : "";
   ```
   זו תבנית גנרית, לא תלוית-קלפים — ניתנת-להעברה כמעט as-is לשכבת-adapter חדשה.

2. **מבנה ה-prompt (system + user נפרד, בעברית):** `system` = "זהות" קבועה (מי אתה, מה מותר/אסור לך לעשות) + `userMessage` = עובדות מובנות (שאלה, פרופיל, ממצאי-מנוע) + הוראת-ניסוח מפורשת. הדפוס הזה — **"מנוע דטרמיניסטי קובע עובדות ופולריות, ה-AI רק מנסח לטקסט זורם ואסור לו להוסיף עובדה"** — תואם *בדיוק* לעקרון שכבר קיים ב-`OREN_CONCLUSION_STYLE_SPEC.md` ("אין להמציא נתונים... כל עובדה בפלט חייבת עקיבות-מקור"). הביטוי החוזר בקוד "השתמש אך ורק בידע שקיבלת — אסור להוסיף מילה שלא כתובה בנתונים" הוא בדיוק כלל-הברזל של hall-of-wisdom, בניסוח אחר.

3. **מבנה בקשה/תגובה:** `POST` עם body של עובדות מובנות (לא טקסט חופשי) → `{ conclusion: string, knowledgeContext?: string }`. פשוט, ניתן-להעתקה כחוזה-API.

4. **נפילה חזרה (fallback) לפלט המנוע הדטרמיניסטי:**
   ```ts
   return NextResponse.json({ conclusion: conclusion || engineAnalysis });
   ```
   אם ה-AI מחזיר ריק (שגיאת-רשת, timeout, תגובה ריקה) — חוזרים לטקסט שהמנוע הדטרמיניסטי כבר בנה. זה בדיוק דפוס-הבטיחות שנחוץ ל-hall-of-wisdom: **אם ה-AI נופל, הלקוח עדיין מקבל את המסקנה הקיימת (`goral-conclusion-writer.js`/`kashf-narrative-writer.js`) — לא מסך שגיאה, ולא המצאה.**

5. **עקרון "AI כשכבת-ניסוח בלבד" (מודגם הכי נקי ב-`reading-ppf`):** המנוע (`ppf-engine.ts`) מחשב את כל ה-valence/הקשרים/מחברים-לוגיים לפני שה-AI בכלל נכנס לתמונה. זה ההשראה הישירה לארכיטקטורה המומלצת בסעיף 7 למטה — בדיוק כמו ש-`narrative-fact-phrasing.js` כבר בונה עכשיו משפטים מעובדות-מחושבות בלי AI, ה-AI (אם יתווסף) יכול לרשת בדיוק אותה עובדה מוגמרת ולנסח אותה מחדש, לא להמציא תוכן חדש.

## 6. חלקים שאסור להעתיק ישירות

1. **מוסכמת ה-Route של Next.js** (`export async function POST(request: NextRequest)`, `NextResponse.json`) — אין לה מקבילה ב-hall-of-wisdom (סטטי, בלי שרת). כל שימוש-חוזר בלוגיקה חייב לעטוף מחדש ב-runtime אחר (ראו סעיף 7).
2. **המפתח המחויב ב-`.env.local`** — אסור בהחלט להעתיק את הקובץ, את המפתח, או אפילו את הרעיון "לשמור מפתח בקובץ שנכנס ל-git". ראו "ממצא דחוף" למעלה — נדרש סבב-מפתח לפני כל שימוש חוזר בדפוס הזה בכלל.
3. **המודל המקובע ב-4 מקומות שונים בקוד** (`"claude-sonnet-4-6"`) — הנחת-ספק/מודל מוטמעת בקוד ולא בקונפיג. אם מעתיקים את התבנית, יש לרכז את שם-המודל למשתנה/קונפיג יחיד מההתחלה — לא לשכפל את ההטמעה-הקשיחה.
4. **תוכן ה-prompts עצמם (`READER_IDENTITY`, `READER_IDENTITY_21`, `READER_IDENTITY_PPF`)** — כתובים במפורש עבור קריאת-קלפים (מדברים על "קלף", "סדרה", "קלף הפוך", מבנה של 3/7/10/32 קלפים). אינם ניתנים-לשימוש עבור גורל החול (חאווי/כשף) או גורל העשיריות בלי כתיבה מחדש מלאה — התחום, אוצר-המילים, וכללי-הגזירה שונים לגמרי (בתים, צורות, דיין ועדים, לא קלפים וסדרות).
5. **ה-`await import("@/lib/knowledge/...")` הדינמי** — מסתמך על bundler של Next.js/webpack לפיצול-קוד; אין לו מקבילה בדפדפן-בלי-build של hall-of-wisdom (אפשר `import()` דינמי גם ב-ES modules טהורים, אבל לא באותה מוסכמת-נתיב `@/`).

## 7. הארכיטקטורה הבטוחה ביותר עבור hall-of-wisdom

בהתאם למבנה המבוקש, ובהתאמה לכללי-הברזל של `CLAUDE.md`/`AGENTS.md` (JS טהור, בלי build, בלי framework):

```
hall-of-wisdom/
├── ai/
│   ├── provider/
│   │   └── anthropic-provider.js      ← שכבת-adapter: פונקציה אחת שעוטפת client.messages.create,
│   │                                      עם model כפרמטר-קונפיג (לא מקובע), retry/timeout, וטיפול-שגיאה
│   │                                      שמחזיר null/undefined (לא זורק) כדי לאפשר fallback
│   └── prompts/
│       ├── goral-hachol-runtime.md    ← system prompt ייעודי לחאווי/כשף — "נסח מחדש עובדות
│       │                                  שכבר חושבו, אל תמציא, שמור על 3 השכבות של
│       │                                  OREN_CONCLUSION_STYLE_SPEC.md"
│       ├── cartomancy-runtime.md      ← אם/כש-מודול-הקלפים ייכנס ל-hall-of-wisdom (ראו
│       │                                  MERGE_INNER_COMPASS_INTO_HALL_OF_WISDOM_REPORT.md אופציה B)
│       └── goral-haasiriyot-runtime.md
└── (runtime route — לא קובץ סטטי):
    /api/oren-smart-ai   ← Supabase Edge Function *או* פונקציית-serverless מינימלית נפרדת,
                             לא חלק מהדפדפן-בלי-build. ה-frontend הסטטי קורא לה ב-fetch רגיל,
                             בדיוק כמו שהיא הייתה שרת חיצוני כלשהו — לא נכנס ל-node_modules
                             של hall-of-wisdom עצמו.
```

**עקרונות מפתח:**
- **המפתח לעולם לא בקוד-לקוח** — לא ב-HTML, לא ב-JS שרץ בדפדפן, לא בריפו בכלל. הוא חי רק בסביבת-הרצה של ה-Edge Function/Route, כ-secret אמיתי של הפלטפורמה (Vercel/Supabase).
- **המנוע הדטרמיניסטי נשאר הבעלים היחיד של העובדות** — בדיוק כמו ב-`reading-ppf`: `narrative-fact-phrasing.js`/`goral-conclusion-writer.js`/`kashf-narrative-writer.js` ימשיכו לחשב הכל; ה-AI (אם יתווסף) יקבל רק פלט-מוגמר לניסוח-מחדש, לא נתונים גולמיים לפרש בעצמו. זה שומר על כלל "אין נתונים מומצאים" באופן מבני, לא רק בהוראת-prompt.
- **Fallback חובה** — בדיוק כמו `conclusion || engineAnalysis`: אם קריאת ה-AI נכשלת/timeout/ריקה, חוזרים למסקנה הדטרמיניסטית הקיימת. הלקוח **אף פעם** לא רואה שגיאת-AI — לכל היותר מקבל את הפלט המדויק שהוא מקבל היום, בלי שכבת-AI כלל.
- שלוש קבצי ה-prompt נפרדים בכוונה לפי מודול — כך שכל מודול (חאווי/כשף, קלפים, עשיריות) שומר על אוצר-המילים והכללים הספציפיים שלו, בלי לערבב.

## 8. מסלול-מעבר: קריאת-הקלפים הקיימת כמימוש-Runtime-AI ראשון

ה-API הקיים ב-`inner-compass` **יכול** לשמש כבסיס-השראה (לא-העתקה) למימוש הראשון של Runtime AI — אבל רק אחרי, ובסדר הזה:

1. **סבב מפתח-API** (חובה — ראו "ממצא דחוף" למעלה, לא תלוי בשום החלטה אחרת).
2. **הסודות עוברים לצד-שרת בלבד** — לא בקובץ בריפו, לא ב-`.env.local` המחויב. Secret אמיתי בסביבת-הפלטפורמה.
3. **אין מפתחות בגיטהאב** — כולל היסטוריה: המפתח-הישן שכבר דלף נחשב שרוף לצמיתות, גם אם יימחק מקומיט עתידי.
4. **אין מפתחות ב-HTML** — כל קריאה ל-AI עוברת דרך ה-Edge Function/Route, אף פעם לא ישירות מ-JS-בדפדפן עם מפתח מוטמע.
5. **אישור אורן משה** — לפני כל חיבור-AI חי, כולל לפני כל preview עם מפתח אמיתי (גם לא-production).

רק אחרי שכל 5 מתקיימים — אפשר לשקול לקחת את דפוס ה-`reading-ppf` (מנוע דטרמיניסטי → AI מנסח-בלבד) כתבנית ליישום המקביל בגורל החול.

## 9. המלצה מעשית

| אפשרות | תיאור | הערכה |
|---|---|---|
| **A — לעשות שימוש-חוזר בדפוס Anthropic הקיים** | לאמץ את אותו ספק (Anthropic) ואת אותו דפוס "מנוע דטרמיניסטי → AI מנסח" | **מומלץ.** הדפוס כבר קיים, עובד, ומוכח (יש אתר חי שמשתמש בו). אין צורך ללמוד ספק חדש. הסיכון היחיד שדורש תיקון הוא ניהול-הסודות (סעיף "ממצא דחוף"), לא הספק עצמו. |
| **B — להחליף ל-OpenAI** | ספק אחר לגמרי | לא מומלץ כברירת-מחדל — אין סיבה טכנית לכך; ידרוש כתיבה-מחדש של אינטגרציית-הספק בלי תועלת ברורה. שקול רק אם יש שיקול עסקי חיצוני (תמחור/הסכם) שלא מופיע כאן. |
| **C — שכבת-adapter גנרית שתומכת בשניהם** | `ai/provider/anthropic-provider.js` + מבנה שמאפשר גם ספק חלופי בעתיד בלי לשכתב את שאר המערכת | **הכי בטוח לטווח-ארוך**, אך תוספת-מורכבות שלא נחוצה ביום הראשון. |

**המלצה מעשית לאורן משה:** להתחיל ב-**A בתוך מבנה שכבר בנוי כמו C** — כלומר: לממש `anthropic-provider.js` כפונקציה קטנה ומבודדת (לא מפוזרת ב-4 מקומות כמו היום), עם המודל כפרמטר לא מקובע. זה נותן את הפשטות המיידית של A (ספק אחד, דפוס מוכח) עם כמעט אפס עלות להשארת הדלת פתוחה ל-B בעתיד אם יהיה צורך. **לא להתחיל בשום מימוש** לפני סבב-המפתח שתואר למעלה.

---

## 10. הצהרות

- שום קוד לא הועתק מ-`inner-compass` ל-`hall-of-wisdom`.
- שום מפתח-API אמיתי לא נוסף לשום מקום.
- שום AI חי לא חובר.
- שום התנהגות של אף אחת מהאפליקציות לא שונתה.
- **פעולה נדרשת ממך באופן עצמאי, לא תלויה בהחלטת-ארכיטקטורה:** סבב מפתח-ה-API שנמצא חשוף ב-`inner-compass/.env.local`.
- הצעד הבא — לפי החלטתך בלבד.
