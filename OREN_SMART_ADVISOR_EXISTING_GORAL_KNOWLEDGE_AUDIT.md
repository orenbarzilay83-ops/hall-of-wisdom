# OREN_SMART_ADVISOR_EXISTING_GORAL_KNOWLEDGE_AUDIT — מה כבר קיים בפועל, חאווי וכשף

> **מסמך בדיקה בלבד. לא נכתב קוד, לא שונה HTML, לא שונה מנוע, לא נוצר storage/tables/migrations, לא הועלו ספרים, לא נוצרו embeddings, לא נוצר UI, לא נוסף AI חי, לא נוסף secret, לא בוצע deploy, לא נגע ב-`inner-compass`. לא בוצע commit/push — רק יצירת הקובץ.**
> תאריך: 2026-07-08. **אין כאן שום קביעה של "מי קודם" ואין הנחה ש"חסרים ספרים"** — המסמך בודק ומתעד בלבד את מה שכבר בנוי, לפני כל החלטת-המשך. כל ממצא נבדק ישירות בקוד (קריאת-קבצים + `grep`/`wc`/`find` בפועל).

---

## 1. חאווי — מה כבר קיים

### קבצי-data
`goral-hachol/data/sources/hawi/` — **83 קבצי-JS**, מחולקים כך:
- **16 קבצי figure-transits** (+ 2 קבצי-אינדקס/audit) — משמעות כל אחת מ-16 הצורות בכל אחד מ-16 הבתים. **אומת מבנה בפועל** (`hawi-figure-tariq.js`): כל קובץ מכיל `sourceBook` (שם-ספר ערבי מלא), `sourceSectionArabic`+`sourceSectionHebrew`, `extractionStatus` (המקבילה של `sourceStatus` בחאווי — שם-שדה שונה!), `generalDescription`/`generalMeaning` (עברית מלאה, לא זוגות arabicText/hebrewTranslation מקבילים), ומערך `houses[]` עם `meaning`/`topics`/`specialRules`/`criticalArabicTerms` (מונחי-ערבית ספציפיים משובצים בתוך המשמעות, לא כטקסט-מקור-מלא-מקביל).
- **16 קבצי figure-states** (+ 2 אינדקס/audit) — מצבי-מזל/תנועה לכל צורה.
- **25 קבצי question-rules** (+ תבנית + אינדקס) — כללים ספציפיים-לנושא (מסחר, נישואין, מחלה, ירושה, וכו').
- **foundations/** (8 קבצים) — יסודות: בתים 16, מהו-ות'אבת, עדים, דיאגנוסטיקה-רוחנית, כיווני-דמיר, חילוץ-אותיות.
- **authority-state/, birth-nativity/, yearly-forecast/, triangles/, planetary-correspondences/** — קובץ-אחד-לכל-נושא.
- `hawi-source.js`, `hawi-metadata.js`, `hawi-topic-index.js`, `hawi-knowledge-router.js` (627 שורות — נתב-ידע מרכזי), `hawi-full-book-gap-audit.js` (119 שורות — תיעוד-פערים-קיים-בעצמו).

### engines
`hawi-interpreter.js` (3,019 שורות) — הבנאי המרכזי (`interpretHawiQuestionInitial`, `buildFinalConclusion`, `boardAnalysis`). `goral-conclusion-writer.js` (2,884 שורות) — כותב-הנרטיב ללקוח (`writeHumanGoralConclusion`, ~20 ענפי-נושא). ביחד: **5,903 שורות מנוע**.

### source rules / figure transits / house rules
קיימים במלואם (ראו "קבצי-data" למעלה) — 16/16 צורות × 16/16 בתים מכוסים ב-figure-transits, 25 קבצי-כללי-שאלה ייעודיים.

### prompt (Runtime AI)
`ai/prompts/hawi-runtime.md` — **stub בלבד**, אין תוכן-prompt אמיתי (מאושר-כך במפורש ב-`AI_RUNTIME_GORAL_HACHOL_FIRST_PLAN.md`).

### חיבור ל-`goral-hachol.html`
`goral-hachol/ui/goral-app.js` — משתנה מפורש `let goralMode = 'hawi'; // 'hawi' | 'kashf'` (שורה 8), עם ענף-קוד ייעודי (`if (goralMode !== 'kashf' && window.HAWI_INTERPRETER?.interpretHawiQuestionInitial)`) — **חיבור-קוד אמיתי ופעיל**, לא רק תכנון.

### מה כבר עובד
מנוע מלא, 20 ענפי-נושא עם שכבת-סינתזה חלקית (`narrative-fact-phrasing.js`, מסחר בלבד עדיין), מחובר ל-UI, נבדק בדפדפן (היסטוריית-WORKPLAN מתעדת בדיקות-Playwright חוזרות).

### מה רק מתוכנן
Runtime-AI prompt אמיתי (`hawi-runtime.md`), גלגול שכבת-הסינתזה ל-19 נושאים נוספים.

### מה חסר
לפי `WORKPLAN.md` #20: כ-90 פונקציות/קבועים ב-`hawi-interpreter.js` לא כולם אומתו שורה-שורה מול הטקסט-המקורי (רק סבב-סריקה-אחד בוצע). `computeGhalibMaghloub` (אלגוריתם שונה מהמתואר במקור, מתועד כפער-פתוח).

## 2. כשף אל-אסראר — מה כבר קיים

### קבצי-data
`goral-hachol/data/sources/kashf-al-asrar/` — 11 קבצים, כולל `kashf-al-asrar-book.js` (4,509 שורות, מערך `KASHF_AL_ASRAR_PAGES`, **260 רשומות-עמוד**, עמודים 21–276+).

### מבנה `kashf-al-asrar-book.js` — **ממצא חשוב, מתקן הנחה קודמת**
נבדק ישירות (ספירת-שדות בפועל):
```
hebrewTranslation: 260 מופעים
sourceStatus:       260 מופעים
topics:             260 מופעים
arabicText:           0 מופעים  ← לא קיים בקובץ הזה בכלל!
chapter:               0 מופעים (יש chapterHebrew בלבד)
```
**מסמכים קודמים בריפו (כולל `CLAUDE.md` וכולל מסמכים שנכתבו קודם בסבב הזה) תיארו את `KASHF_AL_ASRAR_PAGES` כמכיל `arabicText` + `hebrewTranslation` יחד לכל עמוד — זה לא נכון בפועל.** הקובץ הזה מכיל **רק את התרגום העברי**, בלי טקסט ערבי מקביל בכלל.

### קובץ ערבי נוסף שנמצא בריפו — מעמדו טרם הוכרע
`kashf-al-asrar-arabic-only.html` (3,688 שורות, **189,172 תווים ערביים** — הכי-הרבה-ערבית מכל קובץ בריפו) — מובנה בדיוק לפי-עמוד (`<span class="page-num">صفحة 21</span>` וכו', מתחיל מעמוד 21 — **אותו טווח-עמודים** כמו `kashf-al-asrar-book.js`). **מתועד ב-git עם היסטוריית-קומיטים אמיתית, שקדמה לסבב-העבודה הנוכחי כולו** (`git ls-files`/`git log` אושרו — הקובץ קיים פיזית בריפו, לא בטעות ולא כתוצר-זמני).

**תיעוד עובדתי, לפי הכרעת אורן משה (2026-07-08):**
1. כשף אל-אסראר באפליקציה כולל **בוודאות** את התרגום העברי של אורן משה (`kashf-al-asrar-book.js`, 260 עמודים, ראו לעיל).
2. בנוסף, נמצא בריפו קובץ ערבי בשם `kashf-al-asrar-arabic-only.html`.
3. הקובץ הערבי **קיים פיזית בריפו**, אך **מעמדו לשימוש בבינה החכמה עדיין לא הוכרע**.
4. **עד אישור מפורש של אורן משה — הבינה לא תשתמש בו כמקור מאושר.**
5. אם בעתיד אורן משה יאשר אותו, הוא יוכל לשמש להשוואה/אימות מול התרגום העברי.
6. אם אורן משה לא יאשר אותו — הוא יישאר קובץ קיים בריפו, אך **לא** מקור-עבודה לבינה.

**קיים page-alignment פוטנציאלי** בין `kashf-al-asrar-arabic-only.html` לבין `kashf-al-asrar-book.js` (שני הקבצים מתחילים בעמוד 21) — אך הם אינם מקושרים בקוד בפועל, ואין לראות בכך אישור-שימוש. שום ingest/storage/database/embeddings לא בוצעו ולא יבוצעו על הקובץ הזה בלי אישור נפרד ומפורש.

### עוד קובץ: `kashf-al-asrar.html`
קובץ שלישי (3,932 שורות) — "ספר" מלא בעיצוב-הדפסה (TOC + פרקים מלאים, כולל שערים 1–6 בשלמותם לפי כותרות-התוכן-עניינים שנבדקו: הבתים, הצורות, סדרי-השיבוץ, גילוי-הכוונה-הנסתרת, ארבע-צורות-היסוד, הדין-על-12-הבתים). **עברית בלבד** (0 תווים ערביים). `<title>` הקובץ: **"חשיפת הסודות הנצורים — בהוצאת המחשבות הגנוזות לאור | מהדורה עברית מלאה"** — שם-הקובץ תואם-בדיוק לשם-התרגום-העברי-המאושר שכבר נקבע במסמכי-המדיניות. **אינו מייבא/מקושר ל-`kashf-al-asrar-book.js`** — נראה כקובץ עצמאי, ייתכן-שמכיל-יותר-תוכן (כל 6 השערים לפי ה-TOC) ממה ש-`kashf-al-asrar-book.js` חושף כרגע כ-JS-data-מובנה.

### ⚠ ממצא-פער — אי-התאמת שם-ספר ערבי, דורש הבהרת אורן משה
שם-הספר הערבי ששימש בכל המסמכים המאושרים עד כה (`OREN_SMART_ADVISOR_BRAIN_PLAN.md` ואילך): **كشف الأسرار المصونة في إخراج الضمائر المخزونة**.
שם-הספר שמופיע בפועל בקבצים שנבדקו כרגע:
- `<title>` של `kashf-al-asrar-arabic-only.html`: **"كشف الأسرار — النص العربي الكامل"** (כותרת-תיאורית, "כשף אל-אסראר — הטקסט הערבי המלא").
- `<h1>` בתוך אותו קובץ: **"كشف الأسرار في علم الحرف والغبار"**.
זו **לא בהכרח סתירה** — ייתכן שם-קצר מול שם-מלא/פורמלי, או שם-שער מול שם-ספר — אך זהו הבדל-מילולי ממשי שנמצא בין מה-שכבר-מאושר-בתיעוד למה-שכתוב-בפועל-בקובץ. **המסמך הזה לא מכריע ביניהם** — זו שאלה שדורשת הבהרה ישירה מאורן משה לפני שקובעים מטא-דאטה סופית (`sourceName` בסכמה שהוצעה ב-`OREN_SMART_ADVISOR_PRIVATE_KNOWLEDGE_STORAGE_PLAN.md`).

### engine
12 קבצים תחת `goral-hachol/engine/kashf-*.js`, **6,939 שורות סה"כ** — הגדול מבין השניים (חאווי: 5,903). המרכזיים: `kashf-topic-rules.js` (1,952), `kashf-pending-extraction.js` (1,781), `kashf-narrative-writer.js` (704), `kashf-reading-engine.js` (645), `kashf-dhamir.js` (535), `kashf-book-additions.js` (532).

### prompt (Runtime AI)
`ai/prompts/kashf-runtime.md` — **stub בלבד**, זהה-במעמד ל-`hawi-runtime.md`.

### חיבור ל-`goral-hachol.html`
אותו מנגנון `goralMode` (`goral-app.js`) — `goralMode === 'kashf'` מפעיל ענף-קוד נפרד שקורא ל-`window.KASHF_ENGINE.buildKashfReading`/`writeKashfReading`, בונה `kashfBoard`, מציג ל-`#kashfReadingOutput`. **חיבור-קוד אמיתי ופעיל**, לא מסך נפרד-לגמרי — אותו `goral-hachol.html`, ענף-לוגי-נפרד.

### מה כבר עובד
29 נושאים מלאים (`KASHF_TOPIC_RULES`), 5 שיטות-דמיר (3 פעילות מלאות + סוג-4 עם גילוי-מקור-חיצוני), שער 5 (4 צורות-יסוד), שכבת-סינתזה (`narrative-fact-phrasing.js`) עדיין-לא-מיושמת-בכשף (רק בחאווי, נושא מסחר).

### מה רק מתוכנן
`kashf-runtime.md` (אחרי חאווי, לפי `AI_RUNTIME_GORAL_HACHOL_FIRST_PLAN.md`), גלגול שכבת-הסינתזה.

### מה חסר
שער 4 סוג 5 (הליכה-איטרטיבית, אין כלל-עצירה במקור). חלק משיבוצי שער 3 (4–15) ללא-שימוש-מזוהה. **וכעת גם:** קישור-בפועל בין `kashf-al-asrar-arabic-only.html`/`kashf-al-asrar.html` לבין `kashf-al-asrar-book.js` — שלושתם קיימים, אך לא מאוחדים למבנה-נתונים-יחיד ניתן-לשאילתה.

## 3. הפרדה בין השיטות

**כן, מופרדות בבירור ברמת-הקוד-החי:** `goralMode` (`goral-app.js` שורה 8) הוא נקודת-הכרעה יחידה וברורה, לא לוגיקה-מפוזרת. שני ה-engines (`hawi-interpreter.js`/`goral-conclusion-writer.js` מול 12 קבצי `kashf-*.js`) עצמאיים.

**קבצים משותפים שנמצאו (2, שניהם מתועדים-במפורש כבכוונה, לא בטעות):**
1. **`raml-board-generator.js`** — בונה-הלוח-מ-4-אמהות (`buildRamlBoardFromMothers`). זו לוגיקת-מתמטיקה-גיאומנטית משותפת (מהו/ות'אבת, 16 בתים) — לא תוכן-ספר. **ממצא-משנה לתיוג:** האובייקט-המוחזר מכיל `source: 'hawi'` קבוע (שורה 247) — לא נמצא ענף-מקביל `source: 'kashf'`. ייתכן זה תג-ברירת-מחדל שלא-נבדק-בפועל אם משפיע על משהו, או שריד-מתקופה-שהמנועים-לא-היו-מופרדים — **לא נבדק לעומק בסבב הזה, מסומן כשאלה-לבדיקה** (לא ודאי שזו בעיה, רק ממצא-תיוג לא-אינטואיטיבי שכדאי-לבדוק).
2. **`kashf-pending-extraction.js`** — מייבא `isMaleficG`/`FIGURE_HEBREW_G` (מסווגי-צורה גנריים) ו-`ELEMENT_DIRECTION` (מיפוי-יסוד→כיוון, מסומן בפירוש בהערת-קוד כ-"משותף גם עם computeDiggingDirection החאווי") מ-`hawi-interpreter.js`. **זו לא דליפת-תוכן** — הערת-הראש של הקובץ אומרת במפורש: אלו פונקציות **שהוצאו** מ-`hawi-interpreter.js` כי הן **שייכות-לכשף**, לא לחאווי (טעות-מיקום היסטורית שכבר תוקנה חלקית) — הקובץ "עדיין לא מחובר לשום מנוע חי" (הערת-הקוד עצמה), כלומר תהליך-הפרדה-שטרם-הושלם, לא ערבוב-פעיל.

**סכנת-ערבוב מקורות:** נמוכה ברמת-הקוד-החי (`goralMode` תקיף וברור). **קיימת בהחלט ברמת-התיעוד** — הממצא בסעיף 2 (`arabicText` לא-קיים בפועל, שם-הספר-הערבי לא-תואם) מראה שתיעוד-קודם (כולל שלי, במסמכים שנכתבו מוקדם-יותר היום) כבר טעה בפרט-עובדתי לגבי מבנה-הנתונים של כשף — לא ערבוב-חאווי/כשף, אלא **תיאור-לא-מדויק של מה-שכבר-קיים**, בדיוק מהסוג שהמסמך הזה נועד למנוע.

**שמות/נתיבים העלולים-לבלבל:** `kashf-al-asrar-book.js` (JS data, עברית-בלבד), `kashf-al-asrar.html` (HTML "ספר"-מלא, עברית-בלבד), `kashf-al-asrar-arabic-only.html` (HTML, ערבית-בלבד) — שלושה קבצים בשמות-דומים-מאוד, תפקידים-שונים-לגמרי, לא-מקושרים-בקוד. סכנה-ממשית שבינה-פנימית (או מפתח-אנושי) יבלבל ביניהם או יניח-שהם-מסונכרנים כשהם-לא.

## 4. מפת קבצים — לפי שיטה

### חאווי (83 קבצי-data + 2 קבצי-engine)
`goral-hachol/data/sources/hawi/**` (כל 83 הקבצים שנמנו בסעיף 1) + `goral-hachol/engine/hawi-interpreter.js` + `goral-hachol/engine/goral-conclusion-writer.js` (משותף חלקית — כותב גם עבור נושאי-חאווי, לא ייעודי-בלעדית) + `ai/prompts/hawi-runtime.md` (stub).

### כשף אל-אסראר (11 קבצי-data + 12 קבצי-engine + 3 קבצי-HTML עצמאיים)
`goral-hachol/data/sources/kashf-al-asrar/**` (11 קבצים) + כל 12 קבצי `goral-hachol/engine/kashf-*.js` + `goral-hachol/data/sources/al-falak-al-mashhun/al-falak-abjad-decomposition.js` (מקור-חיצוני-מגולה, לא-כשף-עצמו) + `ai/prompts/kashf-runtime.md` (stub) + **בנפרד, לא-מקושר בקוד:** `kashf-al-asrar.html`, `kashf-al-asrar-arabic-only.html`.

### משותף לשתי-השיטות (מתועד, בכוונה)
`goral-hachol/engine/raml-board-generator.js`, `goral-hachol/engine/raml-board.js`, `goral-hachol/engine/raml-figures.js`, `goral-hachol/ui/goral-app.js` (מנגנון `goralMode`), `goral-hachol/ui/question-bank.js` (`topicId`+`kashfTopicId` נפרדים, כבר אושר-כמבנה-נכון ב-WORKPLAN #23).

## 5. מצב ידע קיים — לפני כל שיקול של "מה חסר"

**הממצא המרכזי של המסמך הזה:** יש **הרבה יותר ידע-מתועד-בפועל** בריפו משהמסמכים-הקודמים-של-הבינה-הפנימית הניחו:
- חאווי: 83 קבצי-data מובנים, 16/16 צורות × 16/16 בתים מכוסות, 25 קבצי-כללי-נושא.
- כשף: לא רק `kashf-al-asrar-book.js` (260 עמודים, עברית) — אלא **גם** טקסט-ערבי-מלא כבר-דיגיטלי (`kashf-al-asrar-arabic-only.html`, 189K תווים) **וגם** רינדור-ספר-עברי-שני נפרד ומקיף-יותר (`kashf-al-asrar.html`, כולל שערים 1–6 לפי ה-TOC).

**המסקנה המתבקשת:** לפני כל שיחה על "להכניס ספרים" למאגר-ידע-פרטי-חדש (`OREN_SMART_ADVISOR_PRIVATE_KNOWLEDGE_STORAGE_PLAN.md`) — **הידע הגולמי כבר קיים בריפו במידה רבה**. מה שחסר הוא לא הטקסט עצמו (לרוב-שערי-כשף, לפחות) אלא: (א) איחוד-שלושת-הקבצים-הנפרדים למבנה-ניתן-לשאילתה, (ב) פתרון-אי-ההתאמה בשם-הספר-הערבי, (ג) הוספת `sourceStatus`/מטא-דאטה עקבית גם לחלקים-שעדיין-בתוך-HTML-סטטי ולא ב-JS-data מובנה.

## 6. המלצה — לפי הבדיקה בלבד, לא לפי הנחה

1. **צריך מפת-ידע לשתי השיטות — כן, אך בעדיפות-שונה מהצפוי.** לא "איזה ספר מכניסים" — אלא **מיפוי-מדויק של מה-שכבר-קיים** לכל אחד מ-3 קבצי-הכשף (מה בדיוק מכיל `kashf-al-asrar.html` שלא ב-`kashf-al-asrar-book.js`? האם השערים 2–6 שם כבר-מכוסים-בקוד-אחר, או רק בעיצוב-הדפסה-שלא-מנוצל?).
2. **צריך סטנדרט-metadata — כן**, אבל קודם-כל כדי **לתעד-נכון את מה-שכבר-קיים** (כולל תיקון-ההנחה-השגויה על `arabicText`), לא רק כתשתית-לספר-חדש-עתידי.
3. **צריך בדיקת-איכות לקבצים-קיימים — כן, בדיוק ובראש-סדר-העדיפויות:** לפחות שני ממצאים מהמסמך הזה (העדר `arabicText` בפועל, אי-התאמת שם-הספר-הערבי) דורשים בירור-ישיר מול אורן משה **לפני** כל עבודת-אינטגרציה נוספת.
4. **צריך לחבר את הבינה קודם לקריאת-מסמכים-קיימים ולא להכניס ספר חדש — כן, זו ההמלצה החזקה ביותר של המסמך הזה.** הכמות והעומק של הידע-הכבר-קיים (5,903+6,939 שורות-engine, 83+11 קבצי-data, 189K-תווי-ערבית, ספר-עברי-שני-מלא) גדולה משמעותית ממה שתועד קודם — צעד-הכי-בעל-ערך הבא הוא **לגרום לבינה לדעת-לקרוא ולצטט את מה שכבר קיים כאן**, לא לתכנן קליטת-חומר-חדש.

---

## 7. רשימת קבצים שנבדקו בפועל (סבב זה)

**חאווי:** `goral-hachol/data/sources/hawi/` (מבנה-מלא, `find`+`wc`), `hawi-figure-tariq.js` (נקרא במלואו לדוגמה-מבנה), `hawi-interpreter.js`+`goral-conclusion-writer.js` (`wc -l`), `goral-hachol/ui/goral-app.js` (mode-gating).

**כשף:** `goral-hachol/data/sources/kashf-al-asrar/kashf-al-asrar-book.js` (נקרא בחלקו + נספר-שדות בפועל), `kashf-al-asrar-arabic-only.html` (נבדק מבנה+כמות-ערבית), `kashf-al-asrar.html` (נבדק TOC+כותרת+שפה), 12 קבצי `goral-hachol/engine/kashf-*.js` (`wc -l`).

**הפרדה:** `raml-board-generator.js` (חיפוש-מקורות-hawi/kashf), `kashf-pending-extraction.js`+`kashf-reading-engine.js` (חיפוש-imports-חוצי-שיטה).

**כלי-חיפוש:** `find`, `wc -l`, `grep` (רוחב וממוקד), סקריפט-python לספירת-תווים-ערביים בכל קבצי הריפו (לזיהוי-מיקום-הטקסט-הערבי-המלא).

---

## הצהרות

- שום קוד לא נכתב, שום storage/tables/migrations לא נוצרו, שום ספר לא הועלה.
- שום UI, embeddings, AI חי, secret, deploy — לא נוצרו.
- שום HTML/מנוע לא נערך. שום נגיעה ב-`inner-compass`.
- שום commit/push — רק יצירת קובץ המסמך הזה.
- **לא נקבע** אם חאווי או כשף "קודם" — זו לא שאלה שהמסמך הזה עונה עליה.
- הצעד הבא — לפי החלטת אורן משה בלבד.
