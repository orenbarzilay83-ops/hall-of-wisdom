# OREN_SMART_ADVISOR_PANEL_PLACEMENT_DECISION — החלטת-מיקום, פאנל-יועץ-פנימי

> **מסמך תכנון+החלטה בלבד. לא נכתב UI, לא חובר API, לא נוסף secret, לא שונה קוד-קיים, לא בוצע deploy, לא נגע ב-`inner-compass`, לא מוזג ל-main. לא בוצע commit/push — רק יצירת הקובץ.**
> תאריך: 2026-07-08. ממשיך את `OREN_SMART_ADVISOR_SITE_BRAIN_POC_PLAN.md`/`_test_oren_smart_advisor_site_brain_poc.mjs` (commit `7fa574d`) — מגדיר **איפה** הפאנל יופיע ו**איך** הוא נשאר-פנימי-ליועץ-בלבד, לפני שנכתב שורת-UI אחת.

---

## 1. איפה הפאנל יופיע בפיילוט Kashf

**בתוך אזור גורל-החול / Kashf, מיד-אחרי שיש קריאה קיימת** — כלומר: אחרי ש-`buildKashfReading`/`computeCommerceSmartLayer` כבר-רצו והפיקו `reading`/`kashfReading` אמיתיים (המסך `kashf-reading` שכבר-קיים ב-`goral-app.js`, ראה `showScreen("kashf-reading")`). **לא לפני** — הפאנל תלוי-תוצאה, לא-מחליף-שלב-בתהליך.

מיקום-פיזי מוצע (לתכנון-עתידי, לא-מומש): אזור-נוסף **מתחת** ל-`kashfReadingOutput` הקיים (שבו מוצג היום ה-HTML ללקוח, `buildBoardHtml + kashfHtml`) — **לא בתוכו**, כדי-שההפרדה-הוויזואלית תהיה חד-משמעית מהרגע-הראשון: אזור-לקוח למעלה, אזור-יועץ למטה, גבול ברור ביניהם (למשל כותרת-מפרידה "🔒 לוח-יועץ פנימי" בצבע/מסגרת-שונים לגמרי מהצבעים-הקיימים של האתר).

## 2. איך הוא נשאר פנימי ליועץ בלבד

שלוש-שכבות-הפרדה, לא-אחת:
1. **תוכן** — הפאנל מציג רק את 12 המפתחות של `OREN_SMART_ADVISOR_SITE_BRAIN_POC_PLAN.md` §5, ואף אחד מהם **אינו** מוזרם ל-`writeKashfReading`/ה-HTML-הלקוחי הקיים. `clientAnswerDraft` הוא **טיוטה-בלבד** — גם הוא לא-מוצג-ללקוח-אוטומטית, רק ליועץ (ראו §5 למטה — אפילו השדה-הזה-עצמו advisor-only בשלב-הזה).
2. **מבנה-DOM** — כשייבנה-בפועל, הפאנל יהיה ב-container נפרד לחלוטין מ-`kashfReadingOutput`, לא-מקונן-בתוכו — כדי שאין-סיכוי-שסקריפט-עתידי-שמעתיק/משכפל את-תוכן-הלקוח (export/PDF/שיתוף) יגרור-בטעות גם-את-הפאנל.
3. **הרשאה** (לעתיד-רחוק-יותר, כש-API יחובר) — ראו §8: session/UID-check אמיתי, לא-רק-הסתרה-ויזואלית.

**בשלב-הנוכחי (PoC, בלי-UI-בכלל)** — ההפרדה-הזו היא-כרגע-רק-מבנה-הנתונים (`clientAnswerDraft` נפרד מ-`advisorDiagnosis`/`engineCritique`/וכו') ומדיניות-מפורשת-בקוד-הבדיקה (`_test_oren_smart_advisor_site_brain_poc.mjs` כבר-בודק-מכנית ש-`privacyBlockedFields` לא-דולף ל-`clientAnswerDraft`) — לא-עדיין-הפרדת-DOM-אמיתית, כי-אין-DOM.

## 3. איך בעתיד אותו פאנל ישמש גם קלפים וחאווי

**אותו-router, אותה-סכימה, מיקום-אנלוגי** — לא-ארכיטקטורה-שונה-לכל-מודול:
- **חאווי:** אותו-פאנל, אותו-מיקום-יחסי (מתחת-לפלט-הקיים במסך-הקריאה של חאווי), עם `module:"hawi"` — נדרש-קודם `buildAiSynthesisInput`-מקביל ל-Hawi (טרם-נכתב, `reading`/`interpretHawiQuestionInitial` כבר-מספקים-את-רוב-הנתונים-הדרושים).
- **קלפים:** כש-ייבנה-מודול-הקלפים (טרם-קיים, ראו `OREN_SMART_ADVISOR_SITE_BRAIN_POC_PLAN.md` §7) — הפאנל יופיע-באותה-מתכונת מתחת-לדוח-הקלפים, עם `module:"cards"`.
- **תקלות-אתר/ניהול-אתר:** לא-קשור-למסך-קריאה-ספציפי — פאנל **נפרד** (לא-"מתחת-לקריאה", כי-אין-קריאה) שכן-ייתכן שידרוש-מיקום-משלו (כלי-נפרד, לא-בתוך `goral-hachol.html`) — לא-מוכרע-כאן, מעבר-להיקף.

**עקרון-קבוע:** רק-סכימת-הקלט/פלט **הכללית** (12 המפתחות) חוזרת-בכל-מודול; תוכן-`readingData`/`engineOutput` הספציפי משתנה-לפי-מודול (בדיוק כמו-שכבר-מתוכנן ב-`OREN_SMART_ADVISOR_SITE_BRAIN_POC_PLAN.md` §4-5).

## 4. אילו חלקים בפלט יוצגו ליועץ

**כל 12 המפתחות**, מחולקים ל-7 אזורים-ויזואליים (כפי-שאושר):

| אזור | שדה/שדות-JSON |
|---|---|
| 1. אבחון ליועץ | `advisorDiagnosis`, `module`, `confidence` |
| 2. תשובה אפשרית ללקוח | `clientAnswerDraft` — **מוצג-כטיוטה-מסומנת-בבירור**, לא-כפלט-סופי (ראו §5) |
| 3. ביקורת מנוע | `engineCritique` (`hasProblem`/`problems`/`severity`) |
| 4. חוקים/ידע חסרים | `missingKnowledgeOrRules`, `recommendedFix` |
| 5. הוראה לקלוד קוד | `codeInstructionForClaude` (כל 5 תתי-השדות) + כפתור-העתקה (§6) |
| 6. בטיחות ופרטיות | `safetyNotes`, `privacyBlockedFields` |
| 7. פעולה מומלצת | `nextBestAction`, `needsOrenDecision` |

## 5. אילו חלקים לעולם לא יוצגו ללקוח

**כל 12 השדות** — אין-אף-אחד-מהם-שמיועד-ללקוח, **כולל `clientAnswerDraft`**: זו טיוטה-ליועץ-לבחון, לא-פלט-אוטומטי. אם-בעתיד-ירצו-לאמץ-חלק-ממנה-לפלט-ללקוח-בפועל, זה-יהיה **תהליך-נפרד-ומפורש** (כמו-שכבר-נקבע ל-`kashf-commerce-smart-layer.js` — פער-מ-`clientWording` הדטרמיניסטי, עובר-בדיקה, ורק-אז-משולב). באופן-ספציפי, לעולם-לא: `phone` (בשום-ניסוח), תוכן-`dynFields`-רגיש, `sourceText`-גולמי, `clientHistorySummary` גולמי — כל-אלה-כבר-מוגנים-מבנית ע"י `kashf-context-sanitizer.js` (לא-מגיעים-אפילו-ל-input) ונבדקים-שוב ב-output (`privacyBlockedFields` לא-בתוך `clientAnswerDraft`, מאומת-מכנית ב-`_test_oren_smart_advisor_site_brain_poc.mjs`).

## 6. כפתור/פעולה "העתק הוראה לקלוד קוד"

מוצע (תכנון-בלבד, לא-מומש): כפתור בתוך אזור-5 (§4) שמעתיק ל-clipboard **טקסט-מפורמט מוכן** המשלב את `codeInstructionForClaude.instruction` + `filesToInspect` + `filesNotToTouch` + `testsToRun` בפורמט-קריא (למשל Markdown-קצר), כדי-שאורן-משה יוכל-להדביק-אותו-ישירות-בשיחה-עם-Claude-Code בלי-לערוך-ידנית. **לא-מריץ-כלום** — `navigator.clipboard.writeText()` בלבד, שום-קריאת-רשת/API כתוצאה-מלחיצת-הכפתור. מוצג **רק** כש-`codeInstructionForClaude.needed === true` — אחרת-הכפתור-לא-מופיע-כלל (לא-מציע-הוראה-מיותרת).

## 7. מה צריך לבדוק לפני בניית UI אמיתי

1. **החלטה על מיקום-DOM מדויק** — לפני-כתיבת-קוד, לוודא-מול-אורן-משה שהמיקום-המוצע-ב-§1 (מתחת ל-`kashfReadingOutput`) אכן-מתאים, לא-רק-לתכנון-שלי.
2. **בדיקת-Layout** — הפאנל צריך-לא-לשבור-את-ה-layout-הקיים (RTL, Navy/Gold theme) — אך **בלי לשנות** את הצבעים/פונטים/מבנה הקיימים ל-לקוח (איסור-מפורש ב-`CLAUDE.md`) — צריך-סכמה-ויזואלית-נבדלת-משלו.
3. **החלטה: הרחבה-בתוך `goral-hachol.html` או iframe/מודול-נפרד** — משפיע-על-בידוד-אבטחתי (§8) ועל-מורכבות-התחזוקה.
4. **מנגנון-toggle** — האם הפאנל גלוי-תמיד-ליועץ-מחובר, או-דורש-לחיצה-מפורשת ("הצג-ניתוח-פנימי") — ברירת-מחדל-מומלצת: **מכווץ-כברירת-מחדל**, לא-פתוח-אוטומטית, כדי-שלא-יוסח-בטעות-כחלק-מהדוח-ללקוח גם-ויזואלית.
5. **לוודא ש-buildAiSynthesisInput בפועל (טרם-נכתב) לא-משנה שום-פלט-קיים** — אותו-עיקרון-שכבר-הוכח בכל-שלב-קודם (השוואת-HTML-לפני/אחרי).

## 8. מה דורש Auth/UID allowlist לפני חיבור חי

**קריטי, ומבוסס-ממצא-קיים** (`OREN_SMART_ADVISOR_ACCESS_CONTROL_AUDIT.md`, לא-נבדק-מחדש-כאן): כרגע **אין הפרדת-הרשאות ברמת-קוד-האתר** בין אורן-משה לבין כל-חשבון-Supabase-אחר-בפרויקט — login רגיל מבחין רק "מחובר"/"לא-מחובר", לא "אורן"/"כל-מי-שאחר". `myseal.html` נמצא-אז **ללא-שום session-check בכלל** (ממצא-חי, לא-תוקן, מזכיר-בדיוק-את-הסיכון).

**המשמעות לפאנל-הזה:** תוכן-הפאנל (מבנה-מנועים, ביקורת-קוד, `codeInstructionForClaude`) הוא **רגיש-הרבה-יותר** מפלט-קריאה-רגיל — login-בלבד **לא-מספיק**. לפני-כל-חיבור-חי (לא-רק-deploy, גם-בדיקת-מבנה-מול-נתונים-אמיתיים):
1. **Allowlist לפי UID** (המומלץ-כשלב-ראשון לפי הביקורת-הקיימת) — בדיקה **בצד-שרת-בלבד** (Edge Function), משווה את ה-UID-של-המשתמש-המחובר מול-UID-קבוע-של-אורן-משה, מוגדר-כ-secret בסביבת-ה-Function — **לא בקוד-לקוח**.
2. **לא-להסתמך על הסתרת-UI** — כפתור/פאנל מוסתר-ב-HTML הוא **נוחות בלבד**, לא-הגנה (בדיוק-הלקח-מ-`myseal.html`).
3. עד-שזה-ייבנה — **אין-לחבר-שום-חלק-מהפאנל-הזה לשום-API-חי**, גם-לא-לבדיקה, כי-כל-משתמש-מחובר-קיים היה-מקבל-גישה-זהה לתוכן-רגיש-על-מבנה-הקוד.

---

## הצהרות

- שום UI לא נכתב. שום API חי לא חובר. שום secret לא נוסף. שום קוד-קיים לא שונה.
- שום deploy. שום נגיעה ב-`inner-compass`. לא מוזג ל-main.
- שום commit/push — רק יצירת קובץ המסמך הזה.
- הצעד הבא — לפי החלטת אורן משה בלבד.
