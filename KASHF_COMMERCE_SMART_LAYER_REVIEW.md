# KASHF_COMMERCE_SMART_LAYER_REVIEW — ביקורת-אחרי-מימוש, נושא Commerce

> **דוח בלבד. לא נכתב/שונה קוד, לא שונה HTML/מנוע, לא בוצע deploy/merge, לא חובר AI חי, לא נוסף secret, לא נוצר UI, לא נגע ב-`inner-compass`. לא בוצע commit/push — רק יצירת הקובץ.**
> תאריך: 2026-07-08. ביקורת על commit `1350e38` — כאילו נבחן על-ידי יועץ-חיצוני, לא על-ידי הכותב. **המסקנה המרכזית מגיעה מבדיקה בפועל על-לוחות-אמיתיים, לא מקריאת-קוד בלבד — ונמצא בה באג-קוהרנטיות אמיתי (סעיף 4).**

---

## 1. מה השתנה בפועל

`goral-hachol/engine/kashf-commerce-smart-layer.js` (חדש) מחשב `signWeighting`/`certaintyLevel`/`strongestSignals`/`weakSignals`/`contradictions`/`advisorDiagnosis`/`clientWording`/`practicalGuidance` מתוך ה-`reading` שכבר-מחושב. `kashf-reading-engine.js` מחבר אותו, גייטד ל-`commerce` בלבד. `kashf-narrative-writer.js::writeConclusionPara` מחליף את משתנה `guidance` (רק אותו) בטקסט-החדש כשהוא זמין. 5 בדיקות + רגרסיית-29-נושאים עברו.

## 2. מה נשאר Fallback

- **28 מתוך 29 נושאי-כשף** — עדיין 100% על `TOPIC_GUIDANCE` הקבוע. לא נגעו.
- **`writeShortVerdictBox`** (תיבת "תשובה לשאלה" למעלה) — **לא עודכנה בכלל**. שדה `detail` שם עדיין מציג את `primaryFormula.verdict.text` הישן (למשל "רוב מיטיבים") — לא את הניסוח-החדש. חוסר-עקביות בין תיבת-התשובה-הקצרה (עדיין-ישנה) לפסקת-הקריאה-ללקוח (חדשה) — **פער שנוצר על-ידי המימוש הזה עצמו**, לא תוקן.
- **`advisorDiagnosis`/`strongestSignals`/`weakSignals`** — מחושבים ונשמרים על `reading.commerceSmartLayer`, אך **לא מוצגים בשום מקום ב-HTML היום** — נתונים-רדומים-לחלוטין. לא דולפים (בטוח), אבל גם לא-בשימוש (לא-ממומש-במלואו).
- אם `computeCommerceSmartLayer` נכשלת/מחזירה-`null` — `guidance` נופל-חזרה ל-`TOPIC_GUIDANCE.commerce` **בלבד**; `judgeNote`/`altNote`/`keyFact` (שכבר-היו-קיימים לפני המימוש) ממשיכים-לפעול תמיד, גם-בלי-קשר-לשכבה-החדשה.

## 3. שלוש דוגמאות פלט commerce (לוחות אמיתיים, לא מדומיינים)

**מצב מיטיב** (mothers `1221,2211,2212,1112`, `overallPositive=true`, `certaintyLevel=high`, בלי סתירה):
> "בית המבקש בעסקה מראה סוהר — מזיקה. בית אחרית העסקה מראה ממון נכנס — מיטיבה. **זה זמן טוב להתקדם, בזהירות סבירה.**"

**מצב מזיק** (mothers `1112,2122,1121,2211`, `overallPositive=false`, `certaintyLevel=high`):
> "בית המבקש בעסקה מראה סף יוצא — מזיקה. בית אחרית העסקה מראה ממון יוצא — מזיקה. **עדיף להמתין או לבחון תנאים אחרים לפני שממשיכים.**"

**מצב ממוזג/לא-ודאי** (mothers `1211,1112,1121,1122`, `certaintyLevel=low`):
> "בית המבקש בעסקה מראה בר הלחי — **ממוזגת-מיטיבה**. בית אחרית העסקה מראה ממון יוצא — מזיקה. **יש כאן אי-ודאות של ממש — לא כדאי להסתמך על תשובה אחת בלבד.** כדאי לבדוק שוב בזמן אחר, או להיעזר בשיקול-דעת נוסף לפני החלטה."

## 4. האם הפלט נשמע כמו יועץ, או עדיין רובוטי — **נמצא באג-קוהרנטיות אמיתי**

**חלקית-משופר, אך נמצאה בעיה ממשית שדורשת תיקון לפני כל הרחבה נוספת.** בזמן חיפוש-אחר-דוגמה-רביעית (מצב עם `contradictions.length > 0`), הופק הפלט המלא הבא **בפועל, לא-מדומיין**:

> "שרה — בית המבקש בעסקה מראה כבוד יוצא — מיטיבה. בית אחרית העסקה מראה סוהר — מזיקה. **זה זמן טוב להתקדם, בזהירות סבירה.** אולם הדיין — הצורה סוהר — מזהיר מפני קשיים אפשריים בהמשך. יש לציין: בדיקת אימות נוספת על הלוח נתנה כיוון הפוך — כדאי להתייחס לתשובה זו בזהירות ולא כוודאות מוחלטת."

זה **פסקה אחת עם שלושה קולות סותרים**: (1) `clientWording` אומר "בית אחרית העסקה... מזיקה", (2) מיד-אחריו `practicalGuidance` אומר "זמן טוב להתקדם", (3) `judgeNote` הישן (עדיין-רץ, לא-נגעתי-בו) אומר "מזהיר מפני קשיים", (4) `altNote` הישן (גם-הוא) אומר "כיוון הפוך — זהירות". **זה בדיוק סוג-הבעיה שכל הסבב-הזה נועד-למנוע** (PR #18/#19, "יחד עם זאת").

**סיבת-השורש, מזוהה במדויק:** `practicalGuidance` בקובץ-החדש מחושב **רק** מ-`overallPositive`+`certaintyLevel` — **הוא לא-בודק את `contradictions`**, למרות ש-`contradictions` **כבר מחושב באותה שכבה עצמה**, שתי שורות-קוד מעליו. זה באג-ישיר-בקוד-שכתבתי-היום, לא בעיה-עקרונית-בגישה.

## 5. האם יש מילים שצריך לרכך ללקוח

לא נמצאו ניסוחים-קשים-מדי (אין "אתה תפסיד", אין ודאות-שקרית מפורשת חוץ מהבאג בסעיף 4). "מזיקה"/"מיטיבה" הם מונחי-המקור הרגילים (כבר-בשימוש בכל שאר-כשף) — לא חדשים ולא-דורשים-ריכוך-נוסף.

## 6. האם יש מידע שצריך להישאר רק ליועץ

**כן — וזה כבר-כך היום, אך רק במקרה, לא בעיצוב-מכוון:** `advisorDiagnosis` (כולל `strongestSignals`/`weakSignals`/`supportRatio` המדויק) **לא מוצג בשום מקום** — לא בפאנל-הלקוח ולא בפאנל-"קרא עוד". זה נכון-לעכשיו כי הוא-פשוט-לא-מרונדר, לא כי יש-הפרדה-מתוכננת. כשיבנה בעתיד ממשק-שמציג את `advisorDiagnosis`, יש לוודא שהוא-מוצג **רק** בפאנל-היועץ (מאחורי "קרא עוד"), לא בתיבת-הלקוח — זו בדיוק ההבחנה ש-`OREN_CONCLUSION_STYLE_SPEC.md` דורש.

## 7. האם צריך לשפר את clientWording

**כן, בשני כיוונים:**
1. **תיקון-הבאג בסעיף 4** — `practicalGuidance` (ובעצם גם `clientWording`) חייבים-לבדוק `contradictions.length > 0` ולהוסיף-ניסוח-מיישב, **או** להימנע-מהמלצה-חד-משמעית כש-יש-סתירה, **לפני** שהטקסט-הזה יוצג-ללקוח-אמיתי.
2. **גיוון-תבניתי** — הניסוח-הנוכחי הוא תבנית-קבועה ("X מראה Y — Z. בית... מראה... — Z2.[+משפט-ודאות]") — עדיף-מהמשפט-הקבוע-הישן (כי הוא-שם-צורות-ספציפיות), אך עדיין **תבנית אחת נוקשה**, לא-פרוזה-חופשית. שיפור-עתידי: לגוון את חיבורי-המשפטים (כמו `connectClauses` בחאווי) במקום נוסחה-קבועה-אחת.

## 8. האם הארכיטקטורה מתאימה להרחבה לנושאים אחרים

**העקרונות — כן.** `signWeighting` מבוסס-ספר, `certaintyLevel` משוקלל, הפרדת `strongestSignals`/`weakSignals`, ו-`advisorDiagnosis` כאובייקט-נפרד — כל אלה תבנית טובה שניתן-להעביר-לנושאים-אחרים (לכל נושא-`count-quality` יהיה `COMMERCE_HOUSE_WEIGHT`-מקביל-משלו, מבוסס-`sourceText` של הנושא-ההוא).

**אך לא לפני שהבאג בסעיף 4 מתוקן — אחרת הוא ישוכפל.** אם מעתיקים את אותה תבנית ל-`hiddenTreasure`/`lostAnimal`/`religion` (שלושת-הנושאים-האחרים-מסוג-`count-quality`) בלי-לתקן קודם את חוסר-הבדיקה-של-`contradictions` ב-`practicalGuidance`, כל אחד מהם עלול-להפיק את-אותה-פסקה-סותרת-את-עצמה.

## 9. מה הסיכון אם מרחיבים מוקדם מדי

- **שכפול-הבאג** (סעיף 4) על-פני נושאים-נוספים — הכי-דחוף.
- **הצטברות קוד-רדום** — אם `advisorDiagnosis` ממשיך-להיבנות-בלי-להיות-מוצג באף-נושא, נערמת תשתית-לא-מנוצלת שקשה-לדעת-אם-היא-נכונה (אין-מי-שבודק-אותה חוץ מבדיקות-אוטומטיות).
- **חוסר-עקביות מצטבר** בין `writeShortVerdictBox` (ישן) ל-`writeConclusionPara` (חדש) — ככל-שיותר-נושאים-מקבלים-ניסוח-חדש-בפסקה-אחת-בלי-לעדכן-את-התיבה-השנייה, הפער גדל.
- **עלות-תיקון-עתידית** — באג שמשוכפל ל-4 נושאים יקר-יותר-לתקן מבאג-בנושא-אחד.

## 10. המלצה

**לשפר קודם את commerce — לא לבחור נושא שני, ולא להתחיל לתכנן תיבת-שיחה.**

הנימוק: נמצא באג-קוהרנטיות אמיתי (סעיף 4) שמייצר פלט **גרוע-יותר** מהמשפט-הקבוע-הישן במקרים-מסוימים (שלושה-קולות-סותרים בפסקה-אחת, לעומת המשפט-הישן שלפחות-לא-סתר-את-עצמו כי היה-קצר-וגנרי). זה **לא** "עוד-ליטוש" — זו רגרסיית-איכות אמיתית שצריכה-תיקון **לפני** כל צעד-הבא, כולל לפני מדידה-סופית של "האם הארכיטקטורה מוצלחת" (סעיף 8 לא-ניתן-להערכה-מלאה עד שהבאג הזה מתוקן).

**סדר מוצע (לא לבצע עכשיו — רק לרישום):** (1) לתקן את `practicalGuidance`/`clientWording` כך שיבדקו `contradictions` ויתאמו-לניסוח-הקיים-של-`judgeNote`/`altNote` במקום-לסתור-אותו; (2) לשקול-לאחד את `writeShortVerdictBox`'s `detail` עם הניסוח-החדש; (3) רק-אז — נושא שני או תיבת-שיחה.

---

## הצהרות

- שום קוד לא נכתב/שונה. שום HTML/מנוע לא נערך. שום deploy/merge.
- שום AI חי, secret, UI. שום נגיעה ב-`inner-compass`.
- שום commit/push — רק יצירת קובץ המסמך הזה.
- הצעד הבא — לפי החלטת אורן משה בלבד.
