# AGENTS.md — הוראות מחייבות לכל סוכן-קוד בריפו הזה

> Binding instructions for every coding agent working in this repository.
> המסמך המלא והמחייב לכללי-הפרויקט הוא `CLAUDE.md` — קרא אותו במלואו לפני כל עבודה. המסמך הזה מתמצת את שערי-האישור.
> מסמכים נלווים: `OREN_APP_STRUCTURE.md` (מבנה), `OREN_CONCLUSION_STYLE_SPEC.md` (סגנון מסקנות), `OREN_AI_SUPERVISOR_SPEC.md` (תהליך אישור).

## כללים שאין לעבור עליהם

1. **אין לעצב מחדש UI אלא אם התבקש במפורש.** ערכת Navy/Gold, פריסת RTL עברית — נשארות.
2. **אין לשנות לוגיקת-חישוב של גורל החול (חאווי / כשף / רמל) בלי אישור אורן משה.**
3. **סדר הלוח מימין לשמאל נשמר תמיד** — בית 1 בצד ימין בתצוגה.
4. **ידע רוחני/אוקולטי ממוקור נשמר כמו שהוא.** אין להסיר ואין לרכך מונחים רגישים (כישוף, עין הרע, קנאה, חסימה רוחנית, מס, ג'ין, שדים, רוחות, קללות) כשהם חלק מידע-המקור המאושר. עידון ניסוח מותר אך ורק בשכבת הטקסט-ללקוח — לא באבחון-היועץ ולא בשכבת הנתונים.
5. **אבחון-יועץ ונוסח-ללקוח נשארים שכבות נפרדות.** אין לערבב ביניהן (ראו OREN_CONCLUSION_STYLE_SPEC.md).
6. **לפני שינוי התנהגות — סכם את השינוי המוצע והמתן לאישור.** במבנה: תקציר בעיה, קבצים מושפעים, סיבה, רמת סיכון, תוצאה צפויה.
7. **אם שינוי מבוקש עלול לגעת במנועים, בארכיון הלקוחות, בדוחות, במסקנות או בפריסה (layout) — עצור ושאל קודם.**
8. **שינויים קטנים ומבודדים עדיפים על שכתובים רחבים.**
9. **אין להחליף לוגיקה עובדת במערכת חדשה בלי אישור מפורש.**
10. **אין להמציא נתונים פרשניים.** כל טענה — עקיבות למקור (ספר/עמוד). פער-מקור נשאר ריק עם `sourceStatus` מתאים (ראו CLAUDE.md).
11. **אין ציטוטי-עמוד, "מקור:", או טקסט ערבי בפלט המוצג ללקוח.** המקור נשאר בקוד.
12. **אין טכנולוגיות חדשות:** בלי React/Vue/TypeScript/webpack/build. JS טהור, ES modules, דפדפן בלבד.
13. **אין לגעת ב-`goral-hachol/engine/kashf-figure-appearance.js`.**

## עבודה עם git

- **ענף יחיד:** `claude/app-cleanup-organization-mia9b2`. אין ליצור ענפים חדשים. אין worktree.
- קומיט + פוש אחרי כל שינוי לוגי. `git fetch` לפני כל קומיט (בדיקת drift).
- **אין למזג ל-`main` (= האתר החי) בלי הוראה מפורשת של אורן משה.**

## חובת אימות לפני כל קומיט

- `node --check` על כל קובץ JS שנערך.
- סריקת-שיבוש: אין ערבית/קירילית מחוץ להערות-קוד.
- בדיקת דפדפן אמיתית: `python3 -m http.server` + Playwright (chromium: `/opt/pw-browsers/chromium`) על `goral-hachol.html` — כולל קריאה ידנית של הפלט.
- עדכון `WORKPLAN.md` בסיום משימה.
