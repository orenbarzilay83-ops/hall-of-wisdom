# OREN_SMART_ADVISOR_BRAIN_PLAN — תכנון "המוח החכם" הפנימי של אורן משה

> **מסמך תכנון בלבד. לא נכתב קוד, לא שונה HTML, לא שונה מנוע, לא נכתב prompt אמיתי לחאווי/כשף, לא נוצר sync script/`_shared/`, לא בוצע deploy, לא נוסף secret, לא חובר AI חי, לא נגע ב-`inner-compass`. לא בוצע commit/push — רק יצירת הקובץ.**
> תאריך: 2026-07-08. עצמאי ונפרד מ-`AI_RUNTIME_GORAL_HACHOL_FIRST_PLAN.md` (ראו הבחנה בסעיף 1).

---

## 1. ההבדל בין Runtime AI לבין Oren Smart Advisor Brain

| | Runtime AI (`AI_RUNTIME_GORAL_HACHOL_FIRST_PLAN.md`) | Oren Smart Advisor Brain (מסמך זה) |
|---|---|---|
| **למי** | ללקוחות — משתמש בפרשנות/ניסוח קריאת גורל החול/כשף/קלפים | לאורן משה בלבד — משתמש-פנימי יחיד |
| **תפקיד** | מנסח-מחדש פלט-מנוע קיים לנרטיב עברי חם | עוזר-פיתוח: הכוונה, הבנת-מנועים, הבנת-ספרים, אזהרת-סיכונים |
| **קלט** | פלט-מנוע דטרמיניסטי סגור לקריאה בודדת (שאלה+לוח+פסיקה) | כל מבנה-הפרויקט: קוד, תיעוד, WORKPLAN, מקורות-ידע, היסטוריית-שינויים |
| **מותר לו לחשב** | כלום — רק ניסוח | כלום שנוגע ללוגיקת-חישוב של המנועים — רק הסבר/איתור/סיכום |
| **חשיפה** | פוטנציאלית ללקוח-קצה (בעתיד, מאחורי שערי-אישור) | לעולם לא ללקוח — כלי-פיתוח פנימי, מוגן login, אורן בלבד |
| **סיכון עיקרי** | ניסוח שגוי/לא-נאמן-למקור בטקסט-ללקוח | הכוונת-פיתוח שגויה שגורמת לשבירת מנוע-עובד או להמצאת-תוכן בטעות |

שני הפרויקטים חולקים **רכיבי-תשתית** (ראו סעיף 6) — לדוגמה, `ai/provider/anthropic-provider.js` הכללי שכבר נבנה יכול לשמש את שניהם ללא שינוי (הוא לא ייעודי-ללקוח) — אבל הם **שני מוצרים נפרדים לגמרי** עם prompt-ים נפרדים, הרשאות נפרדות, ומשטר-אישורים נפרד. אין לערבב ביניהם.

## 2. מטרת הבינה

עוזר-פרטי-פנימי לאורן משה בלבד — לא ללקוחות, לא לשימוש-ציבורי, לא חשוף בשום מסך שלקוח יכול להגיע אליו. מטרתו: לעזור לאורן להתמצא בפרויקט הגדל-והולך (עשרות קבצי-מנוע, אלפי שורות-קוד, עשרות מסמכי-מקור, WORKPLAN בן מאות שורות) — לענות "איפה מוגדר X", "מה כבר בנוי ומה רק תוכנן", "האם יש מקור לטענה הזו", "מה הסיכון בשינוי הזה" — בלי לגעת בעצמו בקוד או להחליט במקום אורן.

## 3. מה היא צריכה לדעת

מבנה-ידע שהבינה קוראת (לא כותבת) בזמן-הרצה, לפי הריפו כפי שהוא היום:

- **מבנה הפרויקט** — `OREN_APP_STRUCTURE.md` (כבר קיים, נקודת-פתיחה מוצהרת).
- **כל המודולים הקיימים** — נקודות-כניסה (`index.html`, `calculator.html`, `goral-hachol.html`, `myseal.html`, `ramal-shastra.html`, `kashf-al-asrar.html`) ומה קיים-בפועל מול מה שלא (כולל ההבחנה שכבר תועדה: מודול-קלפים **אינו** קיים בקוד hall-of-wisdom, הוא ב-`inner-compass`).
- **המנועים הקיימים** — כל `goral-hachol/engine/*.js` (רשימה מלאה: `goral-conclusion-writer.js`, `hawi-interpreter.js`, `kashf-reading-engine.js`, `kashf-narrative-writer.js`, `kashf-topic-rules.js`, `kashf-dhamir.js`, `raml-board-generator.js`, `narrative-fact-phrasing.js`, `goral-spiritual-diagnostics-engine.js`, ועוד) — מה כל קובץ עושה, מה זורם לאן (ראו "Data Flow" ב-`CLAUDE.md`).
- **WORKPLAN.md** — היסטוריית-שינויים מלאה, מה בוצע, מה פתוח, מה נדחה ולמה.
- **AGENTS.md / מסמכי-מפקח** — `OREN_AI_SUPERVISOR_SPEC.md`, `AGENTS.md` עצמו — כללי-הברזל שחלים על כל שינוי.
- **`OREN_CONCLUSION_STYLE_SPEC.md`** — כללי-הסגנון של מסקנות (רלוונטי גם ל-Runtime AI, לא רק לבינה הפנימית — הבינה יכולה להסביר אותם לאורן, לא לאכוף אותם בעצמה).
- **גורל החול (חאווי)** — מבנה-נתונים, שדות, קבצי-מקור (`data/sources/`), מצב-אימות (`sourceStatus`) לכל נתון.
- **כשף (אל-אסראר)** — אותו דבר, כולל ההבחנה החשובה "כשף ≠ חאוי" (עמודי-מקור שונים לגמרי, לא להתבלבל).
- **גורל העשיריות** — מודול קיים ב-`calculator.html`, פחות מתועד מגורל-החול; הבינה יכולה לסמן זאת כפער-תיעוד.
- **ארכיון לקוחות** — `goral-client-archive.js` — מבנה, לא תוכן-לקוחות עצמו (ראו סעיף 10 — הרשאות/פרטיות).
- **דוחות** — כל מסמכי ה-`OREN_*`/`MERGE_*`/`AI_RUNTIME_*` שכבר נכתבו בריפו — היסטוריית-החלטות.
- **מה כבר בנוי מול מה רק תוכנן** — הבחנה מפורשת וקריטית (בדיוק כמו `AI_RUNTIME_GORAL_HACHOL_FIRST_PLAN.md` מול הקוד שכבר קיים) — הבינה חייבת להיות מסוגלת להגיד "זה תוכנן במסמך X, לא מומש בקוד" ולא לטשטש בין תיעוד-כוונה לקוד-פעיל.

## 4. מדיניות מקורות

עקרון-הברזל של `CLAUDE.md` ("No Invented Data") חל על הבינה הפנימית באותה חומרה כמו על כל שורת-קוד באפליקציה — **אולי אף ביתר-שאת**, כי אורן עלול לסמוך על תשובתה בהחלטות-פיתוח.

**חשוב: גורל החול אינו שיטה אחת.** הוא כולל **שתי שיטות נפרדות ובלתי-מעורבבות**, כל אחת עם מסלול-מקורות משלה — חאווי וכשף אל-אסראר. אין לטשטש ביניהן, ואין להשתמש במקור של שיטה אחת כדי "להשלים חוסר" בשיטה השנייה.

### רשימת מקורות מאושרים לפי שיטה

**גורל החול / חאווי:**
- להשתמש רק במקורות-חאווי שאורן משה אישר במפורש לשיטת חאווי.
- אין להשתמש במקורות-כשף כדי להשלים חאווי, אלא אם אורן משה מאשר זאת במפורש, מקרה-לגופו.

**גורל החול / כשף אל-אסראר:**
המקורות המאושרים הם:
1. המקור הערבי — كشف الأسرار المصونة في إخراج الضمائر المخزونة (המובנה כבר באפליקציה, `kashf-al-asrar-book.js`).
2. התרגום העברי שאורן משה הכין — **חשיפת הסודות הנצורים**.

שני המקורות עובדים יחד: הערבית כמקור-יסוד, והתרגום העברי של אורן משה כמקור-עבודה-והבנה. אורן משה משתמש כרגע בשניהם גם יחד.

**כל מקור אחר** (כולל גורל-העשיריות, כל ספר עתידי, כל מקור-Drive שטרם נדון) — **לא מאושר כרגע** להכנסה לבינה החכמה בלי אישור מפורש וחדש מאורן משה, לכל מקור בנפרד.

### כללי-חובה

- **אם הבינה נשאלת על שיטה מסוימת ואין לה מקור מאושר לאותה שיטה** — עליה לענות: **"לא נמצא במקורות המאושרים לשיטה הזאת."** לא ניחוש, לא "לפי הידע הכללי שלי", לא פרפרזה-שנשמעת-סבירה, ולא שאילת-מקור-של-שיטה-אחרת בלי אישור.
- **אין לערבב בין חאווי לבין כשף אל-אסראר.** כל שיטה נשארת במסלול-המקורות שלה — בדיוק כמו שכבר נאכף בקוד עצמו (`CLAUDE.md`: "כשף ≠ חאוי", "עמודים בקוד המוזכרים 'חאוי עמ' X' שונים לגמרי מ'כשף עמ' X'").
- הבחנה בין "מקור-מאושר-לשיטה-הזו" ל"הכשרה כללית של המודל" חייבת להיות מוחלטת ומפורשת בכל תשובה — בדיוק כמו שדה `sourceStatus` שכבר קיים בכל רשומת-נתונים באפליקציה.

## 5. הכנסת ספרים

חוברת-כללים לפני שספר חדש נכנס למאגר-הידע של הבינה:

- **לא להעלות ספרים מלאים או OCR מלא לריפו הציבורי (`hall-of-wisdom` ב-GitHub).** תוכן-ספרים שייך למאגר-ידע פרטי נפרד (ראו סעיף 6) — לא ל-git history שגם כך כבר תועד כמקום-שדליפות-סוד קורות בו (`OREN_EXISTING_AI_API_AUDIT.md`).
- **אחסון פרטי / מאגר-ידע פרטי** — לא תיקיית-git ציבורית. Supabase Storage (private bucket) הוא המועמד הטבעי, כי הפרויקט כבר משתמש ב-Supabase (`supabase-client.js` הקיים לאימות-משתמשים).
- **חלוקה לפי ספר/עמוד/נושא** — כמו המבנה הקיים כבר ל-`kashf-al-asrar-book.js` (`KASHF_AL_ASRAR_PAGES`, מערך של רשומות `{page, chapter, chapterHebrew, arabicText, hebrewTranslation, topics, sourceStatus}`) — לא בלוק-טקסט-אחד-ענק, אלא יחידות-ניתנות-לציטוט ולסינון.
- **מטא-דאטה חובה לכל יחידה:** שם-ספר, עמוד, נושא, שפה (ערבית/עברית/מעורב), איכות-OCR (למשל "סרוק-נקי"/"סרוק-מטושטש"/"תמלול-ידני-מאומת") — כדי שהבינה תדע להזהיר "המקור כאן באיכות-OCR נמוכה, כדאי לאמת ידנית" במקום להציג ודאות-שווא.
- **שמירת מונחים בערבית** — לא לתרגם-מראש-ולאבד את המקור; כל יחידה שומרת גם `arabicText` וגם `hebrewTranslation` (בדיוק כמו המבנה הקיים).
- **שמירת חומר רוחני רגיש** — כישוף, עין הרע, קנאה, חסימה רוחנית, מס, ג'ין, שדים, רוחות, קללות — **לא נמחק ולא מרוכך** במאגר-הידע (זו שכבת-מקור, לא שכבת-ללקוח — ראו `AGENTS.md` כלל 4).
- **חיוב הפניות-למקור בתשובות** — כל תשובה של הבינה שמתבססת על ספר חייבת לצטט ספר+עמוד (לאורן, לא ללקוח — זו זכות-יתר של כלי-פיתוח פנימי, בניגוד מוחלט לכלל "אין ציטוטים ללקוח" שחל על Runtime AI).

## 6. ארכיטקטורה מוצעת

- **עוזר פנימי מוגן ב-login** — משתמש באותה מערכת-האימות הקיימת כבר (`index.html`/`supabase.auth`) — לא מסך חדש-לגמרי, מסך נוסף מאחורי אותו login שכבר קיים לאורן.
- **Supabase Edge Function לקריאות-AI בצד-שרת** — **פונקציה נפרדת** מ-`supabase/functions/oren-smart-ai` (שמיועדת ל-Runtime AI/ללקוח) — למשל `supabase/functions/oren-advisor-brain/` — כדי לא לערבב הרשאות/prompt-ים/סיכונים בין שני המוצרים (עקרון-ההפרדה מסעיף 1). יכולה לעשות שימוש-חוזר באותה שכבת-adapter הכללית (`ai/provider/anthropic-provider.js`) — היא כבר כללית ולא ייעודית-ללקוח.
- **מאגר-ידע פרטי** — Supabase (Storage לקבצי-מקור + טבלת-DB למטא-דאטה/יחידות-ידע), לא בריפו הציבורי.
- **אפשרות עתידית ל-vector search / embeddings** — לשלב מאוחר יותר, כשמאגר-הידע גדול מספיק שחיפוש-מילות-מפתח-פשוט כבר לא מספיק; **לא בשלב הראשון**, לא נדרש כדי שהבינה תהיה שימושית (סעיף 7 מראה שהשלב הראשון עובד בלי embeddings בכלל).
- **שימוש במסמכי-הפרויקט כשאין צורך בספרים** — הרבה שאלות ("מה בנוי", "מה תוכנן", "איפה X מוגדר") נענות ישירות ממבנה-הריפו/WORKPLAN בלי לגעת במאגר-הספרים בכלל — נתיב זול-וזמין-מיד.

## 7. שלב ראשון — בלי AI חי: "מפת מוח" בלבד

השלב הראשון בפועל (כשיאושר) הוא **סטטי לגמרי, בלי שום קריאת-AI** — קובץ/מסמך-אינדקס (JSON או Markdown מובנה) שממפה:

- **רשימת מסמכים** — כל קבצי ה-`.md` בריפו (`OREN_*`, `AGENTS.md`, `WORKPLAN.md`, `CLAUDE.md`) עם תיאור-שורה-אחת לכל אחד.
- **רשימת מודולים** — כל נקודת-כניסה (HTML) + סטטוס (פעיל/ניסיוני/לא-קיים-רק-מתוכנן).
- **רשימת מקורות מאושרים, לפי שיטה** — לפי סעיף 4: לחאווי — מקורות-חאווי המאושרים בלבד; לכשף אל-אסראר — המקור הערבי + התרגום העברי של אורן משה; לכל שיטה/מודול אחר — אין עדיין מקור מאושר.
- **רשימת ידע חסר** — סעיפים שכבר מתועדים כ-`not-yet-found-in-current-code-search` או כפערים פתוחים ב-WORKPLAN — כדי שהבינה (ואורן) ידעו איפה **לא** לצפות לתשובה.
- **כללי-סיכון לפני שינוי-קוד** — תמצית של `AGENTS.md`/`OREN_AI_SUPERVISOR_SPEC.md` בפורמט-שאילתה-מהירה ("אם השינוי נוגע ב-X → עצור ושאל").

זהו שלב-תיעוד/אינדקס בלבד — לא AI, לא Edge Function, לא secret. ניתן לבנות ולבדוק אותו בלי לגעת בשום דבר מהמנועים.

## 8. שלב שני — ממשק פנימי "Ask Oren Smart Advisor"

רק **אחרי אישור נפרד**: מסך-שאלות-פשוט (מוגן-login) שמזין את "מפת-המוח" (שלב 7) כקונטקסט לקריאת-AI, עונה לאורן על שאלות-ניווט/הבנה על הפרויקט. עדיין בלי גישה למאגר-ספרים-מלא (זה שלב 9) — רק על מבנה-הפרויקט עצמו.

## 9. שלב שלישי — שליפת-ידע אמיתית מתוך ספרים מאושרים

רק **אחרי אישור נפרד**: חיבור מאגר-הידע הפרטי (סעיף 6) לשלב 8, כך שהבינה יכולה גם לענות שאלות-תוכן ("מה כתוב על X בעמוד Y של הספר") — לא רק שאלות-ניווט-בפרויקט. זהו השלב שדורש בפועל את מדיניות-המקורות המלאה (סעיף 4) ואת מבנה-הכנסת-הספרים (סעיף 5).

## 10. שערי-אישור

אורן משה חייב לאשר במפורש, בנפרד, לפני כל אחד מהצעדים הבאים — אף אחד מהם לא בוצע ולא יבוצע כתוצאה-לוואי מהמסמך הזה:

- הכנסת ספרים (כל ספר, כל עדכון-מאגר).
- הוספת storage (Supabase Storage/DB חדשים למאגר-הידע).
- embeddings / vector search.
- קריאות-AI חיות (כל קריאה ראשונה, לא רק deploy).
- UI חדש (מסך/כפתור "Ask Oren Smart Advisor").
- deploy (של כל Edge Function הקשורה לבינה הזו).
- secret (כל מפתח-API חדש).
- שינוי מנועים (לעולם לא כתוצאה מהמלצת-הבינה בלי בדיקה ואישור אנושיים).
- שינוי-התנהגות ללקוח (הבינה הזו לא אמורה לגעת בשכבת-הלקוח בכלל — אם אי-פעם תעלה הצעה כזו, זה כשל-בעיצוב שדורש עצירה).

---

## סדר-בנייה מומלץ

1. **שלב 7 בלבד קודם** — "מפת-המוח" הסטטית (מסמך/JSON-אינדקס). אין בו AI, אין Edge Function, אין secret — אפשר לבנות ולבדוק את השלמות-והדיוק שלו לבד, בלי תלות בשום החלטת-ארכיטקטורה נוספת. זה גם הבסיס שכל שלב הבא נשען עליו.
2. **רק אז שלב 8** (ממשק-שאלות, בלי מאגר-ספרים) — כי הוא הכי-קטן-שימושי (עוזר לאורן בניווט-הפרויקט מיד) ובעל-הסיכון-הנמוך-ביותר מבין שני השלבים שדורשים AI חי (אין בו עדיין מאגר-ידע-פרטי-חדש להקים).
3. **לבסוף שלב 9** (מאגר-ספרים + שליפת-תוכן) — הכי-שימושי לטווח-ארוך, אבל גם בעל-ההיקף-הגדול-ביותר (Storage, מטא-דאטה, מדיניות-מקורות מלאה) — הגיוני לבנות אחרון, אחרי ששלבים 7-8 כבר הוכיחו שהמבנה הבסיסי עובד ואמין.

**המלצה מפורשת: להתחיל (כשיאושר) בשלב 7 בלבד** — הוא היחיד מבין השלושה שאינו דורש אף שער-אישור מסעיף 10 (אין בו AI/storage/secret/UI/deploy) ונותן ערך מיידי כתשתית לכל מה שאחריו.

---

## הצהרות

- שום קוד לא נכתב, שום HTML לא שונה, שום מנוע לא נגע.
- שום prompt אמיתי (לא ל-Runtime AI, לא לבינה הזו) לא נכתב.
- שום sync script / `_shared/` / deploy / secret / AI חי — לא בוצעו.
- שום נגיעה ב-`inner-compass`.
- שום commit/push — רק יצירת קובץ המסמך הזה.
- הצעד הבא — לפי החלטת אורן משה בלבד.
