# KASHF_SAFE_CONTEXT_USAGE_FOR_COMMERCE_PLAN — תוכנית תכנון בלבד

> **מסמך תכנון בלבד. לא נכתב/שונה קוד, לא שונה `narrative-writer`/`kashf-commerce-smart-layer.js`/`goral-app.js`/מנועים/HTML, לא נוסף age, לא חובר AI חי, לא נוסף secret, לא בוצע deploy, לא נוצר storage/database/embeddings, לא נגע ב-`inner-compass`. לא בוצע commit/push — רק יצירת הקובץ.**
> תאריך: 2026-07-08. ממשיך את השלבים המאושרים (א׳: שמירת-כשף לארכיון, `60d751d`; ג-חלקי: העברת context, `06bbd74`) — ומתכנן איך להשתמש ב-context הזה **בבטחה**, בנושא `commerce` בלבד, לפני כל הרחבה.

---

## 1. אילו שדות-context קיימים היום בקריאת-כשף בפועל

מאומת מול `reading.clientContext` כפי שהוא נבנה היום ב-`kashf-reading-engine.js` (אחרי שלב ג-חלקי):

| שדה | קיים? | מקור |
|---|---|---|
| `name` | ✅ | תמיד היה |
| `question` | ✅ | תמיד היה |
| `gender` | ✅ | תמיד היה |
| `age` | ⚠ **תמיד `''`** | שלב-age הוחזר במלואו (לא-בוצע) — עדיין phantom-field |
| `maritalStatus` | ✅ | שלב ג-חלקי |
| `workStatus` | ✅ | שלב ג-חלקי |
| `hasChildren` | ✅ | שלב ג-חלקי |
| `parentName` | ✅ | שלב ג-חלקי |
| `quesitedName` | ✅ | שלב ג-חלקי |
| `phone` | ✅ | שלב ג-חלקי |
| `dynFields` (אובייקט מקונן) | ✅ | שלב ג-חלקי |
| `clientHistorySummary` | ❌ **לא קיים ב-Kashf reading בכלל** | מחושב ב-`goral-app.js` אך לא-מועבר ל-Kashf (ולפי הביקורת הקודמת — גם לא-רלוונטי, כי קריאות-כשף שנשמרות לארכיון מ-שלב-א׳ לא נזרקות-חזרה לתוך `clientHistorySummary` באופן אוטומטי; זה עדיין שלב-נפרד-לא-מתוכנן) |

**חשוב:** כל השדות האלה כבר נכנסים ל-`reading.clientContext` — **אבל `kashf-narrative-writer.js` עדיין קורא רק `name`/`question`/`gender`** (ו-`age`, שריק-תמיד). שום שדה-חדש עדיין לא משפיע על שום פלט. זו בדיוק נקודת-ההתחלה של התוכנית הזו.

---

## 2. סיווג שדות לפי שימוש

### א. מותר לשימוש-זהיר בניסוח-ללקוח (בעתיד, אחרי שכבת-סינון)
- `name` — כבר בשימוש.
- `question` — כבר בשימוש.
- `gender` — לצורת-פנייה בלבד (לשון זכר/נקבה), לא כתוכן-מהותי.
- `workStatus` — **רק** כאשר רלוונטי-ישירות למסחר/עסקים (למשל: `self`/`employed` משנים את המשמעות של שאלת-מסחר).
- `maritalStatus` — **רק** אם באמת-רלוונטי לשאלה הספציפית (לא כברירת-מחדל).
- `hasChildren` — **רק** אם רלוונטי לאחריות/סיכון/פרנסה בהקשר-השאלה.

### ב. ליועץ-בלבד / אסור-להציג-ללקוח
- `phone` — פרטי-קשר, לעולם לא בתוכן-הקריאה.
- `clientHistorySummary` הגולמי (במידה-ויחובר-בעתיד) — נתונים-סטטיסטיים-על-לקוח, לא ניסוח-ישיר.
- `repeatedSpiritualFlags` — רגיש-במיוחד, אבחון-רוחני-חוזר.
- מידע-רגיש מתוך `dynFields` (למשל `symptoms`/`treatment` בנושאי-בריאות — לא רלוונטי ל-commerce אך העיקרון-כללי).
- הערות-פנימיות (`advisorDiagnosis`, `weightedHouses`, `strongestSignals` וכו' — כבר-קיימים ב-`kashf-commerce-smart-layer.js` וכבר-מסווגים-נכון כ-advisor-only).
- `sourceText` גולמי מהספר — כבר-מוסתר-מהלקוח היום (התנהגות-קיימת, לא-לשנות).

### ג. שימוש-מותנה (תלוי-הקשר-השאלה)
- `quesitedName` — מותר **רק** אם הוא חלק ברור מהשאלה (למשל שותף/קונה/מוכר בעסקה) — לא סתם "מוזכר".
- `parentName` — בדרך-כלל **לא** לניסוח-ללקוח, משמש רק לזיהוי/הקשר-מנהלי (כמו בחאווי היום).
- `dynFields` — **לא** לצטט-אוטומטית (אין להעתיק את הטקסט-החופשי-שהלקוח-הקליד ישירות לתוך המסקנה) — להשתמש **רק** להבנת "מהות-העניין" (כלומר: לבחור-נכון בין ניסוחים-קיימים, לא ליצור-ניסוח-חדש מתוך תוכן-חופשי).

---

## 3. איך context אמור להשפיע על commerce — דוגמאות (לתכנון בלבד, לא מומש)

| מצב | התאמה נכונה | התאמה אסורה |
|---|---|---|
| `workStatus: 'employed'` שואל על מסחר/עסק | לנסח בזהירות-כללית — לא להניח שיש לו עסק קיים | לכתוב "העסק שלך" כאילו הוא בעל-עסק בפועל |
| `workStatus: 'self'` שואל על עסקה | מותר להתייחס להכנסה/לקוחות/תזרים כהקשר-רלוונטי | להמציא-פרטים על סוג-העסק שלא נאמרו |
| `workStatus: 'unemployed'` שואל על קנייה/השקעה | להדגיש-זהירות וסיכון בניסוח (התאמת-טון, לא-שינוי-מסקנה) | להניח הכנסה-קבועה שלא קיימת |
| `maritalStatus: 'married'` + `hasChildren: 'yes'` + שאלה **נוגעת-לפרנסה/התחייבות-כספית** | אפשר להזכיר-בזהירות אחריות-משפחתית כהקשר-רך | לכתוב "בגלל שיש לך ילדים" כטיעון-מרכזי במסקנה |
| `maritalStatus: 'single'` + `hasChildren: 'no'` + שאלת-מסחר-כללית | **לא להזכיר בכלל** — לא-רלוונטי, אין ערך מוסף | לציין-בכוח "אתה רווק בלי ילדים" ללא סיבה |
| `dynFields: { matter: 'עסקת נדל"ן עם שותף' }` | להשתמש **להבנת-הנושא** (למשל: לבחור בין ניסוח "עסקה" כללי ל"שותפות" אם רלוונטי) | לצטט את המשפט המדויק שהלקוח הקליד בתוך המסקנה |

**עיקרון-על:** ההקשר יכול **לכוון בחירת-ניסוח מתוך אפשרויות-קיימות** (כמו שכבר-נעשה ב-`certaintyPhrase`/`practicalGuidance` הקיימים ב-`kashf-commerce-smart-layer.js` — בחירה בין-משפטים-מוכנים לפי-מצב) — **לא ליצור-תוכן-פרשני-חדש** שלא נגזר מהחישוב הגורלי עצמו. context הוא **מסנן/מתאם-טון**, לא **מקור-מסקנה** — הכלל הזה מקביל-בדיוק ל"no invented data" ב-`CLAUDE.md`, רק ברמת-הלקוח ולא ברמת-הספר.

---

## 4. כללי-איסור (מפורש)

1. **לא** להציג `phone` ללקוח — בשום שלב, בשום נסיבה.
2. **לא** לכתוב "אתה נשוי/רווק/עם ילדים" אם זה לא-מוסיף-ערך-אמיתי להבנת-התשובה.
3. **לא** להשתמש בסטטוס-חיים כדי **להמציא** מסקנה — הוא יכול-לכוון-ניסוח, לא-לשנות-את-כיוון-החישוב (`overallPositive`/`certaintyLevel` נשארים תוצר-הלוח בלבד).
4. **לא** להפוך metadata (סטטוס-משפחתי/תעסוקתי) למסר מיסטי ("מכיוון שאתה נשוי, הצורה X מרמזת...") — זו בדיוק הסכנה של "המצאת-נתונים" שה-CLAUDE.md אוסר.
5. **לא** לצטט `dynFields` רגישים בפלט-ללקוח.
6. **לא** לערבב היסטוריית חאווי/כשף בלי `method` — כבר-מטופל מבנית בשלב-א׳ (ברירת-המחדל של `getGoralClientHistory` מסננת ל-`hawi` בלבד).
7. **לא** להשתמש ב-`clientHistorySummary` לפני-שמוודאים שהוא-מתאים לנושא-ולשיטה (הוא כרגע **לא-מגיע לכלל** ל-Kashf reading — סעיף 1).
8. **לא** לשנות את חישוב-הלוח — `primaryFormula`/`altFormula`/`dhamir`/`witnessTestimony` נשארים בדיוק כפי-שהם.
9. **לא** להחליף את **כללי-הספר** בהקשר-הלקוח — ה-context הוא שכבה-נוספת-של-ניסוח, לא-תחליף לפסיקה שמבוססת-על `COMMERCE_HOUSE_WEIGHT`/`fortuneHebrew`/`kashf-topic-rules.js`.

---

## 5. מבנה מוצע לשכבת-שימוש עתידית (לתכנון בלבד)

הצעה לחמישה מבנים חדשים, המשך-ישיר לדפוס-שכבר-קיים (`advisorDiagnosis` מול `clientWording` ב-`kashf-commerce-smart-layer.js`):

- **`advisorContextNotes`** — ליועץ-בלבד. כל מה שנגזר מ-context גם אם לא-הוצג-ללקוח (למשל: "workStatus=unemployed, לכן טון-הזהירות הועלה").
- **`clientSafeContext`** — **רק** המידע שעבר-סינון ואושר-כרלוונטי (למשל: `{ workStatus: 'self' }` בלבד, בלי `phone`/`parentName`/`dynFields` גולמי).
- **`contextRelevance`** — מבנה-החלטה מפורש (לא-מוסתר) שמתעד **למה** שדה נחשב-רלוונטי או לא לשאלה הספציפית (למשל: `{ hasChildren: { relevant: false, reason: 'שאלת-מסחר-כללית, אין קשר-ישיר' } }`) — זה מה שמונע את "לכתוב את זה בכוח אם לא רלוונטי" (סעיף 4.2).
- **`clientWordingAdjustments`** — רשימת-התאמות-הניסוח-הבטוחות שבוצעו בפועל (למשל: "הוחלף טון ל'זהירות-מוגברת' עקב workStatus=unemployed") — שקוף-ל-audit, לא-חלק-מהטקסט-עצמו.
- **`blockedContextFields`** — רשימה מפורשת של שדות-שנחסמו-במכוון מהפלט (`phone`, `dynFields` גולמי וכו') — הגנה-כפולה: גם אם שדה נכנס בטעות ל-`clientContext`, המבנה הזה מתעד שהוא **לא** אמור-להגיע-לפלט, כדי שבדיקה עתידית תוכל-לוודא-זאת-אוטומטית (בדיוק כמו בדיקה #1 בסעיף 7).

---

## 6. איך זה ישתלב בעתיד עם commerce smart layer

1. **לא** להשתמש ב-context ישירות בתוך `kashf-narrative-writer.js` — לעולם לא גישה-ישירה מ-`reading.clientContext.workStatus` בתוך הפונקציות הקיימות (`writeConclusionPara` וכו').
2. **קודם** להעביר את ה-context דרך שכבת-סינון (Context Sanitizer, שם-עבודה) שמייצרת את חמשת-המבנים בסעיף 5 — **פונקציה טהורה, נפרדת**, לא מוטמעת בתוך `kashf-commerce-smart-layer.js` הקיים.
3. `kashf-commerce-smart-layer.js` (בעתיד, לא-עכשיו) יקבל **רק** את הפלט-המסונן (`clientSafeContext`/`clientWordingAdjustments`) — לא את `reading.clientContext` הגולמי — כך שהחשיפה-לשדות-רגישים בתוך שכבת-הניסוח מוגבלת-מבנית מלכתחילה, לא-תלויה-במשמעת-תכנותית-בלבד.
4. הפלט יישאר מחולק כפי-שהוא-כבר-היום: `advisorDiagnosis` (כולל עכשיו גם `advisorContextNotes`/`contextRelevance`) מול `clientWording`/`practicalGuidance` (שיכולים-להיות-מושפעים מ-`clientWordingAdjustments` בלבד).
5. כל שינוי עתידי **חייב** להיבדק מול פלט לפני/אחרי (בדיוק כמו שנעשה בשלב-ג-חלקי: השוואת-HTML בייט-לבייט בין `clientContext` מינימלי לעשיר) — **לפני** שממשיכים לשלב-הבא.

---

## 7. בדיקות-חובה לשלב-מימוש-עתידי (לא נכתבו כעת)

1. `phone` לא מופיע בשום מקום בפלט ה-HTML (בדיקה מכנית: `!html.includes(phoneValue)`).
2. `dynFields` רגישים לא-מופיעים אוטומטית — הטקסט-החופשי-שהוקלד לא-מוטמע-מילה-במילה בפלט.
3. לקוח עם `workStatus: 'employed'` **לא** מקבל ניסוח שמניח שיש-לו-עסק-קיים.
4. לקוח עם `workStatus: 'self'` **כן** מקבל ניסוח-עסקי-מתאים כשזה-רלוונטי-לשאלה.
5. `maritalStatus`/`hasChildren` **לא מופיעים כלל** בפלט כשאינם-רלוונטיים (בדיקה שלילית — absence-of-mention, לא רק correctness-of-mention).
6. פלט-`commerce` **משתנה רק** כאשר context באמת-רלוונטי — לוח זהה + context-לא-רלוונטי-שונה ⇒ פלט זהה (מקביל-ישירות לבדיקה שכבר-בוצעה בשלב-ג-חלקי, אבל הפעם עם context-כן-רלוונטי מול context-לא-רלוונטי, לא רק "עשיר מול ריק").
7. חישוב-הלוח (`primaryFormula`/`overallPositive`/`certaintyLevel` הבסיסי-מהלוח) **לא-משתנה** בין שני-הרצות-עם-context-שונה על-אותו-לוח.
8. Hawi לא-נשבר (רגרסיה — כמו בכל שלב קודם).
9. הארכיון (שלב-א׳) לא-נשבר — קריאות ממשיכות-להישמר עם `method`/`clientContext` נכון.

---

## 8. המלצה — שלב-מימוש-עתידי מוצע (לא בוצע)

**commerce בלבד, Context Sanitizer בלבד, בלי לגעת בשאר-הנושאים.** בפירוט:

- לבנות **קודם** פונקציה-טהורה-נפרדת (Context Sanitizer) שמייצרת את חמשת-המבנים בסעיף 5 מתוך `reading.clientContext` — **בלי** לגעת ב-`kashf-narrative-writer.js` או ב-`kashf-commerce-smart-layer.js` בשלב-הזה (כלומר: שלב-ביניים נבדק-בעצמו, בדיוק כמו ששלב-ג-חלקי היה שלב-ביניים-שלא-שינה-פלט).
- **רק אחרי** שהוא נבדק ומאושר (בדיוק כמו שקרה כאן עם `_test_kashf_context_fields_transfer.mjs`), לחבר אותו ל-`kashf-commerce-smart-layer.js` **בנושא commerce בלבד** — לא לשאר-29-הנושאים.
- שאר-הנושאים ימשיכו-לפעול **בדיוק כפי-שהם** (fallback ל-`TOPIC_GUIDANCE` הישן) — אותו-דפוס-מדויק-שכבר-הוכיח-את-עצמו כשהוקם `kashf-commerce-smart-layer.js` לראשונה.

---

## הצהרות

- שום קוד לא נכתב/שונה. שום `narrative`/`kashf-commerce-smart-layer.js`/`goral-app.js`/מנוע/HTML לא נערך.
- שום age לא נוסף. שום AI חי, secret, storage/database/embeddings. שום נגיעה ב-`inner-compass`.
- שום commit/push — רק יצירת קובץ המסמך הזה.
- הצעד הבא — לפי החלטת אורן משה בלבד.
