# AI_RUNTIME_PHASE_6_SHARED_SYNC_PLAN — תכנון סנכרון `ai/` אל `supabase/functions/_shared/`

> **מסמך תכנון בלבד. לא בוצע קוד, לא deploy, לא מפתח, לא AI חי, לא שינוי-HTML, לא שינוי-מנועים, לא נגיעה ב-inner-compass.**
> תאריך: 2026-07-08. המשך ישיר ל-#28 ב-`WORKPLAN.md` (תשתית Runtime AI, שלבים 1–4).

---

## 1. הבעיה

`supabase/functions/oren-smart-ai/index.ts` (השלד שנוצר בשלב 3) מייבא כרגע:
```ts
import { callAnthropic } from '../../../ai/provider/anthropic-provider.js';
```
וטוען קבצי-prompt מ-`../../../ai/prompts/*.md` בזמן-ריצה. זה מבטא את *כוונת* המבנה, אך **לא יעבוד בפועל בזמן `deploy`**: Supabase Edge Functions (סביבת Deno) מארזת ופורסת כל פונקציה כיחידה מבודדת מתוך `supabase/functions/<שם-הפונקציה>/` — ואינה תומכת ב-import של קבצים שיושבים מחוץ לעץ הזה (חוץ מהחריג המתועד למטה, סעיף 2). בזמן `deploy` אמיתי, ה-import הזה ייכשל או פשוט לא ייכלל בחבילה שנפרסת.

זו בדיוק ההערה שכבר מתועדת כ-TODO בקוד עצמו (`index.ts`, שורות 16–21) — המסמך הזה בא לתכנן את הפתרון לפני שממשים אותו.

## 2. הפתרון המומלץ

שימוש במוסכמה הרשמית של Supabase: תיקיית `supabase/functions/_shared/`. זו התבנית המתועדת של Supabase עצמה לשיתוף קוד בין Edge Functions מרובות — קבצים בתוך `_shared/` **כן** נתמכים ב-import יחסי מתוך כל פונקציה תחת `supabase/functions/`, כי הם עדיין בתוך עץ ה-deploy הכולל של הפרויקט, לא מחוצה לו.

כלומר: לא משנים את מבנה `ai/` הקיים בשורש הריפו, ולא מוותרים על תמיכת-הדפדפן-בלי-build של שאר האתר — רק מוסיפים העתק מיועד-לפריסה בתוך `supabase/functions/_shared/`.

## 3. מה יועתק

- `ai/provider/anthropic-provider.js` → `supabase/functions/_shared/anthropic-provider.js`
- קובץ ה-prompt הרלוונטי, למשל `ai/prompts/cartomancy-runtime.md` → `supabase/functions/_shared/prompts/cartomancy-runtime.md`

(בשלב זה — קלפים בלבד, כי זה המודול היחיד עם prompt אמיתי. `hawi-runtime.md`/`kashf-runtime.md` נשארים stub ולכן אין טעם לסנכרן אותם עד שיהיה בהם תוכן אמיתי מאושר.)

## 4. מי נשאר מקור-האמת

`ai/provider/` ו-`ai/prompts/` בשורש הריפו **נשארים מקור-האמת היחיד** — כאן עורכים, כאן בודקים (`node --check`, בדיקת-mock), כאן קורה ביקורת-קוד רגילה. `supabase/functions/_shared/` הוא **artifact לפריסה בלבד** — נוצר-מחדש מהמקור בכל סנכרון, לעולם לא נערך ידנית שם. זה מונע מצב שבו שני עותקים "אמיתיים" סותרים.

## 5. איך הסנכרון יעבוד

סקריפט-קובץ יחיד ופשוט (רעיון, לא מומש בשלב זה) — למשל `scripts/sync-ai-shared.mjs` — שמבצע העתקה ישירה (`fs.copyFile`) מ-`ai/provider/anthropic-provider.js` ומ-`ai/prompts/cartomancy-runtime.md` אל `supabase/functions/_shared/`. מורץ ידנית לפני כל `supabase functions deploy`, לא אוטומטית ולא כ-hook.

**חשוב להדגיש:**
- **לא build system** — אין webpack/bundler/טרנספילציה. העתקת-קבצים גולמית בלבד (Node `fs`, אין תלות-npm חדשה).
- **לא framework** — לא מוסיף React/TypeScript-compilation/שום דבר לאתר הסטטי.
- **לא שינוי Runtime באתר** — הסקריפט רץ רק כהכנה ל-deploy עתידי של ה-Edge Function; שום קובץ שהדפדפן טוען לא מושפע.

## 6. סיכונים

- **שכפול קבצים** — שני עותקים פיזיים של אותו קוד/prompt קיימים על הדיסק בו-זמנית (`ai/...` ו-`supabase/functions/_shared/...`) — סיכון-drift קלאסי אם מישהו עורך את ההעתק בטעות במקום המקור.
- **שכחה להריץ sync** — אם עורכים את `ai/provider/anthropic-provider.js` או את ה-prompt ושוכחים להריץ את הסקריפט לפני `deploy`, הפונקציה הפרוסה תמשיך לרוץ עם גרסה ישנה בלי שום התראה.
- **prompt לא מעודכן בזמן deploy** — מקרה פרטי של הסיכון הקודם, אך חמור יותר: ה-AI עלול לפעול לפי כללי-סגנון/הגבלות ישנות (למשל גרסה שעדיין לא כללה איזשהו כלל-ברזל שנוסף מאוחר יותר ל-`cartomancy-runtime.md`).

## 7. מנגנוני הגנה

- **בדיקה שהקבצים מסונכרנים** — לפני `deploy`, השוואת-hash (או תוכן מילה-במילה) בין `ai/provider/anthropic-provider.js` לבין `supabase/functions/_shared/anthropic-provider.js` (וכנ"ל ל-prompt); אם שונים — עצירה עם שגיאה ברורה במקום deploy בשקט של גרסה ישנה. ניתן להריץ כבדיקה עצמאית, בלי תלות ב-CI שלא קיים בריפו.
- **אין secrets** — הסקריפט מעתיק אך ורק את שני הקבצים הספציפיים שצוינו (סעיף 3), לא תיקיות שלמות ולא `.env*` — כך שאין דרך שהוא "יגרור" בטעות קובץ-סוד לתוך `_shared/`.
- **אין מפתחות** — אין בשום קובץ מהקבצים המסונכרנים ערך-מפתח בפועל (כבר אומת בשלבים 1–4: `anthropic-provider.js` תמיד מקבל `apiKey` כפרמטר-נכנס, לא מכיל אותו).
- **fallback נשמר** — הסנכרון לא נוגע בלוגיקת ה-fallback הקיימת ב-`index.ts` (`{ok:false, fallback:true, conclusion:engineConclusion}`); אם ה-sync נשכח או ה-Edge Function נכשלת מכל סיבה, ההתנהגות עדיין נופלת-חזרה לפלט-המנוע הדטרמיניסטי, לא שוברת את חוויית-הלקוח.

## 8. תוכנית קומיטים עתידית (לשלב הביצוע, כשיאושר)

- **commit 1** — יצירת `supabase/functions/_shared/` + סקריפט הסנכרון (`scripts/sync-ai-shared.mjs`), הרצה ראשונה ידנית ליצירת ההעתקים הראשוניים.
- **commit 2** — בדיקת-סנכרון: סקריפט/בדיקת-mock קטנה שמוודאת שההעתקים ב-`_shared/` זהים-בתוכן למקור ב-`ai/` (סעיף 7) — מריצים אותה כדי לאשר שהיא באמת תופסת אי-התאמה, ואז שהיא עוברת נקי על המצב התקין.
- **commit 3** — עדכון תיעוד: `index.ts` מתעדכן להצביע בפועל על `../_shared/anthropic-provider.js` (במקום ה-import החוצה-לתיקייה הנוכחי), `WORKPLAN.md` מתעד את הסבב, והערת ה-TODO הישנה ב-`index.ts` מוסרת (הבעיה שהיא תיארה נפתרה).

## 9. מה דורש אישור אורן משה (לפני כל ביצוע)

- יצירת סקריפט-הסנכרון עצמו (שינוי-מבנה בריפו, גם אם קטן).
- `deploy` ראשון בפועל של ה-Edge Function ל-Supabase.
- הכנסת secret אמיתי (`ANTHROPIC_API_KEY` חדש, אחרי סבב-מפתח) לסביבת ה-deploy.
- חיבור כפתור/UI כלשהו באתר שקורא לפונקציה הזו.
- הפעלת AI חי בפועל (כל קריאה אמיתית ראשונה, כולל בדיקה).

---

## המלצה

**לעצור כאן ולא לבצע שלב 6 עכשיו.**

הסיבה: שלב 6 (כתיבת סקריפט-הסנכרון בפועל) הוא הצעד הראשון בשרשרת שמובילה ישירות ל-deploy אמיתי — ברגע שהסנכרון קיים, הפיתוי/ההיגיון הטבעי הוא להמשיך מיד ל-`deploy` בפועל, מה שדורש כבר עכשיו להחליט על סבב-מפתח חדש, איפה מוגדר ה-secret, ומי מותר לו לקרוא לפונקציה. אלה החלטות מוצריות/אבטחה אמיתיות (לא רק טכניות) שכדאי לקבל במפורש **לפני** שכותבים את הקוד שמייתר אותן — לא כתוצאה-לוואי של "כבר יש לנו סקריפט, בוא נמשיך".

מבנית: `ai/provider/anthropic-provider.js` ו-`ai/prompts/cartomancy-runtime.md` כבר קיימים ותקינים (שלבים 1–2, מאומתים). שום דבר לא "נתקע" אם ממתינים — אין עלות-זמן אמיתית בעצירה כאן. כשתחליט שאתה מוכן להתקדם לכיוון deploy אמיתי (כולל ההחלטות שבסעיף 9), שלב 6 מוכן להתחיל תוך דקות.
