
# Claude Code local settings (session-specific)
.claude/settings.local.json

# Local backups before Goral Hachol UI rebuild
_backup_goral_core_before_ui_rebuild/
backup-goral-core-before-ui-rebuild.tar.gz

# Claude Code local settings — worktrees ו-local בלבד
.claude/worktrees/
.claude/settings.local.json

# ── מיזוג "המצפן הפנימי" (merge-inner-compass) ──
# הגנת סודות — לעולם לא לקמט מפתחות/סביבה
.env
.env.*
!.env.example
# שאריות build/וונדור שאסור שייכנסו לריפו הסטטי
node_modules/
.next/
# ארכיוני snapshot כבדים
*.zip
# גיבויים של המצפן (לא מפורטים לוונילה)
*.bak
backup_*/
