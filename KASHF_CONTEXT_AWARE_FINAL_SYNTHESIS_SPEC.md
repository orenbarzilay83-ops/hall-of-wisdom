# KASHF_CONTEXT_AWARE_FINAL_SYNTHESIS_SPEC — תכנון שכבת-סינתזה מייעצת, מודעת-הקשר

> **מסמך תכנון בלבד. לא נכתב קוד, לא שונה מנוע/HTML, לא חובר AI חי, לא נוסף secret, לא בוצע deploy, לא נוצר storage/database/embeddings/UI, לא נגע ב-`inner-compass`. לא בוצע commit/push — רק יצירת הקובץ.**
> תאריך: 2026-07-08. **הבהרה מהותית של אורן משה, המסמך כולו בנוי סביבה:** הבעיה אינה חוסר-ידע — כמעט כל-הידע כבר קיים בקבצי כשף/חאווי/מנועים (אומת שוב-ושוב במסמכי-audit קודמים). הבעיה היא **שהמנועים לא-משתמשים בהקשר-המלא** (פרטי-לקוח, שאלה, היסטוריה) כדי-לבנות מסקנה — הם פולטים שורות-ידע/verdict-קבוע. **המטרה כאן היא לא "להכניס ספרים" — היא לגרום למנוע להשתמש-נכון במה-שכבר-יש.**

---

## 1. הבעיה הנוכחית

המנועים (`kashf-topic-rules.js::verdictByCountQuality` וכו', `kashf-narrative-writer.js::TOPIC_GUIDANCE`) בונים מסקנה על-ידי **הדבקת שורת-ידע/פסק-קבוע** (`verdict.text` הנבחר-לפי-פולריות, או משפט-אחד-מ-TOPIC_GUIDANCE) — לא על-ידי **סינתזה** שמחברת בין הלוח, השאלה, ופרטי-הלקוח. `kashf-commerce-smart-layer.js` (שכבר-נבנה, `commerce` בלבד) הוא צעד-ראשון לכיוון-הנכון (קורא-שם-צורה-ספציפי, לא-רק-פולריות) — **אך גם הוא עדיין לא-משתמש בפרטי-הלקוח בכלל** (ראו סעיף 3 — ממצא מדויק).

## 2. איזה קלט חייב להיכנס למסקנה

| שדה | קיים היום ב-`reading`/`clientContext`? |
|---|---|
| `clientName` | ✅ כן (`clientContext.name`) |
| `gender` | ✅ כן (`clientContext.gender`) — משמש-רק-ל-לשון-זכר/נקבה, לא-להקשר-תוכני |
| `maritalStatus` | ❌ **נאסף ב-UI, לא-מגיע ל-`buildKashfReading`** (ראו סעיף 4) |
| `childrenStatus` | ❌ **נאסף ב-UI, לא-מגיע** (ראו סעיף 4) |
| `occupation`/`employmentStatus` | ❌ **נאסף ב-UI (`workStatus`), לא-מגיע** (ראו סעיף 4) |
| `question` | ✅ כן |
| `topicId` | ✅ כן |
| `readingMethod: kashf` | ✅ כן (מובנה — `goralMode==='kashf'`) |
| `board`/`houses`/`figures` | ✅ כן (`keyHouseReadings`) |
| `dhamir` | ✅ כן (`reading.dhamir`) |
| `leshonHaInyan` | ⚠ **מחושב בקובץ נפרד (`kashf-leshon-hainyan.js`), אך לא-מיובא/לא-מחובר כלל ל-`buildKashfReading`** (ראו סעיף 8) |
| `witnesses` | ✅ כן (`reading.witnessTestimony`, בתים 13-14) |
| `judge` | ✅ כן (בית 15 בתוך `keyHouseReadings`, ומוצג ב-`judgeNote`) |
| `complement` (משלים) | ⚠ חלקי — `dhamirType4External` קיים אך לא-מוצג-ללקוח (הוסר בכוונה, WORKPLAN #25) |
| `topicFormulaResult` | ✅ כן (`primaryFormula`/`altFormula`) |
| `sourceEvidence` | ✅ כן (`sourceText`/`sourceRef` בכל רמה) — קיים בקוד, מוסתר-מהלקוח בכוונה |
| `previousClientReadings` | ⚠ **מחושב (`reading.clientHistorySummary`), אך נופל לפני שמגיע לכשף** (ראו סעיף 4/8 — ממצא-מדויק) |

## 3. דוגמת פרופיל — איך אותו לוח צריך-להתנסח אחרת

**לקוח א':** אבי, רווק, ללא ילדים, שכיר, שואל על-מסחר ("האם ההשקעה תצליח?").
**לקוח ב':** אותו-לוח-בדיוק, אך: נשוי-עם-ילדים, עצמאי/בעל-עסק, שואל על-אותה-שאלה.

**היום — התוצאה זהה-לחלוטין לשניהם** (שום שדה-פרופיל לא-משפיע על-הניסוח). **מה שצריך-להשתנות:**
- לאבי (שכיר, רווק): אין-להזכיר "עסק קיים"/"שותפים"/"בת-זוג" כלל — המסקנה צריכה-להתייחס-ל"השקעה" באופן-שמתאים-לשכיר-שמשקיע, לא-לבעל-עסק.
- ללקוח-ב' (עצמאי, נשוי+ילדים): מותר-להתייחס-ל"העסק"/"הפרנסה-המשפחתית" — כי-זה-רלוונטי-לפרופיל-שלו, **אם וכאשר** הבתים-הרלוונטיים (2, 10 וכו') תומכים-בכך מהותית — לא-סתם-כי-הוא-נשוי.
- לשואל-על-זוגיות-קיימת (במקום-מסחר): "בית-הזוגיות" (7) הופך-רלוונטי-ישירות לניסוח, לא-רק-כרקע.

**העיקרון:** פרטי-הלקוח **לא ממציאים תוכן חדש** — הם קובעים **אילו-מהעובדות-שכבר-חושבו רלוונטיות-להזכיר, ואיך-לנסח-אותן** (מונחים מתאימים-למצב, לא-מונחים-שסותרים-את-הפרופיל).

## 4. כללי-התאמה לפי פרטי-לקוח

- **לא לכתוב "בן/בת-זוג" אם הלקוח רווק ואין-קשר-קיים**, אלא-אם השאלה-עצמה עוסקת-בקשר (למשל "האם אמצא זוגיות").
- **לא להזכיר ילדים אם אין ילדים**, אלא-אם הלוח-והשאלה מכוונים-לכך (שאלת-הריון/פוריות למשל).
- **לא לנסח עסק כעסק-עצמאי אם הלקוח שכיר** — ולהפך.
- **לא לתת מסקנה כללית-מדי אם השאלה מאוד-ספציפית** (למשל שאלה על-עסקה-קונקרטית מקבלת מסקנה-על-אותה-עסקה, לא-על-"מסחר-בכלל").
- **לא להכניס פרטים שלא-קיימים-בפרופיל** — אם `maritalStatus`/`workStatus` ריקים (המשתמש-לא-מילא), לא-מניחים-כלום, לא-מנחשים.

## 5. מבנה-מסקנה נכון (9 שלבים)

א. **פתיחה לפי השאלה** — לא-פתיחה-גנרית, פתיחה-שמזכירה-את-השאלה-שנשאלה-בפועל.
ב. **מה הלוח מראה בפועל** — צורות-ספציפיות בבתים-הרלוונטיים (כבר-קיים חלקית ב-`commerce`).
ג. **מה הדמיר/מחשבת-השואל מוסיפים** — קיים בקוד (`reading.dhamir`), לא-מנוצל-בניסוח-הסינתטי.
ד. **מה הדיין/העדים מחזקים-או-מחלישים** — קיים (`judgeNote`, `witnessTestimony`), חלקית-מנוצל.
ה. **מה נוסחת-השאלה אומרת** — `primaryFormula`/`altFormula`, קיים.
ו. **איך זה קשור לפרטי-הלקוח** — **לא קיים בכלל היום** (הפער המרכזי).
ז. **אבחון-פנימי ליועץ** — קיים-כ-data (`advisorDiagnosis` ב-`commerce`), לא-מוצג.
ח. **ניסוח ללקוח** — קיים-חלקית (`clientWording`), לא-מודע-פרופיל.
ט. **המלצה מעשית** — קיימת-חלקית (`practicalGuidance`).

## 6. הפרדה חשובה: Advisor Diagnosis מול Client Wording

**Advisor Diagnosis** (כבר-קיים-כ-data ב-`commerce`, מסעיף-קודם): חומר-רגיש, סימנים-רוחניים, ספקות, אזהרות — כל-מה-שאורן-משה צריך-לדעת-לפני-שהוא-מדבר-עם-הלקוח. **לא מוצג ללקוח לעולם.**

**Client Wording**: ניסוח-נקי, מקצועי, מותאם-לפרופיל-הלקוח-ולשאלה, בלי-להפחיד, בלי-להמציא, בלי-להיות-רובוטי. **תמיד עובר דרך כללי-ההתאמה בסעיף 4** לפני-שהוא-מוצג.

זו **בדיוק** ההבחנה שכבר קיימת-במסמכי-מדיניות (`OREN_CONCLUSION_STYLE_SPEC.md` שכבות 1-2) — המסמך-הזה **לא ממציא-הבחנה-חדשה**, הוא-מיישם-אותה-עם-מודעות-פרופיל.

## 7. מה אסור לשכבת-הסינתזה לעשות

- **לא לחשב מחדש את הלוח** — `executeFormula`/`countQualityInHouses`/`classifyFigure` נשארים-בדיוק-כמו-שהם.
- **לא להמציא חוק** — כל-קביעה-פרשנית מצוטטת ממקור-קיים-ומאומת בקוד.
- **לא להשתמש במקור לא-מאושר** — הקובץ הערבי (`kashf-al-asrar-arabic-only.html`) נשאר-בלתי-מאושר.
- **לא לערבב חאווי וכשף** — פרופיל-הלקוח משותף-מבנית (`getClientContext`), אך שכבת-הסינתזה-של-כשף נשארת-קוד-נפרד מזו-של-חאווי (בדיוק כמו `kashf-commerce-smart-layer.js` הקיים, שלא-מייבא מ-`narrative-fact-phrasing.js`).
- **לא להתעלם מפרטי-הלקוח** — זה-בדיוק-הבאג-הנוכחי (סעיף 3-4) שהמסמך-הזה בא-לתקן.
- **לא לומר דברים שסותרים את פרטי-הלקוח** — למשל "בת-הזוג שלך" ללקוח-רווק.
- **לא להציג חומר-רגיש ללקוח בלי ניסוח-מקצועי** — Advisor Diagnosis נשאר-נפרד (סעיף 6).
- **לא להפוך סימן-חלש לוודאות** — עיקרון שכבר-מיושם ב-`certaintyLevel`/`weakSignals` (commerce).
- **לא להגיד שהכול תקין רק כי אין שגיאת-קוד** — בדיקת-קוהרנטיות (כמו ב-`KASHF_COMMERCE_COHERENCE_FINAL_REVIEW.md`) נדרשת-גם-לכל-הרחבה-עתידית-שכוללת-פרופיל-לקוח.

## 8. מה נבדק בפועל בקוד הקיים (לא-הנחה — כל שורה כאן אומתה)

- **האם פרטי-הלקוח נשמרים-ונגישים בזמן-הקריאה?** **כן, חלקית וברור-איפה-נעצר:** `getClientContext()` (`goral-hachol/ui/goral-app.js:627-647`) **כבר אוסף** `maritalStatus`, `workStatus`, `hasChildren` (מ-`profileState`, שממולא-ב-UI) — אך בבניית `clientCtx` לכשף (`goral-app.js:1155-1160`) **מועתקים רק 4 שדות**: `name`, `question`, `age`, `gender`. שלושת-השדות-הנוספים **נאספים ואז נזרקים**, לא-מגיעים-אף-פעם ל-`buildKashfReading`.
- **האם `reading` object כולל client profile?** רק-חלקית — `reading.clientContext` ב-`kashf-reading-engine.js:611-616` בנוי-מ-4-שדות-בלבד (אותה-הגבלה).
- **האם archive/history נגישים למסקנה?** **תשתית-קיימת-ולא-מנוצלת, בדיוק כמו-ה-profileState:** `reading.clientHistorySummary` **כן מחושב** (`goral-app.js:1126-1130`, קורא ל-`window.GORAL_CLIENT_ARCHIVE.summarizeGoralClientHistory`) — **לפני** שענף-כשף בכלל מתחיל — אך **לא מועתק** ל-`clientCtx` שנשלח ל-`buildKashfReading`.
- **האם `kashf-reading-engine.js` מקבל את פרטי-הלקוח?** רק-4-שדות (name/question/age/gender) — לא-marital/work/children/history.
- **האם `kashf-narrative-writer.js` משתמש-בפרטי-הלקוח?** רק ל-`name` (פתיח-אישי) ו-`gender` (לשון-זכר/נקבה) — **אין שום-שימוש-תוכני** ב-marital/work/children אפילו-אם-הם-היו-מגיעים.
- **איפה נוצר `verdict.text` היום?** `kashf-topic-rules.js` (מפת `verdictByCountQuality`/`verdictBySaadNahs` וכו' — 28/29 נושאים) + `kashf-narrative-writer.js::TOPIC_GUIDANCE` (משפט-קבוע-שני, ברמת-הפסקה-ללקוח).
- **איפה אפשר להוסיף context-aware synthesis בלי-לשבור מנועים?** **אותה נקודה-מדויקת שכבר-הוכחה-עובדת ב-`commerce`:** שדה-נוסף על `reading` (למשל `reading.contextSynthesis`), מחושב ב-`kashf-reading-engine.js` בסוף `buildKashfReading` (try/catch, נופל-ל-`null`), נצרך-אופציונלית ב-`kashf-narrative-writer.js` (אם קיים, משתמשים; אחרת נופלים-לניסוח-הישן) — **התבנית-המדויקת שכבר-אומתה ב-`kashf-commerce-smart-layer.js`, רק-עם-קלט-עשיר-יותר** (כולל-פרופיל, לא-רק-לוח).

## 9. ארכיטקטורה מוצעת

```
Deterministic Engine (קיים, לא-משתנה)
  executeFormula / countQualityInHouses / classifyFigure
      │  מחשב עובדות-לוח בלבד — לא-נוגעים
      ▼
Context Collector (חדש — הפער העיקרי שנמצא)
  אוסף: profileState המלא (לא-רק-gender), clientHistorySummary,
  question, topicId — כל-מה-שכבר-נאסף-ב-UI/ב-archive אך-נזרק היום
      │
      ▼
Kashf Reading Intelligence (קיים ברובו, כבר-מחובר ל-reading)
  dhamir / leshonHaInyan (צריך-חיווט, ראו סעיף 8) / witnessTestimony / judge
      │
      ▼
Final Synthesis Layer (חדש, מרחיב את kashf-commerce-smart-layer.js)
  מחבר: עובדות-לוח + הקשר-לקוח + דמיר/עדים/דיין → advisorDiagnosis מלא
      │
      ▼
Client Wording Layer (מרחיב clientWording הקיים)
  מיישם את כללי-ההתאמה (סעיף 4) על advisorDiagnosis → משפט מותאם-פרופיל
```

**כל שכבה חדשה = תוספת, לא-שכתוב** — בדיוק כמו ש-`kashf-commerce-smart-layer.js` לא-נגע ב-`executeFormula`.

## 10. דוגמת Output רצויה

**אותו-לוח, שני-פרופילים, נושא מסחר:**

**אבי (רווק, ללא-ילדים, שכיר):**
> "אבי — בהשקעה שאתה שואל עליה, בית-הכניסה שלך מראה [צורה] — [פסיקה]. בהיותך שכיר, כדאי-לבחון את זה כהשקעה-נקודתית, לא-כמהלך-עסקי-רחב. [המשך לפי-הלוח]."

**אדם נשוי, עצמאי, עם ילדים (אותו לוח):**
> "[שם] — בעסק שאתה שואל עליו, בית-הכניסה שלך מראה [אותה-צורה] — [אותה-פסיקה]. כבעל-עסק, כדאי-לשקול איך זה משפיע גם-על-היציבות-המשפחתית. [המשך לפי-הלוח]."

**ההבדל:** לא-בעובדות-הלוח (זהות), רק-בהקשר-שנבחר-להדגיש ובמינוח-שמתאים-לפרופיל. **שום-פרט לא-הומצא** — רק-נבחר-מה-רלוונטי-מתוך-מה-שכבר-חושב.

## 11. המלצה מעשית — סדר צעדים (לא בוצע)

לפי-הממצאים-בסעיף 8, הפער האמיתי הוא **לא בחישוב, לא-בידע** — הוא **בצינור-ההעברה** (`goral-app.js:1155-1160`) שזורק-שדות-שכבר-קיימים. לכן:

1. **קודם:** audit-ממוקד (לא-כללי) של **מה-בדיוק** `profileState`/`workStatus` יכולים-להכיל (ערכים-אפשריים — לא-אומת-בסבב-הזה) ו-`clientHistorySummary` (מבנה-מדויק) — כדי-לדעת-מה-בדיוק-יש-להעביר, לא-רק-שיש-משהו.
2. **אחר-כך:** בניית ה-**Context Collector** (סעיף 9) — הרחבת `clientCtx` ב-`goral-app.js` להעביר-את-כל-מה-שכבר-נאסף (לא-לאסוף-חדש), ו-`buildKashfReading`/`kashf-reading-engine.js` להעביר-את-זה-הלאה ל-`reading.clientContext` המלא.
3. **רק-אז:** הרחבת `kashf-commerce-smart-layer.js` (או-בניית-שכבה-מקבילה) לצרוך-את-הפרופיל-המלא ולהחיל-את-כללי-ההתאמה (סעיף 4).
4. **לא-קודם:** prompt/AI — אין-טעם-לבנות-ניסוח-AI-מודע-הקשר לפני-שההקשר-עצמו-בכלל-מגיע-למנוע.

**לא בוצע — רק תיעוד-סדר-מומלץ.**

---

## הצהרות

- שום קוד לא נכתב/שונה. שום מנוע/HTML לא נערך. שום deploy/merge.
- שום AI חי, secret, storage/database/embeddings, UI. שום נגיעה ב-`inner-compass`.
- שום commit/push — רק יצירת קובץ המסמך הזה.
- הצעד הבא — לפי החלטת אורן משה בלבד.
