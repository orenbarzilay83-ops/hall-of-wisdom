# KASHF_CONTEXT_COLLECTOR_IMPLEMENTATION_PLAN — תוכנית מימוש בלבד

> **מסמך תכנון בלבד. לא נכתב/שונה קוד, לא שונה `goral-app.js`, לא שונה מנוע, לא שונה HTML, לא נוצר UI חדש, לא חובר AI חי, לא נוסף secret, לא בוצע deploy, לא נוצר storage/database/embeddings, לא נגע ב-`inner-compass`. לא בוצע commit/push — רק יצירת הקובץ.**
> תאריך: 2026-07-08. ממשיך את `KASHF_CLIENT_CONTEXT_FIELDS_AUDIT.md` (מאושר, `ef074f9`) — תוכנית-מימוש עתידית, לא קוד.

---

## 1. החלטות מוצר שאושרו (ציטוט-מסוכם)

1. **קריאות כשף יישמרו לארכיון**, לא רק חאווי — כל קריאת כשף תישמר עם `method`, `topicId`, `question`, `clientContext` בסיסי, ו-summary — כדי ש-`clientHistorySummary` ישקף בעתיד גם היסטוריית-כשף.
2. **`age` יתווסף כשדה UI אמיתי, אופציונלי בלבד.** אין לחייב גיל. אם ריק — לא לנחש ולא להשתמש בו. אם קיים — יכול לעזור להתאמת-ניסוח.
3. **`maritalStatus`/`workStatus`/`hasChildren` יועברו** לכשף (כבר קיימים ב-UI, רק לא מגיעים).
4. **`dynField_*` יועברו** לכשף — אלה שדות-תוכן (מהות-הענין, תסמינים, תיאור-חלום וכו'), לא metadata.
5. **`clientHistorySummary` יועבר רק אחרי** שהוא כולל גם היסטוריית-כשף (תלוי בהחלטה 1) — לא לפני, כדי לא ליצור מצג-שווא של "אין היסטוריה" כשבעצם יש (רק שלא נשמרה).

---

## 2. קבצים שיידרשו לבדיקה/שינוי עתידי (לא נבדקו/שונו כעת מעבר לביקורת הקודמת)

| קובץ | תפקיד עתידי |
|---|---|
| `goral-hachol/ui/goral-app.js` | נקודת-ההשמטה (שורות 1154-1160), חישוב-ה-history (1126-1130), מנגנון-הטוגל של profileState (1352-1364), ה-UI-buttons עצמם דרך `getClientContext()` (627-650), ונקודת השמירה-לארכיון בחאווי (887-889) |
| `goral-hachol/engine/kashf-reading-engine.js` | `buildKashfReading` — היכן `clientContext` נבנה מחדש בתוך המנוע (שורות ~611-616 לפי הביקורת הקודמת) |
| `goral-hachol/engine/kashf-narrative-writer.js` | היכן `clientContext` נקרא בפועל (היום: רק `name`/`gender`) — מקום עתידי-פוטנציאלי לשימוש ב-`dynField_*`/`age` בניסוח |
| `goral-hachol/engine/goral-client-archive.js` | `saveGoralReadingToArchive`, `getGoralClientHistory`, `summarizeGoralClientHistory` — צריך להרחיב לתמוך ב-`method` מבלי לשבור רשומות-חאווי קיימות |
| `goral-hachol.html` | היכן מוגדרים כפתורי-הפרופיל הקיימים (`data-profile`) ואיפה יתווסף שדה-קלט חדש ל-`age` |

`goral-hachol-new.html` — **לא רלוונטי**: אומת (בביקורת הקודמת) שהוא לא קורא ל-`buildKashfReading` ולא משתמש ב-`goralMode`/`profileState` בכלל — אין צורך לגעת בו.

---

## 3. Context Collector מוצע — סיווג שדות

### שדות-חובה
- `name` — כבר מגיע.
- `question` — כבר מגיע.
- `gender` **אם קיים** — כבר מגיע, אך לא לחייב (יכול להישאר `null`, כפי שהוא היום).
- `topicId` — **חדש להעברה** (היום `kashfTopicId` מחושב ב-`goral-app.js:1146` אך לא נכלל בתוך `clientCtx` עצמו — הוא כבר פרמטר-נפרד ל-`buildKashfReading`, לא שינוי מבני, רק לתעד).
- `method` — **שדה חדש**, קבוע `"kashf"` בענף הזה — נדרש לצורך סעיף 4 (ארכיון) יותר מאשר לצורך ה-reading עצמו.

### שדות-אופציונליים
- `age` — רק אם המשתמש הזין; ברירת-מחדל `null`/`undefined`, לא `''` ולא ניחוש.
- `maritalStatus`, `workStatus`, `hasChildren` — מועברים כפי-שהם (כולל האפשרות ש-`null`), בלי לתקן את בעיית ה-toggle-הדו-משמעי שתועדה בביקורת (זה לא בהיקף התוכנית הזו).
- `parentName`, `quesitedName`, `phone` — מועברים כטכני/הקשרי בלבד.
- `dynField_*` — מועברים כאובייקט מקונן (למשל `dynFields: { matter, symptoms, duration, ... }`) ולא כשדות-שטוחים בודדים, כדי שלא יתנגשו בשמות עתידיים ויישארו ניתנים-לזיהוי כ"תוכן-שהוקלד-ע"י-משתמש" (רלוונטי לסעיף 8 — רגישות).

### שדות ליועץ-בלבד (לא לניסוח-ישיר-ללקוח)
- `phone` — פרטי-קשר, לא תוכן-לקריאה.
- `clientHistorySummary` המלא (כולל `repeatedTopics`/`repeatedSpiritualFlags`) — כפי שנקבע כבר ב-`OREN_CONCLUSION_STYLE_SPEC.md`: אבחון-יועץ ≠ ניסוח-ללקוח.
- `spiritual`/`repeatedSpiritualFlags` — רגיש במיוחד (אבחון-רוחני חוזר), אף פעם לא ישירות ללקוח.
- כל `notes` עתידי (לא קיים היום, אך אם ייווסף — אותו כלל).

---

## 4. שינוי ארכיון — שמירת קריאות-כשף בלי לשבור חאווי

**עקרון-על:** להוסיף, לא לשנות מבנה קיים. שדה `method` חדש עם ברירת-מחדל משתמעת לרשומות-ישנות.

- כל רשומה חדשה (משני הענפים) תקבל שדה `method: "hawi" | "kashf"`. רשומות-חאווי קיימות ב-`localStorage` (שנוצרו לפני השינוי) **אין להן `method`** — `getGoralClientHistory`/`summarizeGoralClientHistory` העתידיים צריכים להתייחס ל-`method` חסר כ-`"hawi"` (כי כל מה שנשמר עד היום הוא חאווי בלבד, לפי הממצא המאושר בביקורת) — לא כ"לא-ידוע", כדי לא לאבד היסטוריה קיימת.
- שמירה-לכשף תיקרא מנקודה מקבילה ל-`buildInterpretationHtml`'s שמירה (`goral-app.js:887-889`), אך בתוך ענף-הכשף (סביבות `goral-app.js:1162-1170`, **אחרי** `buildKashfReading` כדי שיהיה `kashfReading` זמין ל-summary, **לפני** ה-`return`).
- מה שנשמר לכשף: `method: "kashf"`, `topicId` (kashfTopicId), `clientContext` הבסיסי (name/gender/age אם יש/maritalStatus וכו' — **לא** את כל ה-`reading` הענק), ו-`conclusionSummary` (טקסט קצר — למשל `kashfReading.commerceSmartLayer?.clientWording` אם קיים, או fallback לחלק מ-`primaryFormula.verdict.text`) — **לא** שמירת ה-HTML המלא, לא שמירת `sourceText`/ציטוטי-ספר (אין צורך, וזה חוסך גודל-אחסון).
- **לא לשמור secrets** — אין שדה כזה היום בכל מקרה; לוודא בזמן-המימוש שאין `phone`/`age` נשמרים במקום שלא-מיועד-להם מעבר ל-`clientContext` הרגיל (אותה רמת-רגישות כמו חאווי כבר שומר היום).
- **Fallback:** אם `window.GORAL_CLIENT_ARCHIVE?.saveGoralReadingToArchive` לא זמין (כמו הבדיקה הקיימת בחאווי, `goral-app.js:887`) — הענף הכשפי ממשיך לפעול בלי שמירה, לא זורק שגיאה. זהה לדפוס הקיים.

---

## 5. `age` — תוכנית

- **איפה להוסיף UI:** ליד קבוצת-הכפתורים הקיימת של `profileState` ב-`goral-hachol.html` (סביבות שורות 2966-2986) — קלט-מספר בודד (`<input type="number">`), **לא** כפתור-בחירה (כי גיל הוא ערך-רציף, לא קטגוריה), עם label ברור שמסמן "אופציונלי".
- **איך לשמור כערך אופציונלי:** ב-`profileState` (`goral-app.js:15`) יתווסף `age: null` כברירת-מחדל — **לא** מחרוזת ריקה. מאזין-input (לא click כמו שאר הכפתורים) יעדכן `profileState.age = value ? Number(value) : null`.
- **איך להעביר לכשף:** דרך `getClientContext()` (יתווסף `age: profileState.age` באותו דפוס כמו `maritalStatus` וכו') ואז דרך ה-Context Collector (סעיף 3) אל `clientCtx.age`.
- **מה עושים אם ריק:** `age` נשאר `null`/`undefined` עד הקצה — **לא** להמיר ל-`''` ולא לנחש ערך. כל שכבה במורד הזרם (כשף, ניסוח, ארכיון) חייבת להתייחס ל-`age == null` כ"אין מידע", לא כ-0 או כערך-ברירת-מחדל.

---

## 6. `dynField_*` — תוכנית

- **איך לאסוף:** כבר נאסף היום ב-`getClientContext()` (`goral-app.js:643-648`) — שום שינוי-איסוף נדרש, רק שינוי-העברה.
- **איך להעביר ל-`buildKashfReading`:** כאובייקט מקונן `dynFields` (ראו סעיף 3) בתוך `clientCtx`, לא כשדות-שטוחים — כדי ש-`buildKashfReading`/`kashf-reading-engine.js` יוכלו להתייחס אליהם כקבוצה אחת בלי לדעת מראש את שמות-כל-השדות האפשריים (הם משתנים לפי `selectedQuestion.clientFields` מ-`question-bank.js`).
- **איך לאפשר ל-narrative להשתמש בעתיד:** `kashf-narrative-writer.js` יוכל לקרוא `reading.clientContext.dynFields?.matter` וכו' **רק** דרך שכבת-סינתזה מפורשת (בדומה ל-`kashf-commerce-smart-layer.js`) — לא ישירות בתוך `TOPIC_GUIDANCE` הקבוע — כדי לשמר את עקרון "אבחון-יועץ מול ניסוח-ללקוח" ואת ה"no invented data": תוכן חופשי שהמשתמש הקליד יכול לשמש כ**הקשר להבנת השאלה**, אבל אסור שיהפוך למקור-פסוקה עצמאי שמחליף את חישוב-הלוח.
- **לא להציג אוטומטית ללקוח אם רגיש:** `dynField_*` כמו `symptoms`/`treatment` (מידע-בריאותי) הם קטגוריה רגישה יותר מ-`matter` (מהות-כללית) — כל שימוש עתידי ב-narrative צריך סקירה נפרדת לפי-שדה, לא הנחה גורפת ש"כל dynField בטוח להצגה".

---

## 7. בדיקות חובה למימוש עתידי (לתכנון בלבד — לא נכתבו כעת)

1. קריאת-כשף נשמרת לארכיון עם `method: "kashf"`, `topicId`, `clientContext`, `conclusionSummary`.
2. קריאת-חאווי ממשיכה להישמר בדיוק כפי שהיא היום (רגרסיה על `saveGoralReadingToArchive` הקיים) — כולל רשומות-ישנות-בלי-`method` שממשיכות להיקרא נכון.
3. לקוח עם `maritalStatus: 'single'`, `workStatus: 'employed'`, `hasChildren: 'no'` — הערכים האלה מגיעים בפועל עד `clientCtx` שנכנס ל-`buildKashfReading` (בדיקת-שרשרת מקצה-לקצה, לא רק unit).
4. `dynFields` (כגון `matter`/`symptoms`) מגיעים בפועל עד `clientCtx`.
5. `age` ריק (`null`) לא שובר שום קריאה קיימת — כולל נושאים שאין להם היום כל טיפול ב-age.
6. `age` עם ערך אמיתי לא "מודלף" לניסוח-ללקוח בלי שכבת-סינתזה מפורשת שמטפלת בו (כלומר: לא סתם `${age}` מוטמע איפשהו כברירת-מחדל).
7. `clientHistorySummary` לאחר-השינוי **לא מערבב** חאווי וכשף בלי סימון — `repeatedTopics`/`summaryHebrew` חייבים להבחין (או לפחות לתעד-בבירור) אילו קריאות ממקור `hawi` ואילו מ-`kashf`, כדי לא ליצור מצג-מטעה כמו "3 קריאות חוזרות בנושא X" כשבפועל 2 מהן חאווי ו-1 כשף בנושא-מקביל-אך-שונה.
8. אין שינוי-HTML לא-קשור — כל שינוי ב-`goral-hachol.html` מוגבל לאזור ה-profile-buttons בלבד (הוספת שדה-age), בלי לגעת בשום מבנה אחר בקובץ.
9. אין secret, אין deploy, אין AI חי — נבדק ידנית לפני כל commit עתידי, כמו בכל שלב קודם בסבב הזה.

---

## 8. סיכונים

| סיכון | הסבר | מניעה מתוכננת |
|---|---|---|
| שבירת ארכיון-חאווי | הוספת `method` עלולה לשנות את מבנה-הרשומה שקוד קיים (למשל תצוגת-ארכיון ב-UI) מצפה לו | `method` הוא שדה-נוסף בלבד, לא מחליף שדה קיים; רשומות-ישנות מטופלות כ-fallback ל-`"hawi"` (סעיף 4) |
| ערבוב היסטוריית חאווי/כשף בלי סימון | אם `summarizeGoralClientHistory` לא יתעדכן להתחשב ב-`method`, "3 קריאות חוזרות" עלול לכלול קריאות משתי-שיטות-שונות-לגמרי שאינן-ניתנות-להשוואה ישירה | חובה בדיקה #7 (סעיף 7); ה-summary העתידי צריך שדה נפרד (או תת-פירוט) לפי method |
| שימוש בפרטי-לקוח רגישים בניסוח-ללקוח | `maritalStatus`/`workStatus`/`dynField` בריאותי עלולים "לדלוף" לטקסט-ללקוח בלי סינון-מכוון | כל שימוש-בניסוח דרך שכבת-סינתזה מפורשת בלבד (כמו `kashf-commerce-smart-layer.js`), לא גישה-ישירה מתוך `TOPIC_GUIDANCE`; בדיקה #6 |
| `age` ריק/שגוי | קלט-מספרי חופשי (למשל "0" או "999") עלול להתפרש כערך-אמיתי | ולידציה בסיסית ב-UI (טווח סביר), ובכל מקרה `age == null` תמיד-מטופל כ"אין מידע" — לא הופך ל-0 |
| `dynField_*` רגישים מופיעים בטעות בפלט-ללקוח | שדות כמו `symptoms`/`treatment` הם תוכן-בריאותי חופשי — אם ייכתבו-אוטומטית ל-narrative בלי שכבת-ביניים, זו חשיפת-מידע-רגיש ללא-כוונה | איסור מפורש (סעיף 6) על הצגה-אוטומטית; רק דרך שכבת-סינתזה נבדקת לכל שדה בנפרד |

---

## 9. המלצה — סדר ביצוע מומלץ (לא בוצע)

**סדר מוצע: א → ד(חלקי, context בלבד, בלי narrative) → ב → ג(dynFields) → ד(narrative usage מלא)**, ובפירוט:

1. **(א) שמירת-כשף לארכיון קודם לכול.** זו התשתית שכל השאר תלוי בה (בלי זה, `clientHistorySummary` לעולם לא ישקף כשף, לא משנה מה עוד מתווסף) — וגם הכי-מבודד: לא נוגע ב-`clientCtx` הנכנס ל-`buildKashfReading` כלל, רק מוסיף שמירה **אחרי** שה-reading כבר חושב.
2. **(ג-חלקי) העברת `maritalStatus`/`workStatus`/`hasChildren`/`dynFields`/`parentName`/`quesitedName`/`phone`** — הרחבת נקודת-ה-`clientCtx` (`goral-app.js:1154-1160`) לכלול את כל מה ש-`getClientContext()` כבר בונה. סיכון נמוך יחסית כי המידע כבר-נאסף, רק לא-מועבר — אין שינוי-איסוף, רק שינוי-העברה.
3. **(ב) `age`** — דורש שינוי-UI אמיתי (שדה-קלט חדש ב-HTML) ושינוי-`profileState`, לכן מגיע אחרי (2) ולא לפניו: פחות דחוף (אין היום אף שימוש שממתין ל-age), ודורש תשומת-לב-נפרדת ל-validation ול-UI.
4. **(ד-מלא) narrative usage** — שימוש בפועל ב-`maritalStatus`/`age`/`dynFields` בתוך `kashf-narrative-writer.js` (או שכבת-סינתזה חדשה/מורחבת) — **אחרון בכוונה**: זה השלב הכי-רגיש (סיכוני "דליפת-מידע-רגיש" ו"המצאת-ניסוח"), והוא היחיד שדורש בחינה-per-topic (בדומה לתהליך שכבר נעשה ל-commerce) — לא הגיוני לבנות אותו לפני שהמידע-עצמו כבר-זורם-ומאומת (שלבים 1-3).

כל אחד מ-4 השלבים האלה הוא **החלטת-מימוש נפרדת שדורשת אישור-מפורש משלה** — התוכנית הזו רק קובעת סדר, לא מאשרת ביצוע.

---

## הצהרות

- שום קוד לא נכתב/שונה. שום `goral-app.js`/מנוע/HTML לא נערך. שום UI חדש לא נוצר.
- שום AI חי, secret, storage/database/embeddings. שום נגיעה ב-`inner-compass`.
- שום commit/push — רק יצירת קובץ המסמך הזה.
- הצעד הבא — לפי החלטת אורן משה בלבד.
