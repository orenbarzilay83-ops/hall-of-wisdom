# AI_RUNTIME_GORAL_HACHOL_FIRST_PLAN — שינוי-סדר-עדיפויות: גורל החול (חאווי) ראשון, כשף שני, קלפים שלישי

> **מסמך תכנון בלבד. לא בוצע קוד, לא deploy, לא מפתח, לא AI חי, לא שינוי-HTML, לא שינוי-מנועים, לא נגיעה ב-inner-compass.**
> תאריך: 2026-07-08. מחליף את סדר-העדיפויות שנקבע ב-#27/#28 (`OREN_EXISTING_AI_API_AUDIT.md` סעיף 8, "קלפים כמימוש ראשון") — **בהחלטה מפורשת של אורן משה**.

---

## 1. שינוי-סדר-העדיפויות הנוכחי

עד כה (WORKPLAN #27–#29): קלפים (Cartomancy) שימשו כ"מודול-מבחן" למבנה החדש, כי ל-`inner-compass` יש כבר אינטגרציית-Anthropic עובדת שאפשר ללמוד ממנה דפוס (`OREN_EXISTING_AI_API_AUDIT.md`). זה נשאר נכון כרעיון — אבל **סדר-המימוש בפועל משתנה**:

1. **גורל החול / חאווי — יעד-המימוש הראשון.**
2. **כשף — שני.**
3. **קלפים — רק אחר-כך.**

**מה שכבר נבנה (שלבים 0–6, `ai/provider/anthropic-provider.js`, `ai/prompts/cartomancy-runtime.md`, שלד `supabase/functions/oren-smart-ai/index.ts`, בדיקת-mock) נשאר תשתית/מבנה-ייחוס בלבד** — לא נמחק, לא משוכתב. `anthropic-provider.js` בפרט הוא כללי-לגמרי (מקבל `model`/`system`/`userMessage` כפרמטרים, לא ייעודי לקלפים) ומתאים ללא שינוי גם לחאווי/כשף. `cartomancy-runtime.md` נשאר כמות שהוא, כ"פרומפט ראשון שנכתב" — ייכנס לשימוש בפועל רק כשקלפים יגיעו לתורם.

**המשמעות המעשית:** הצעד הבא בפועל הוא לכתוב תוכן אמיתי ל-`ai/prompts/hawi-runtime.md` (כרגע stub, ראה #28) — **לא** לגעת ב-`cartomancy-runtime.md` ולא להתקדם לסנכרון/deploy (שלב 6 שכבר תוכנן ונדחה, `AI_RUNTIME_PHASE_6_SHARED_SYNC_PLAN.md`) לפני שיש בכלל תוכן אמיתי במודול הראשון בעדיפות.

## 2. ה-prompt האמיתי הראשון

`ai/prompts/hawi-runtime.md` — יוחלף מ-stub לתוכן-prompt אמיתי, **רק לאחר אישור נפרד** (סעיף 10). זהו יעד-המימוש הראשון בפועל.

## 3. ה-prompt השני, בהמשך

`ai/prompts/kashf-runtime.md` — יוחלף מ-stub לתוכן אמיתי **רק אחרי** ש-`hawi-runtime.md` נכתב, נבדק (mock, כמו בשלב 4), ואושר על-ידי אורן משה. לא בו-זמנית לחאווי — נושא בזה-אחר-זה, כדי לאפשר ביקורת-איכות נפרדת לכל שיטה (חאווי וכשף הם שני מנועים שונים לגמרי, עם מבני-נתונים שונים — ראו `CLAUDE.md`: "כשף ≠ חאוי").

## 4. מה ה-AI מקבל — קלט סגור מהמנוע הדטרמיניסטי בלבד

ה-AI (כש-יופעל, בעתיד) מקבל אך ורק שדות שכבר חושבו ונקבעו על-ידי המנוע הקיים — לא גולמי-לוח, לא נוסחאות. לפי הזרימה הקיימת (`CLAUDE.md`, "Data Flow"):

**מחאווי** (`hawi-interpreter.js::interpretHawiQuestionInitial` → `goral-conclusion-writer.js::writeHumanGoralConclusion`):
- שאלת השואל (טקסט חופשי, כפי שהוזן).
- שיטה נבחרת: `"hawi"` (קבוע בהקשר הזה — המסמך הזה עוסק רק בענף חאווי; כשף יקבל prompt/הקשר-קריאה נפרד לגמרי בשלב הבא).
- `topicId` (מזהה-הנושא הפנימי, לא מוצג ללקוח).
- פסיקה קצרה (כן/לא/מעורב, `isPositive`/`isNegative`/פולריות-מעורבת, כפי שכבר מחושבת בקוד).
- בתי-הלוח הרלוונטיים לנושא — שם-הצורה (`figureHebrew`) + `fortune` לכל בית, כבר-מחושבים (לא נקודות-יסוד גולמיות).
- עדים ודיין (בית 15/16 + שני העדים) — שם-צורה + פסיקה, לא חישוב-מחדש.
- מסקנת-המנוע הקיימת כפי שכבר נכתבת על-ידי `writeHumanGoralConclusion`/`narrative-fact-phrasing.js` (זו "העובדה" שה-AI מנסח-מחדש, לא מחליף).
- אבחון-ליועץ-בלבד, אם כבר הופק על-ידי המנוע (למשל ממצאי `goral-spiritual-diagnostics-engine.js`) — מועבר כשדה-קלט נפרד, לא מעורבב עם הטקסט-ללקוח.
- אבחון רוחני (אם כבר הופק על-ידי המנוע) — אותו עיקרון: קלט-נפרד, לא מחושב-מחדש על-ידי ה-AI.

**מכשף** (בשלב 3 — סעיף 3 למעלה): מבנה מקביל אך לא-זהה, לפי שדות `kashf-reading-engine.js` (`primaryFormula`/`altFormula`/`keyHouseReadings`/`supportingFindings`/דמיר) — ייקבע במדויק כשייכתב `kashf-runtime.md` בפועל, לא כאן.

**הערה חשובה:** רשימת-השדות המדויקת (שמות-property אמיתיים בקוד) תאומת מול `hawi-interpreter.js`/`goral-conclusion-writer.js` **בזמן כתיבת ה-prompt בפועל**, לא מומצאת כאן מראש — המסמך הזה קובע את *העיקרון* (אילו סוגי-מידע, לא את החוזה-הטכני המדויק).

## 5. מה ה-AI אסור לו לחשב

- **אין חישוב-לוח חדש** — לא בונה/משנה דמיר, מהוו-ת'אבת, עדים, או כל נגזרת-לוח אחרת.
- **אין פסיקת-בית חדשה** — לא קובע האם בית מיטיב/מזיק/ממוזג; זה כבר קבוע מהמנוע.
- **אין שינוי-פסיקה** — לא הופך "כן" ל"לא" או להפך, לא "מרכך" פסיקה לכדי שינוי-משמעות.
- **אין שינוי-דיין** — הדיין (הפסיקה הסופית-מסכמת) נשאר כפי שהמנוע קבע.
- **אין שינוי כללים-ממוקרים** — לא מפרש-מחדש מה צורה "אומרת" לפי ידע כללי/הכשרה; רק לפי מה שנמסר לו כעובדה מהמנוע.
- **אין ידע מומצא** — עקרון-הברזל של `CLAUDE.md` ("No Invented Data") חל על ה-AI בדיוק כמו על כל שורת-קוד אחרת באפליקציה. אם שדה חסר בקלט — ה-AI לא ממציא אותו, פשוט לא מזכיר אותו.

## 6. מה ה-AI כן עושה — ניסוח/ארגון/עריכה בלבד

- **אבחון-ליועץ-בלבד** — ניסוח-מחדש לקריאה שוטפת של האבחון הקיים (לא צנזורה, לא ריכוך — ראו סעיף 8).
- **מסקנה ללקוח** — אריגת העובדות שנמסרו (פסיקה, בתים רלוונטיים, עדים/דיין) לנרטיב עברי זורם, בטון-יועץ (סעיף 7).
- **המלצה מעשית** — ניסוח צעד-המשך אחד לפחות, מבוסס אך ורק על מה שהמנוע כבר קבע (לא המלצה עצמאית של ה-AI).
- **טקסט-דוח להדפסה (אופציונלי)** — גרסה מאורגנת-להדפסה של אותן עובדות, אם וכאשר ייבנה UI כזה (לא בשלב הזה).

## 7. סגנון מחייב

עברית ישראלית טבעית, טון יועץ-רוחני מקצועי — **בדיוק** `OREN_CONCLUSION_STYLE_SPEC.md` הקיים בריפו, לא מסמך-סגנון נפרד:

- **לא רובוטי, לא שפת-מרפאה-פסיכולוגית.**
- ניסוחים אסורים: "המערכת מזהה", "קיימת אינדיקציה", "הנתונים מצביעים", "בהתבסס על הנתונים".
- ניסוחים מועדפים: "אני רואה כאן…", "התמונה מראה…", "יש כאן סימן ש…", "זה לא נותן תשובה חלקה…", "ללקוח הייתי אומר את זה בצורה עדינה יותר…" (בשכבת-היועץ בלבד).

`ai/prompts/hawi-runtime.md`, כשייכתב בפועל, יצטט/יפנה במפורש למסמך הזה — לא ישכפל אותו כדי למנוע שני מקורות-אמת לאותו כלל-סגנון.

## 8. חומר רוחני רגיש

אם נתוני-המנוע/המקור כוללים כישוף, עין הרע, קנאה, חסימה רוחנית, מס, ג'ין, שדים, רוחות, קללות — **האבחון-ליועץ-בלבד שומר על התוכן במלואו**, בלי הסרה ובלי ריכוך (בהתאם ל-`AGENTS.md` כלל 4 ו-`OREN_CONCLUSION_STYLE_SPEC.md` שכבה 1). בטקסט-ללקוח: מותר ניסוח מקצועי-רך יותר ("יש כאן משהו שדורש בדיקה רוחנית"), **אבל אסור למחוק את המשמעות** — זו בדיוק ההבחנה שכבר קיימת במסמך-הסגנון בין "ריכוך לגיטימי" ל"השמטה".

## 9. Fallback

אם קריאת ה-AI נכשלת (מכל סיבה — מפתח חסר, timeout, תגובה ריקה, שגיאת-רשת) — **מוחזרת מסקנת-המנוע הדטרמיניסטית הקיימת של גורל החול כמות שהיא**, בדיוק כפי שהיא מוצגת היום ללא AI. אין מסך-שגיאה, אין תשובה ריקה, אין ניחוש. זהו אותו עיקרון-fallback שכבר ממומש ב-`ai/provider/anthropic-provider.js`/`supabase/functions/oren-smart-ai/index.ts` הקיימים (מוחזר `{ok:false, fallback:true, conclusion:engineConclusion}`) — לא נדרש שינוי-מבנה, רק חיבור עתידי ל-`engineConclusion` האמיתי של חאווי במקום פלייסהולדר.

## 10. שערי-אישור מחייבים

כל אחד מהצעדים הבאים דורש אישור נפרד ומפורש של אורן משה **לפני** ביצוע — אף אחד מהם לא בוצע ולא יבוצע כתוצאה-לוואי מהמסמך הזה:

- לפני כתיבת תוכן prompt אמיתי לחאווי (`hawi-runtime.md`).
- לפני כתיבת תוכן prompt אמיתי לכשף (`kashf-runtime.md`).
- לפני יצירת sync script / `supabase/functions/_shared/` (כבר תוכנן ונדחה ב-`AI_RUNTIME_PHASE_6_SHARED_SYNC_PLAN.md` — נשאר נדחה).
- לפני `deploy` בפועל.
- לפני הוספת secret אמיתי.
- לפני חיבור כפתור/UI באתר.
- לפני כל קריאת-AI חיה ראשונה.

---

## הצהרות

- שום קוד לא נכתב/שונה בשלב הזה.
- שום prompt אמיתי לא נכתב (לא לחאווי, לא לכשף) — `hawi-runtime.md`/`kashf-runtime.md` נשארים stub עד אישור נפרד.
- שום deploy, secret, או חיבור-AI חי לא בוצעו.
- שום HTML/מנוע לא נערך.
- שום נגיעה ב-`inner-compass`.
- הצעד הבא — לפי החלטת אורן משה בלבד.
