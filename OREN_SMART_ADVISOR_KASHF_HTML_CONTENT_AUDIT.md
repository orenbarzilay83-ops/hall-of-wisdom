# OREN_SMART_ADVISOR_KASHF_HTML_CONTENT_AUDIT — מה בדיוק מכיל `kashf-al-asrar.html`

> **מסמך בדיקה בלבד. לא נכתב קוד, לא שונה HTML, לא שונה מנוע, לא נוצר storage/database/embeddings, לא בוצע ingest, לא נוסף AI חי, לא נוסף secret, לא בוצע deploy, לא נמחק/הועבר קובץ, לא נגע ב-`inner-compass`. לא בוצע commit/push — רק יצירת הקובץ.**
> תאריך: 2026-07-08. המשך ישיר מ-`OREN_SMART_ADVISOR_EXISTING_GORAL_KNOWLEDGE_AUDIT.md`. הקובץ הערבי (`kashf-al-asrar-arabic-only.html`) מוזכר כאן **רק לציון-קיום ולהשוואת-טווח-עמודים** — לא נבדק/נבחן כמקור, לא נעשה בו שימוש, מעמדו נותר בלתי-מוכרע כפי שכבר תועד.

---

## 1. מהו `kashf-al-asrar.html`

**קובץ HTML סטטי טהור — 0 תגי `<script>`** (נבדק: `grep -c "<script"` → 0). זה **לא** מקור-נתונים-שהמנוע-קורא-בזמן-ריצה — אין import אליו משום קובץ-JS או HTML אחר בריפו. הוא ספר-מודפס-בעיצוב-הדפסה: `<title>` = **"חשיפת הסודות הנצורים — בהוצאת המחשבות הגנוזות לאור | מהדורה עברית מלאה"**, עם עמוד-כריכה (`id="cover"`), תוכן-עניינים-מלא (3 עמודי-TOC), ו-264 עוגני-`id` (256 עוגני-עמוד `p21`–`p276` + כמה עוגני-מבנה כמו `cover`/`astrology-glossary`).

**כן כולל שערים/פרקים/TOC — במלואם.** ה-TOC חושף **את ששת השערים** (לא רק שער 6, שהוא היחיד-המחובר-כרגע-למנוע-החי — ראו סעיף 3): שער 1 (בתים), שער 2 (צורות), שער 3 (16 סדרי-שיבוץ), שער 4 (גילוי-הכוונה-הנסתרת), שער 5 (ארבע-צורות-היסוד), שער 6 (12 פרקי-הבתים), ועוד נספח-מונחי-אסטרולוגיה.

**מסקנה לשאלה "תצוגה או מקור-ידע":** **שניהם** — זהו קובץ-תצוגה-מובהק (עיצוב-הדפסה, אין נתונים-מובנים-לשליפה), אך תוכנו הוא **מקור-ידע-מקורי-ואמיתי** (לא ציטוט-משני) — ראו סעיף 3, שם מוכח שקוד-חי כבר נכתב **ישירות מתוך** הקובץ הזה.

## 2. השוואה מול `kashf-al-asrar-book.js`

**נבדק ישירות — טווח-העמודים כמעט זהה, לא זהה-לחלוטין:**
```
kashf-al-asrar.html:     256 עמודים (21–276)
kashf-al-asrar-book.js:  260 רשומות (21–280)

עמודים ב-HTML שאינם ב-JS:  0
עמודים ב-JS שאינם ב-HTML:  4  (277, 278, 279, 280)
```
4 העמודים הנוספים ב-`kashf-al-asrar-book.js` **מתועדים בהערת-הראש של הקובץ עצמו**: *"עמודים 277–280: נשמרו מגרסה קודמת (תוכן עניינים וסיום)"* — כלומר שיירי-TOC/סיום מגרסה-ישנה-יותר של הספר, לא תוכן-מהותי חדש.

**האם התוכן זהה, לא רק טווח-העמודים?** **כן, נבדק ואומת מילה-במילה** על עמוד-מדגם (166, פתיחת שער 6): הטקסט ב-`hebrewTranslation` של `kashf-al-asrar-book.js` וב-`<p>` תחת `id="p166"` ב-`kashf-al-asrar.html` **זהים לחלוטין**, כולל אותה כותרת-פרק ("פרק כולל לסימנים רבים והשער השישי").

**מסקנה: `kashf-al-asrar-book.js` הוא כמעט-ודאי חילוץ ישיר מתוך `kashf-al-asrar.html`** (או שניהם מקור-משותף-זהה) — **לא שני מקורות-נפרדים-ותחרותיים**. אין כפילות-בעייתית, יש עקביות.

**מה נמצא רק ב-HTML ולא (עדיין) כרשומת-JS-נפרדת:** מבנה-ההדפסה עצמו (TOC, כותרות-שער מעוצבות, טבלאות-אותיות מעוצבות — ראו CSS `letter-shape-grid`/`letter-key-table` בקובץ) — אלה לא "תוכן חסר", אלא עיצוב-שלא-רלוונטי-לנתונים-מובנים.

## 3. השוואה מול קבצי-engine של כשף

**נבדק ישירות אילו קבצים מה-Gate-הראשונים (שערים 2, 3, 5 — שלא שער 6) כן-מחוברים בפועל:**

| קובץ | שער | מיובא ע"י מנוע חי? |
|---|---|---|
| `kashf-figure-attributes-gate2.js` | שער 2 (מעלה/מושב/גבול/מזג/חודשים) | **כן** — `kashf-dhamir.js` |
| `kashf-figure-classification-gate2.js` | שער 2 (מדרגות/מיטיב-מזיק/`FIGURE_SEEKING_SOUGHT`/`FIGURE_WHEEL_POSITION`) | **כן** — `kashf-topic-rules.js`, `kashf-book-additions.js` (imports ספציפיים-בשם, לא רק import-כללי) |
| `kashf-figure-descriptions-gate2.js` | שער 2 (תיאור-הצורות-כסדרן: צומח/פירות/מתכות/חיות/איברים/תיאור-אדם/שירים) | **לא — 0 קבצי-מנוע מייבאים אותו.** תוכן קיים, מובנה, מאומת — לא מנוצל |
| `kashf-shibutzim.js` | שער 3 (16 סדרי-שיבוץ) | **כן** — `kashf-dhamir.js`, `kashf-book-additions.js`, `kashf-leshon-hainyan.js`, `kashf-dhamir-type4-external.js` |
| `kashf-gate5-foundational-figures.js` | שער 5 (4 צורות-יסוד) | **כן** — `kashf-shibutzim.js`, `kashf-book-additions.js` |

**ממצא-מפתח, מתקן-עדכון ל-WORKPLAN #17:** WORKPLAN #17 (2026-07-07) תיעד ש-Gate2-קבצים "עדיין לא מחוברים לשום מנוע חי". **זה כבר לא מדויק** — לפחות 4 מתוך 5 הקבצים-שנבדקו כאן **כן מחוברים כיום** (ככל-הנראה מסבבים-מאוחרים-יותר, למשל #19 שתיעד חיווט-שער-5). **`kashf-figure-descriptions-gate2.js` הוא היחיד שנשאר-לא-מחובר** — לא כל הקבוצה.

**ממצא נוסף, ישיר וחשוב: `kashf-book-additions.js` מצהיר במפורש בהערת-הראש שלו** — *"פונקציות חדשות שנכתבו ישירות מתוך אימות מול kashf-al-asrar.html (עמ' 174, 272-274)"* — **הוכחה ישירה שהקובץ הזה כבר שימש כמקור-אימות לכתיבת-קוד-חי-ומחובר** (`kashf-book-additions.js` מיובא בפועל ל-`kashf-reading-engine.js`, שורה 91). זו לא "אפשרות עתידית" — זה כבר קרה.

**מה עדיין קיים ב-HTML/JS אך לא מנוצל:** `kashf-figure-descriptions-gate2.js` (כאמור) — תוכן-תיאורי עשיר (צומח/פירות/מתכות/חיות/איברים/תיאור-אדם/דין-כללי/שירים לכל 16 הצורות) שכבר מובנה-ומאומת אך לא מוזן לשום נושא-חי. **ידע עברי-מאושר-שכבר-קיים אך-לא-מנוצל: כן, בבירור, זהו הדוגמה המובהקת ביותר.**

## 4. מעמד הקובץ

**האם `kashf-al-asrar.html` מקור עברי מאושר, כי הוא התרגום של אורן משה?** ה-`<title>` שלו (`חשיפת הסודות הנצורים — ... מהדורה עברית מלאה`) תואם-בדיוק לשם-התרגום-העברי-המאושר שכבר נקבע ב-`OREN_SMART_ADVISOR_BRAIN_PLAN.md`. **בשילוב עם ההוכחה בסעיף 3** (קוד-חי כבר נכתב ישירות ממנו) — הקובץ הזה **הוא, הלכה-למעשה, כבר בשימוש כמקור-אמת לחלק מהמנוע**, גם בלי תהליך-ingest פורמלי. זה שונה-מהותית ממעמד הקובץ-הערבי (שטרם שימש-בכלל כמקור לשום קוד).

**האם להיחשב "ספר לקריאה" או "מקור-ידע"?** **שניהם, וההפרדה הזו כבר קיימת בפועל בריפו** — לא צריך ליצור אותה מחדש: `kashf-al-asrar.html` = ספר-לקריאה/אימות-אנושי (איך שהוא כבר שימש, לפי `kashf-book-additions.js`); `kashf-al-asrar-book.js` = data-למנועים (מערך `KASHF_AL_ASRAR_PAGES`, נצרך תכנותית). **הם לא מתחרים זה בזה — הם שני-ייצוגים של אותו-תוכן, לכל אחד שימוש שונה, ומתועדים כבר כזוגיים.**

## 5. מפת תוכן — שערים/פרקים לפי ה-TOC

| שער/פרק | טווח עמודים | מחובר למנוע? |
|---|---|---|
| מבוא | p21–24 | חלקית (טקסט קיים ב-`kashf-al-asrar-book.js`, לא נבדק שימוש-ספציפי) |
| פרק בהכאת הקו + פרק בהוצאת ההכאה + חשבון-4-היסודות | p25–42 | לא נבדק בסבב זה |
| **שער 1** — הבתים, תכונותיהם, טבעם וסדרם | p43–53 | לא נבדק בסבב זה |
| **שער 2** — הצורות: מדרגות/סיווג | p54–67 | **כן** (`kashf-figure-classification-gate2.js`) |
| **שער 2** — תיאור הצורות כסדרן (צומח/פירות/מתכות/חיות/איברים/תיאור-אדם/שירים) | p68–95 | **לא** (`kashf-figure-descriptions-gate2.js`, 0 מייבאים) |
| שער 2 — מעלות/מושב/מזג/חודשים/שמות | p97–103 | **כן** (`kashf-figure-attributes-gate2.js`) |
| **שער 3** — 16 סדרי-שיבוץ | p104–150 | **כן, חלקית** (`kashf-shibutzim.js`, סדרים 1–3 עיקריים; 4–15 ידע-רקע בלי-שימוש-מזוהה, לפי WORKPLAN #18) |
| **שער 4** — גילוי הכוונה הנסתרת (דמיר) | p151–160 | **כן, חלקית** (3/5 שיטות-דמיר פעילות + סוג 4 עם מקור-חיצוני-מגולה; סוג 5 לא-מיושם) |
| **שער 5** — ארבע צורות-היסוד | p161–165 | **כן** (`kashf-gate5-foundational-figures.js`) |
| **שער 6** — 12 פרקי-הבתים (הדין המפורט) | p166–276 | **כן, במלואו** — זה בדיוק המקור ל-`KASHF_TOPIC_RULES` (29 נושאים חיים) |
| נספח מונחי-אסטרולוגיה | astrology-glossary | לא נבדק בסבב זה |

## 6. סיכונים

- **כפילות מול `kashf-al-asrar-book.js`:** נמוך בפועל — אומת שהתוכן זהה (סעיף 2), לא כפילות-סותרת. הסיכון האמיתי הוא **לא-לדעת** שהם זהים ולטפל בהם כשני-מקורות-נפרדים בטעות.
- **חוסר-התאמה בעמודים:** נמוך — 0 עמודים חסרים ב-JS מול-ה-HTML; 4 העמודים העודפים ב-JS מתועדים ומוסברים.
- **תוכן שלא מחובר למנוע:** **קיים בבירור** — `kashf-figure-descriptions-gate2.js` (שער 2, p68-95) הוא הדוגמה המובהקת: תוכן-עשיר, מאומת, ולא-בשימוש.
- **שימוש כפול באותו ידע:** לא נמצא סיכון-ממשי כרגע — `kashf-book-additions.js` מצטט את ה-HTML כמקור-אימות, לא כמקור-תוכן-כפול-שמוזן-פעמיים למנוע.
- **בלבול בין ספר-עברי, data-עברי, וקובץ-ערבי:** **הסיכון האמיתי-ביותר שנותר.** שלושה קבצים בשמות-דומים-מאוד (`kashf-al-asrar-book.js`, `kashf-al-asrar.html`, `kashf-al-asrar-arabic-only.html`), שניים-מהם עברית-זהה-בתוכן, אחד ערבי-במעמד-שונה-לגמרי (טרם-מאושר) — ללא תיעוד-מפורש-במקום-אחד המסביר את שלושתם יחד, בינה-פנימית (או מפתח אנושי) עלולה לבלבל ביניהם.

## 7. המלצה

לפי הבדיקה בלבד (לא לפי הנחה):

1. **לא לבנות "מפת ידע עברית" מאפס** — היא כבר קיימת-בפועל, רק לא-מתועדת-במקום-אחד. הצעד הנכון הוא **לתעד את המיפוי שכבר נמצא** (סעיפים 2–3+5 של המסמך הזה) כמסמך-ייחוס-קבוע, לא לבנות-מחדש.
2. **ליישר `book.js` מול ה-HTML — לא נדרש תיקון-תוכן** (הם כבר זהים, סעיף 2) — רק **לתעד את הקשר-ביניהם בפירוש בקוד עצמו** (הערת-ראש ב-`kashf-al-asrar-book.js` שמפנה ל-`kashf-al-asrar.html` כמקור-שממנו-חולץ, ולהפך) — שינוי-תיעוד-בלבד, לא תוכן.
3. **לסמן חלקים לא-מנוצלים — כן, זה הצעד הבעל-הערך-הגבוה-ביותר שזוהה כאן:** `kashf-figure-descriptions-gate2.js` (שער 2, p68-95) הוא מועמד-ברור-ומוכן לחיווט לנושא חי (למשל: תיאור-אדם/גנב/נעדר, שכבר יש להם checkType דומה לפי WORKPLAN #17) — **תוכן קיים, לא ספר-חדש**.
4. **לבדוק את 12 קבצי-ה-engine באופן שיטתי-ומלא — מומלץ כצעד-הבא-שלישי, לא ראשון.** הבדיקה-הזו (סעיף 3) כבר בדקה imports-ספציפיים ל-5 קבצי-שער-2/3/5, אך לא ביצעה סקירה-שיטתית-מלאה של כל 12 קבצי ה-`kashf-*.js` מול כל 6 השערים — זה scope גדול-יותר שראוי-לסבב-נפרד.

**המלצה מפורשת לסדר:** #3 (חיווט `descriptions-gate2` שכבר-קיים) לפני #4 (סקירה-שיטתית-מלאה) — כי #3 הוא ניצחון-קטן-ומוכן-לביצוע עם ידע-שכבר-מאומת, בעוד #4 הוא מיזם-סקירה-גדול-יותר.

---

## רשימת קבצים שנבדקו בפועל

`kashf-al-asrar.html` (מבנה, TOC, `<script>`-count, עוגני-עמוד, דגימת-תוכן p166), `goral-hachol/data/sources/kashf-al-asrar/kashf-al-asrar-book.js` (טווח-עמודים, דגימת-תוכן p166 להשוואה), `kashf-al-asrar-arabic-only.html` (רק טווח-עמודים, לציון-קיום בלבד — לא נבדק תוכן/מעמד), `goral-hachol/data/sources/kashf-al-asrar/kashf-figure-attributes-gate2.js`, `kashf-figure-classification-gate2.js`, `kashf-figure-descriptions-gate2.js`, `kashf-shibutzim.js`, `kashf-gate5-foundational-figures.js` (בדיקת-imports לכל אחד), `goral-hachol/engine/kashf-topic-rules.js`, `kashf-book-additions.js`, `kashf-dhamir.js`, `kashf-leshon-hainyan.js`, `kashf-dhamir-type4-external.js`, `kashf-reading-engine.js` (בדיקת מי-מייבא-את-מי, וקריאת הערת-הראש של `kashf-book-additions.js`).

---

## הצהרות

- שום קוד לא נכתב, שום HTML/מנוע לא נערך, שום storage/database/embeddings/ingest לא בוצעו.
- שום קובץ לא נמחק/הועבר. שום AI חי/secret/deploy לא נוספו. שום נגיעה ב-`inner-compass`.
- הקובץ הערבי (`kashf-al-asrar-arabic-only.html`) הוזכר רק לציון-קיום/טווח-עמודים — לא נבדק כמקור, לא נעשה בו שימוש, מעמדו נותר בלתי-מוכרע.
- שום commit/push — רק יצירת קובץ המסמך הזה.
- הצעד הבא — לפי החלטת אורן משה בלבד.
