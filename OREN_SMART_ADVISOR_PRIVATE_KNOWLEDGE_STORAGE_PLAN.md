# OREN_SMART_ADVISOR_PRIVATE_KNOWLEDGE_STORAGE_PLAN — תכנון אחסון הידע הפרטי

> **מסמך תכנון בלבד. לא נכתב קוד, לא נוצר storage בפועל, לא נוצרו טבלאות, לא נוצרו migrations, לא נוצרו embeddings, לא הועלו ספרים, לא נוצר UI, לא נוסף AI חי, לא נוסף secret, לא בוצע deploy, לא שונה HTML, לא שונה מנוע, לא נגע ב-`inner-compass`. לא בוצע commit/push — רק יצירת הקובץ.**
> תאריך: 2026-07-08. המשך ישיר מ-`OREN_SMART_ADVISOR_OPEN_DECISIONS.md` סעיף 1 ("מיקום-אחסון הידע הפרטי"), אחרי ש-`OREN_SMART_ADVISOR_ACCESS_CONTROL_AUDIT.md` כבר תיכנן את שכבת-ההגנה (allowlist-UID בצד-שרת).

---

## 1. השוואת אפשרויות אחסון

| אפשרות | יתרון | חיסרון |
|---|---|---|
| **Supabase Storage בלבד** | טבעי לקבצים גולמיים (PDF/תמונות-סריקה/OCR-גולמי); כבר חלק מהפרויקט הקיים | לא-מובנה לחיפוש-לפי-שדה (עמוד/נושא/שיטה) בלי שכבה נוספת מעליו |
| **Supabase Database בלבד** | חיפוש-מובנה לפי ספר/עמוד/נושא/שפה/שיטה; תואם למבנה שכבר קיים בקוד (`KASHF_AL_ASRAR_PAGES` — מערך-רשומות עם בדיוק אותם שדות) | לא מתאים-לבד לקבצים-גולמיים גדולים (PDF/תמונות) — טבלה היא לא מקום טבעי לבלוב-בינארי גדול |
| **שילוב Storage + Database** | כל צד עושה את מה שהוא טוב-בו: Storage לקבצים-גולמיים, Database ליחידות-הידע-המפורקות-והניתנות-לחיפוש | מורכבות-קלה יותר (שני רכיבים, לא אחד) — אך שניהם כבר בתוך אותו פרויקט-Supabase קיים, לא ספק נוסף |
| **קבצים פרטיים מחוץ ל-Supabase (למשל דיסק-ענן נפרד)** | הפרדה מוחלטת מכל תשתית-האתר | מערכת-נוספת-לתחזק; לא-משולב עם login/Edge-Function הקיימים; דורש אינטגרציה נפרדת רק כדי לקרוא ממנו |
| **Vector search / embeddings** | חיפוש-סמנטי (לא רק מילות-מפתח) כשמאגר-הידע גדול | **לא רלוונטי לשלב-הראשון בכלל** — תוספת-מורכבות בלי צורך-מיידי; נדחה במפורש לעתיד (סעיף 2) |

## 2. המלצה ראשית — שילוב Storage + Database

**Supabase Storage** לקבצי-מקור פרטיים (PDF, תמונות-סריקה, OCR-גולמי-לא-מעובד אם נדרש לשמור אותו כגיבוי) — bucket **פרטי** (לא public), נגיש רק דרך Edge Function מאומתת.

**Supabase Database** ליחידות-ידע קטנות ומפורקות: metadata, ציטוטים, סטטוס-אישור — בדיוק כמו שכבר עובד היום ב-`kashf-al-asrar-book.js` (`KASHF_AL_ASRAR_PAGES`, מערך-רשומות `{page, chapter, arabicText, hebrewTranslation, topics, sourceStatus}`), רק בטבלת-DB-פרטית במקום קובץ-JS-בריפו-ציבורי.

**Embeddings — רק בשלב מאוחר-יותר, לא עכשיו.** אין צורך בהם כדי שהבינה תהיה שימושית בהתחלה (חיפוש לפי שדות-מובנים — ספר/עמוד/נושא/שיטה — מספיק לשלב-הראשון); נשקול אותם רק כשמאגר-הידע גדול-מספיק שחיפוש-מילות-מפתח כבר לא מספיק.

**תשובה מפורשת לשאלה שהתבקשה בסיום:** **שילוב** (Storage + Database) — לא אחד-בלבד. הנימוק: קבצים-גולמיים (PDF/סריקות) לא שייכים בטבלה, ויחידות-ידע-ניתנות-לחיפוש לא שייכות ב-Storage — כל רכיב עושה את מה שהוא-טוב-בו.

## 3. למה לא GitHub

לא מכניסים ספרים מלאים, PDF מלא, OCR מלא, או חומר-פרטי-אחר לריפו הציבורי (`hall-of-wisdom` ב-GitHub), משלוש סיבות:

1. **git history הוא צמית.** גם אם קובץ נמחק בקומיט-הבא, התוכן נשאר בהיסטוריה לצמיתות (אלא אם מבוצע history-rewrite מכוון) — בדיוק כמו שכבר קרה בפועל עם המפתח-שנמצא ב-`inner-compass/.env.local` (`OREN_EXISTING_AI_API_AUDIT.md`) — תקדים-אמיתי, לא תיאורטי.
2. **תוכן-ספרים-מלא הוא נכס שונה מקוד.** קוד-האפליקציה מתועד-ומאומת-לאורך-חודשים בדיוק כדי שכל שורה תהיה ניתנת-לביקורת (`sourceStatus` על כל רשומה) — אבל ספר-מלא-לא-מעובד הוא חומר-גולמי, לא-מתויג, לפני-בקרת-איכות; אין לו מקום בין קבצי-הקוד.
3. **היקף.** ספר סרוק מלא (עשרות/מאות עמודים, כולל תמונות) הוא נפח-קבצים שלא מתאים ל-git (שממוטב-לטקסט-קטן), ומכביד על כל `clone`/`fetch` עתידי של הריפו — כולל לכל סוכן-קוד שיעבוד על הפרויקט בעתיד.

## 4. מבנה ידע מוצע — metadata לכל יחידת-ידע

| שדה | תיאור |
|---|---|
| `sourceId` | מזהה-ייחודי ליחידת-הידע (למשל `kashf-p166-marriage-01`) |
| `sourceName` | שם-הספר המלא (לדוגמה "كشف الأسرار المصونة في إخراج الضمائر المخزونة" או "חשיפת הסודות הנצורים") |
| `method` | `hawi` / `kashf` / `other` — לעולם לא ערבוב (ראו סעיף 5) |
| `language` | `ar` / `he` — או שניהם אם יש שני שדות-טקסט (ראו `textChunk` למטה) |
| `pageNumber` | מספר-עמוד מדויק במקור |
| `topic` | תגית/תגיות-נושא (חופשי או מרשימת-נושאים-קיימת, כמו `topics` הקיים ב-`KASHF_AL_ASRAR_PAGES`) |
| `arabicTerm` | מונח/ים ערביים טכניים ספציפיים ליחידה, נשמרים תמיד לצד התרגום — לא מוחלפים |
| `hebrewTranslation` | התרגום העברי (של אורן משה, לכשף — ראו סעיף 5) |
| `textChunk` | הטקסט עצמו — יכול להיות שני שדות-משנה (`textChunkArabic`/`textChunkHebrew`) בהתאם למקור |
| `ocrQuality` | `"סרוק-נקי"` / `"סרוק-מטושטש"` / `"תמלול-ידני-מאומת"` — כדי שהבינה תדע-מתי-להזהיר |
| `approvalStatus` | `"pending"` / `"approved"` / `"rejected"` — הבינה משתמשת **רק** ב-`approved` (ראו סעיף 7) |
| `confidenceLevel` | נגזר מ-`ocrQuality`+`approvalStatus` — משמש לניסוח "רמת-ביטחון" בתשובה (סעיף 8) |
| `citationLabel` | מחרוזת-ציטוט מוכנה-מראש לשימוש-חוזר בתשובות (למשל `"כשף אל-אסראר, עמ' 166"`) — נגזרת מהשדות למעלה, לא מוזנת בנפרד |

מבנה זה **מרחיב** (לא מחליף) את המבנה שכבר קיים ב-`kashf-al-asrar-book.js` — אותם עקרונות, בטבלת-DB-פרטית במקום קובץ-ציבורי.

## 5. מדיניות מקורות לפי שיטה (ללא שינוי מהמאושר קודם)

- **כשף אל-אסראר:** עובדים עם שני המקורות יחד — המקור הערבי (كشف الأسرار المصونة في إخراج الضمائر المخزونة) והתרגום העברי של אורן משה (חשיפת הסודות הנצורים). הערבית מקור-יסוד, העברית מקור-עבודה-והבנה.
- **חאווי:** רק מקורות-חאווי שאורן משה אישר במפורש לשיטת חאווי.
- **כל מקור אחר:** לא נכנס למאגר-הידע בלי אישור מפורש וחדש מאורן משה, לכל מקור בנפרד.
- **אין לערבב בין חאווי לכשף** — שדה `method` (סעיף 4) אוכף את ההפרדה הזו ברמת-הנתונים עצמה, לא רק כהוראה.

## 6. הגנה והרשאות

הגישה לידע הפרטי **לעולם לא ישירה מהדפדפן** — לא קריאת-Storage/Database ישירה מ-JS-שרץ-בצד-לקוח, גם אם "רק אורן פותח את זה". בעתיד (כשיאושר), **רק Edge Function מוגנת** (בהתאם ל-`OREN_SMART_ADVISOR_ACCESS_CONTROL_AUDIT.md`) תוכל לקרוא את הידע:

1. כל קריאה בודקת **Supabase Auth** (session תקף) **וגם** **UID-allowlist של אורן משה** — שתי הבדיקות, לא רק אחת.
2. ה-UID לא יושב בשום מקום שהדפדפן/HTML/קוד-ציבורי יכול לקרוא — רק כ-secret/קונפיג בסביבת-ה-Edge-Function (כפי שכבר תוכנן ב-`OREN_SMART_ADVISOR_ACCESS_CONTROL_AUDIT.md` סעיף 5).
3. **הסתרת-UI אינה אבטחה** — אותו עיקרון שכבר נקבע, שוב רלוונטי: אם אי-פעם ייבנה מסך שמציג/מסתיר כפתור-גישה-לידע, זו נוחות-בלבד; ההגנה האמיתית תמיד בבדיקת-ה-Edge-Function, בכל קריאה.

## 7. תהליך הכנסת ספר בעתיד (שלבים מוצעים בלבד, לא מבוצע)

1. **אישור מקור** — אורן משה מאשר במפורש שהספר/המהדורה הספציפית מותרים-לשימוש (לפני כל פעולה טכנית).
2. **העלאת קובץ פרטי ל-Storage** — הקובץ הגולמי (PDF/סריקה) עולה ל-bucket הפרטי.
3. **חילוץ OCR / טקסט** — המרת-הקובץ-הגולמי לטקסט (תהליך נפרד, לא חלק מהאחסון עצמו).
4. **חלוקה ליחידות-ידע** — פירוק הטקסט המחולץ ליחידות אטומיות לפי מבנה סעיף 4 (ספר/עמוד/נושא).
5. **שיוך לשיטה** — כל יחידה מתויגת `hawi`/`kashf`/`other` (שדה `method`) — לא "גורל החול" סתמי.
6. **בדיקת איכות OCR** — כל יחידה מקבלת `ocrQuality` — לא מונח-אחיד-לכל-הספר, יכול-להשתנות-מעמוד-לעמוד.
7. **אישור ידני של אורן משה** — כל יחידה נשארת `approvalStatus: "pending"` עד שאורן בדק-ואישר אותה ידנית.
8. **רק אחרי-כן** — הבינה יכולה להשתמש ביחידה (רק `approvalStatus: "approved"`). שום שלב מהתהליך הזה לא מתבצע כאן — זו רשימת-שלבים-לעתיד בלבד.

## 8. ציטוטים

כל תשובה של הבינה שמתבססת על ידע-ספרותי חייבת לכלול, בלי-יוצא-מן-הכלל: **שם-מקור, שיטה, עמוד, שפה, רמת-ביטחון** (נגזרים ישירות משדות-המטא-דאטה בסעיף 4). אם אין מקור-מתאים-ומאושר לשאלה — התשובה היא **"לא נמצא במקורות המאושרים לשיטה הזאת."** — לא ניחוש, לא ידע-כללי-של-המודל.

## 9. סיכונים

- **חשיפת ספרים פרטיים** — אם ה-Storage/Database לא באמת פרטיים (bucket ציבורי בטעות, RLS-policy חסרה), התוכן הפרטי דולף החוצה.
- **ערבוב בין שיטות** — אם שדה `method` לא נאכף (רשומה בלי-שיטה, או שאילתה שלא-מסננת-לפי-שיטה), הבינה עלולה לצטט כשף כאילו הוא חאווי או להפך.
- **OCR שגוי** — טקסט-מחולץ-לא-מדויק שנכנס למאגר בלי סימון `ocrQuality` נכון, ומוצג-בביטחון-שווא.
- **שימוש במקור לא-מאושר** — יחידת-ידע נכנסת למאגר בלי אישור אורן משה (עקיפת-סעיף-7), ומשמשת את הבינה לפני שהיא אמורה.
- **תשובה בלי citation** — הבינה עונה על-שאלת-תוכן בלי לצרף שם-ספר+עמוד+שיטה (עקיפת-סעיף-8).
- **גישה למשתמש שאינו אורן משה** — אם ה-Edge Function לא בודקת UID-allowlist בכל קריאה (לא רק בטעינת-מסך), כל משתמש-מחובר-אחר בפרויקט (וכבר ידוע שיש כמה — `OREN_SMART_ADVISOR_ACCESS_CONTROL_AUDIT.md`) יכול לגשת לידע הפרטי.

## 10. החלטות שעדיין צריך לקבל

- האם מתחילים עם כשף או עם חאווי (איזו שיטה נכנסת ראשונה למאגר-הידע-הפרטי)?
- האם מתחילים מהתרגום-העברי או מהמקור-הערבי (לכשף — סדר-ההזנה, לא סדר-החשיבות; שניהם נשארים שווי-מעמד בסוף)?
- האם שומרים OCR-גולמי (הקובץ-הלא-מעובד) לצד הטקסט-המחולץ, או רק את התוצאה-הסופית?
- האם יוצרים טבלאות ידנית (דרך Supabase dashboard) או דרך migration-files מתועדים (שיכולים-להיכנס לריפו כקוד-סכמה, לא כתוכן-ספרים)?
- האם storage bucket יהיה **פרטי בלבד** — (ברירת-המחדל המחויבת, לפי סעיף 3/6 — יש לאשר מפורשות שאין תרחיש שבו הוא יהיה public)?
- מתי בכלל מוסיפים embeddings (אין תשובה נדרשת עכשיו — רק לאשר שזה נשאר "לא עכשיו", כפי שסעיף 2 קובע)?

## 11. המלצה לשלב הבא (בלי לבצע)

אחרי מסמך-האחסון הזה, הצעד הכי-בטוח הוא **לא** ליצור storage/טבלאות בפועל — אלא לחזור ולסגור את שאלות-ההכרעה בסעיף 10 (בירור, לא קוד), ובמקביל **לתעדף החלטה על סדר-הכנסת-השיטות** (חאווי-קודם או כשף-קודם) — כי היא קובעת איזה `method` ראשון בפועל ייכנס דרך תהליך-סעיף-7, וזה משפיע ישירות על התאמת-סדר-העדיפויות הזו לזו שכבר נקבעה ל-Runtime AI (`AI_RUNTIME_GORAL_HACHOL_FIRST_PLAN.md` — חאווי ראשון שם). **לא חובה** שהמוח-הפנימי ו-Runtime-AI ילכו באותו סדר-שיטות — אבל כדאי-להחליט-במודע אם כן או לא, לא להשאיר סתום.

---

## הצהרות

- שום קוד לא נכתב, שום storage/טבלה/migration לא נוצרו, שום ספר לא הועלה.
- שום UI, embeddings, AI חי, secret, deploy — לא נוצרו.
- שום HTML/מנוע לא נערך. שום נגיעה ב-`inner-compass`.
- שום commit/push — רק יצירת קובץ המסמך הזה.
- הצעד הבא — לפי החלטת אורן משה בלבד.
